# 换行符统一策略
#
# 本项目的编写机是 Windows、编译机是 macOS。
# 统一在仓库内使用 LF，避免每次跨机器 diff 出一堆"M 文件全部行都变了"的噪音。
# Swift / JS / Markdown / plist / yml 全部用 LF，Xcode 与 Node 都能正常读取。
* text=auto eol=lf

# 二进制文件不做任何换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.heic binary
*.mov binary
*.zip binary

# 批处理脚本例外：必须保持 CRLF。
# cmd 对 LF 结尾的 .cmd 支持不可靠（goto / 标签 / 括号块容易出错），
# 而上面那条 `* text=auto eol=lf` 会让 clone 下来的 .cmd 变成 LF。
# 下面这条要放在文件最后 —— .gitattributes 里后面的规则覆盖前面的。
#
# 另注：start-preview.cmd 还要求**以 GBK(936) 保存并首行 chcp 936**，
# 那是为了中文 Windows 的 cmd 码页；换行符由本文件管，编码由文件自身管，两者互不影响。
*.cmd text eol=crlf

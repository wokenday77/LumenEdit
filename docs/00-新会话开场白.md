# 00 · 新会话开场白（整段复制到新会话）

> 用法：**新建对话** → 把下面分隔线之间的**全部内容**复制粘贴进去 → 发送。
> 新会话的 AI 会自己去读那 4 份文件（都在同一个工作区里），不需要你再贴内容。

---

接着做 **LumenEdit** 项目（自用 iOS 修图相机 App，单人开发不上架）。

**项目根目录**：`D:\AI 工作存储\iPhone相机\`

## 一、请先按顺序读这 4 份文件

| # | 文件 | 作用 |
|---|---|---|
| 1 | `CONTEXT_HOT.md` | 项目当前状态（原型侧最新，含 9 轮迭代记录） |
| 2 | `docs/04-交接单.md` | 工程纪律 8 条 |
| 3 | `docs/03-布局调整方案.md` | 布局方案 |
| 4 | `docs/08-P2-Swift同步任务书.md` | **本次任务书**（13 项映射 + 第 2 批拆分 + 验证点） |

## 二、我的角色与必须遵守的纪律

- 我负责 **Swift 端** `LumenEdit/`；另一个 AI（**CB**）正在改网页原型 `prototype/index.html`
- ⚠️ **原型是只读参考** —— 绝不修改 `prototype/` 下任何文件
- ⚠️ **禁止 `git add -A`** —— 必须按路径明确列出，例如
  `git add LumenEdit/Camera/UI/TopBarView.swift`（原型正被 CB 改着，`-A` 会把它的半成品一起提交）
- Swift 改完必跑 `node tools/check_swift.js LumenEdit`
- 颜色用 **`Theme.Palette`**（**不是 `Theme.Color`**），尺寸用 `Theme.Size`
- **本机是 Windows，没有 Xcode：无法编译 Swift、无法截图。**
  → 每改完一件就 push，Mac 侧 AI 拉取后编译 + 真机截图，我根据截图迭代。
  → **所以每件都要做小**，不要一次吞多个组件。

## 三、本次任务：第 2 批（UI 骨架）5 个小件

**按顺序，一次做一件：**

| # | 小件 | 验证点 |
|---|---|---|
| **2-1** | `CaptureSessionMode` 加 `logLive` case（全仓排查 `switch` 引用点） | 编译通过；真机模式条出现第 3 档且可进入 |
| **2-2** | `TopBarView`（顶栏两行：L/R 电平表 + 模式条居中 + 三图标 / 影调预览 + 存储） | 真机截图对比原型；模式条居中 |
| **2-3** | `FocalStripView`（焦段条 13/24/48/120mm） | 药丸版式 + 选中态；点击有反馈 |
| **2-4** | `ToolIconRow`（底部图标行 7 项） | 7 项排布；**每项点击必须有反馈** |
| **2-5** | `ShutterRowView`（快门排四件套 + ⤢ 放大） | 四件套版式；快门居中；录制态正常 |

**几何全部相对布局**：圆盘圆心对齐「对焦」按钮**动态算**（用 `GeometryReader` /
`anchorPreference`），**不照搬**原型的 `--fd-center-x: 83.1px` 这类数值。

## 四、进度基线

- P1a 照片闭环 ✅ ／ P1b-1 Live Photo ✅ ／ **P1b-2 视频录制 ✅**（`MovieCaptureService`）
- **P2 数据层 ✅** —— `LumenEdit/Presets/` 6 个文件 + `tools/check_presets.js`，
  **与网页原型逐条一致**（14 滤镜 / 10 风格 / 5 场景 / 4 焦段）
- P2 硬件层 🔶 部分 —— 手动对焦（`setManualFocus`）、参数能力模型（`capabilities(of:)`）已做；
  平滑变焦（`Ramp`）待做
- **P2 UI 骨架 ⬜ ← 现在做这个**

## 五、第一步做什么

读完那 4 份文件后，**从 2-1 开始**：给 `CaptureSessionMode` 加 `logLive` case。

注意这个枚举被多处 `switch` 引用（`isImplemented` / `requiresMicrophone` / `displayName` /
`systemImageName`，以及 `CaptureSessionController` 里的模式分支），**加 case 后要全仓排查**，
漏一个分支就编译不过。这也正是把它排在第一位的原因 —— 能最快暴露接线遗漏。

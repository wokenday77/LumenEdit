# LumenEdit · 启动包（新会话唯一入口）

> **用法**：新建对话 → 把本文件**整份**贴给 AI → 发送。
> **收尾纪律（2026-09-20 用户明确要求，AI 自动执行）**：只要有交付/修 bug/落盘，收尾必须
> ① 更新本文第一（进度）/ 二（下一件事）/ 四（坑）节 ② 本文控制在 **150-200 行**（超了删旧内容，
> 过时的不留）③ CONTEXT_HOT 的 〇/Backlog/下一步 同步 ④ 更新完 push ⑤ 记当日日志。
> **最后更新：2026-09-21 午（批六**进行中、已提交本地未 push**，切模型后续做）**：4 条修复 ——
> ① 刻度条复刻飓风（代码+自检完，缺变异/docs/23）② 冷启动预热（代码完，缺守卫+真机验）
> ③ 点按对焦按意图重放手动档（**完**）④ 资源埋点（代码完，缺埋点完整性守卫）；⚠️ 未做：o1~o4 新守卫、变异测试、`docs/23`；`check_presets` 第 6 组处「Swift 先行」WARN 态

---

## 一、当前进度

- **A 组纯 UI：✅ 全部完成**（#6 场景·风格条 / #7 滤镜条 / #10 功能面板 / #11 格式芯片 / 底部三问题修复）· 2026-09-18 复验 10 项全过
- **B 组接线**：B1 焦段切镜头 + `Ramp` ✅ 过复验 / B2 刻度条三件套 ✅ 全交付过复验（`docs/16`）/
  B3 拍板全落（物理会话按需 / 手动档不锁焦段 / 点按只测光）/ B3a EV 盘 + B3b 对焦盘 ✅ 复验闭环
  （对焦档 = **意图驱动**，backlog ⑩）/ **物理架构改造（docs/20）✅ 复验核心全通 + 批二/三/四已修**
- **批五（已 push、**等 Mac 复验**，详见 `docs/22`）**：①静默路径退转场（格式缓存**落盘** + 入口**结果回执**
  替盲等 + 竞态死胡同）②刻度加第五档 `hair`（**⚠️ 已被批六作废**）③切模式参数回自动
  （真凶 = `commitConfiguration()` 返回 ≠ 系统完成 → `reverifyManualIntent` 四拍复查补写）；变异 35/35
- **批六（2026-09-21 午 · 用户 4 问题 + 飓风精度依据，**进行中**）**：
  - **① 刻度条复刻飓风（方案 A，用户拍板）——代码与自检已完成**：档距三条**统一 40pt**（飓风 120px@3x）/ 刻度
    **单档 14×1.33pt 每档同高**（五档层级 hair/minor/mid/major/preset 整组退场）/ **选中档 22pt + 加宽 2pt + 绿**
    当指示器（三角指针+竖线退场，**气泡保留**）/ **删档间细分线**（飓风 细:主=1:1）/ **每档全标签**（`labelAt` 退场，
    选中标签不放大）/ 刻度底↔标签顶 **13pt**（拍板"刻度在上、标签在下"）；快门表 15→**42 档**（补 1/3 档 + 影院值
    `1/96`、`1/120`；**删 `1/12000`**——7 字符 ≈43pt 在 40pt 档距下必叠字，拍板删）；ISO 末档 = **设备实读
    maxISO（12096，不是 12800）**，session 发布 `stripISOMax` 运行时覆盖末档；WB 预设位橙刻度保留。
    ⚠️ **旧 `reference/video-frames-2026-09-16` 的"9~10 条细刻度"口径作废**（错判参考图）
  - **② 冷启动预热（修 4.27s 静止帧）——代码完成**：`startPrewarmIfNeeded`（独立私有会话 + 一次性
    `AVCapturePhotoOutput` 判 Live，串行逐颗探格式写缓存）+ 三层兜底（`canAddInput` 假就跳过 /
    **`wasInterruptedNotification` 一到就拆预热且本次运行内不再试** / 开关 `lumen.camera.prewarm.disabled`）；
    只探缓存里没有的、缓存字典属主仍是 sessionQueue
  - **③ 点按对焦按意图重放手动档（「不选自动就永远手动」）——✅ 完成**：`focus(atDevicePoint:)` 点按前取
    意图快照 → 点按后按快照重放曝光/白平衡/EV（复用切模式那套写入）+ `reverifyManualIntent` 两拍复查 +
    「点按对焦后档位」日志；与方案 A 不冲突（那条解**对焦**轴，本条保**曝光/白平衡**轴）
  - **④ 掉帧排查（资源埋点）——代码完成**：input/output 增删**逐点计数**（11 处裸调用全埋）+
    `refreshResourceSummary`（活的 input/output 数超出该模式预期即 WARN）+ `memoryFootprintMB`
    （`phys_footprint`）+ 每 5s 周期日志 + 切模式/换设备**耗时**入日志；对称性**审计结论**：
    `reconfigureOutputsLocked`（先清空再挂）/ `configureAudioInputLocked`（守卫+对称）/ `performFocalSwitch`（remove+add 回滚）—— 结构上**没找到泄漏** → 要靠计数 + 复现脚本交 Mac 定性
  - ⚠️ **批六未完成**：`check_swift` 第 19 组 **o1~o4 守卫没写**（埋点完整性/预热齐备/点按重放/资源账）、**变异测试没跑**、`docs/23` 没写（Mac 复验口径初稿在本文第九节）
  - 自检现状：`check_swift` 全过（按旧守卫）；`check_presets` 过但第 6 组 **WARN**（Swift 先行）
- **Backlog（先不修）**：静默换形态只省"模糊"不省"耗时"（换 input 固有 0.6~0.75s）/ 预切换期间点跨镜头焦段会走换设备+转场 / **取景器双指缩放**（新功能）/ ⑥ 取景器与成片视场不一致（裁左右 ~39%）/ 🔴 **两个既有态 < 50%**：场景·风格展开 49.3% / 滤镜条展开 42.8%（844）——要动 147/144，**需拍板** / ⑤ 遮幅待复核
- B4（格式选择器接线 #11）/ B5（参数导入 #13，等 P4）：⬜ 待做

## 二、下一件事

1. 🔴 **先 push 再续**：批六进行中提交 `a94b59f` **已在本地**（D: `main` 与 worktree 一致、工作区干净）但 **push 卡网络未完成**（420s / 78 轮两条通道都没抢到窗口，错误只有 `CONNECT 502` / 连不上 443）→ 下一棒**先跑 `.workbuddy/push-when-open.py`** 把它推上 origin/main，然后：
   ① 写 `check_swift` 第 19 组 **o1~o4 守卫**：o1 埋点完整性（`session.addInput/removeInput/addOutput/removeOutput`
   每处裸调用的紧邻 3 行内必须有对应计数调用，**先剥 `//` 注释再匹配**）· o2 预热齐备（startPrewarmIfNeeded /
   prewarmProbe / observeInterruptionsIfNeeded / wasInterruptedNotification / 开关键）· o3 点按重放
   （methodBodyOf `focus`：快照 + 三路重放 + 「点按对焦后档位」）· o4 资源账（refreshResourceSummary 超预期
   WARN + memoryFootprintMB + `snapshotTick % 5`）；② o1~o4 做**变异测试**（注入坏数据验证真报错 + 未变异对照组）；
   ③ 写 `docs/23-批六方案与拍板.md`（4 条的方案/精度依据/拍板/复验口径）；
   ④ 收尾：本文第一/二/四节 + CONTEXT_HOT 〇 同步 → push
2. **刻度条路由已拍板（2026-09-21 用户选）=「Swift 先行 + 标记」**：Swift 按飓风口径先落地，
   `ParameterStripCatalog.isSwiftAheadOfPrototype = true` → `check_presets` 第 6 组降级 WARN 并打印差异清单
   （= CB 待办：ISO 12000→12096 / 快门 15→42 档 / slot 46·56·26→统一 40 / labelAt 机制退场改全标签）；
   **CB 同步完原型后把标记改回 `false`，本组自动收紧为逐条全等**（与 FOCALS 的 `isSwiftExtension` 同款机制）
3. **Mac 复验**：批五 3 条（见第九节）+ 批六 4 条（口径初稿在第九节）；批五的"刻度更细"验收**已作废**，改按批六（单档 14 / 选中 22 绿 / 无细分线 / 全标签 / 40pt 档距）。补测：录制中跨镜头拒绝。**下一批候选**：两条既有态 < 50% / 取景器双指缩放

## 三、工程纪律（8 条 · 摘自 `docs/04`，完整版读那份）

1. **分层**：UI（View/VM）不碰设备；设备一切写操作只走 `CaptureDeviceConfigurator`
2. **唯一锁**：`beginConfiguration` / `lockForConfiguration` 只在 configurator（自检会拦）
3. **事件驱动不轮询**：硬件变化由会话 `@Published` 主动发布；例外只有每秒 `refreshSnapshot()`（只写日志）
4. **单一真源**：预设数据（滤镜/风格/场景/焦段/格式/**刻度条**）以原型为真源，`check_presets.js` 逐条比对
5. **浮层互斥逐点接线**：新增浮层必须同时进 `collapseOverlays()` 与 `dismissTransientPopovers()`，且**展开者自己不能调后者**
6. **push 纪律**：先 `git fetch` → **合并用 `git merge FETCH_HEAD`，绝不强推**；`git add` 按路径列出（**禁 `-A`**）；
   通道先试"清空代理变量直连"，失败再走加速器 `-c http.proxy=…`（端口动态，**别 --global**）
7. **节奏**：先出方案 → 用户确认 → 再写代码；做完一步汇报产出与遗留，等确认再下一步
8. **每件交付都要有自检 + 变异测试**：新守卫必须"注入坏数据"验证真会报错，并跑一次未变异对照组

**双机协作**：WB 改 Swift + 自检；**CB / Mac** 改 `prototype/index.html`（**WB 只读不写**）、编译、真机验证、`[mac-fix]`。

## 四、长期记忆里的关键坑（都是真踩过的）

1. 🔴 **Swift `catch` 不到 ObjC 异常** —— 只能在"入参是否合法"上防；读硬件状态的 API 先问"它此刻处于有效状态吗"（`startRunning()` 异步）
2. 🔴 **`let` 带默认值的属性不进 memberwise init** —— 配置字段要用 `var`（否则传参报 "extra argument"，编译才暴露）
3. 🔴 **`AVCaptureDevice` 不暴露镜头绝对焦距** —— 别用 mm 相除，按**镜头角色**读设备（`virtualDeviceSwitchOverVideoZoomFactors` + 原生阶梯）
4. 🔴 **自检守卫的作用域**：断言"某函数里必须有 X"要用 **`methodBodyOf` 切方法体**（全文匹配会被别处同类代码满足=假绿）；**比对顺序前先剥 `//` 注释**（注释里提到目标词会假 FAIL，批六又踩一次：视图注释里留着"hasSubTicks 已删"的说明）
5. 🔴 **两条检查脚本会"打架"** —— 断言语义跨两个文件时，只能放**两边都读**的脚本里
6. 🔴 **镜像/同步整目录 + 固定五步**：① D: fetch+merge → ② D:→工作区 → ③ 改 → ④ 工作区→D: → ⑤ add/commit/push。
   硬编码文件清单会**静默少同步**；顺序颠倒会覆盖别人的改动
7. 🔴 **几何账要写成可执行的检查**（读令牌 + 逐机型复算），文档只贴脚本打印的数字（手算表已两次漏项，各乐观 8.5pp）
8. **2pt 余量 = 危险信号**；中文字宽按 1.0em 推、拉丁/数字必须实测（批六实测对象：40pt 档距下 5 位数+K 的 WB 标签估算 ≈39pt）；SF Symbol 名写错静默留白（小图形用 `Path` 自画）
9. 🔴 **能力探测 API 要"与将要执行的那次写入"逐字对应**（2026-09-19 白平衡 7 连崩）：
   `isWhiteBalanceModeSupported(.locked)` / `isFocusModeSupported(.locked)` 在虚拟多摄上**误报 true**
   （SDK `AVCaptureDevice.h:538-541` 只允许锁 Current）→ 写计算值抛 ObjC 异常 abort。正确：`isLockingWhiteBalanceWithCustomDeviceGainsSupported`（:1601）/ `isLockingFocusWithCustomLensPositionSupported`（:1110）
10. 🔴 **硬件状态回读 ≠ 用户意图**（B3b 复验 ③ 死锁）：`.autoFocus` 对焦一次后**系统自动转 `.locked`**
    → 按 `focusMode == .locked` 推断"用户锁了手动对焦"会死锁成永久"仅测光"。**判据：能被系统改写的状态
    只能当"读数"喂 UI，档位必须由用户操作路径显式记录**（⑱ 守写入点唯一）；同族：**推断值要守卫，显式值不守卫**
11. 🔴 **设备对象**是"实例级"状态容器 —— 复用/换设备都会带出 `.custom` / `.locked` 残留（2026-09-20 实锤 A）：
    `exposureMode` / `whiteBalanceMode` 只会被**当前挂着的那台设备**改写，`removeInput`/`addInput` 都**不重置**它。
    **判据：跨设备搬运 = 先归一（`normalizeToAutoLocked`），再重放；永远不要假设新设备的档位是干净的。**
12. 🔴 **形态切换的转场边界按"用户视角"分类**（2026-09-20 用户要求 ③）：**只有点焦段档位**该看到转场（≤1s）；
    切手动 / 开对焦盘 / 切模式 / App 回切全走**静默**（`applyFocalTargetSilently` 不置 `isLensSwitching`）。
    ⚠️ 入口动作挂 **`form` 边沿**（挂 `isLensSwitching` 边沿 → 静默路径"点了没反应"）；静默省的是"模糊"不是"耗时"（换 input 固有 0.6~0.75s 停顿仍在）
13. 🔴 **对焦读数 0.00 是"最近端 / 未收敛"，不是可锁的"当前值"**（2026-09-20 问题 4）：
    锁上去焦点死在最近端、点按全走"仅测光"**无出路**。判据：**推断值要守卫，用户显式给的值不守卫**
    ——"关自动对焦"是推断 → 读数必须 `finite && > 0.01` 否则拒绝 + toast；圆盘**显式**拖到 0.00 是真实意图 → 照写
14. 🔴 **`commitConfiguration()` 返回 ≠ 系统已完成工作**（批五 · 真凶级）：切模式/换设备会让会话**重新协商
    `activeFormat`**，落地**晚于**我们的重放，把 `.custom`/`.locked` 清成自动。**判据：凡"改完会话配置 →
    立刻按意图写设备参数"的地方都要有"延迟复查 + 被清就补写"层**（`reverifyManualIntent` 四拍；
    补写前先核对**已发布的意图态**，用户中途点回自动就**不能**按回去）
15. 🔴 **等异步任务用"回执"，不要"盲等超时"**（批五）：盲等分不清"慢"与"没做"。
    ⚠️ **"已经满足"必须单独一个回执**（预切换抢先完成时 `form` 不再变化 → 排队动作永不执行 = "点了没反应"）
16. 🔴 **工具坑：同一文件并行编辑会丢写入**（2026-09-20 连踩两次）→ 同文件多次编辑**串行发**，
    改完 `git diff --stat` + grep 关键符号核对。**批六补充（2026-09-21）**：`old_string` 带**尾换行**而
    `new_string` 不带 → 会**吞掉下一行的换行**，把下一行并进注释里（`const` 掉进 `//` 注释 → 守卫变量未定义）；
    所以编辑**注释行**之后必须重读现场或直接跑脚本，别只看"编辑成功"
17. 🔴 **点按对焦会顶掉手动曝光**（2026-09-21 批六 ③ · 系统语义）：
    `setFocusAndExposurePoint` 把 `exposureMode` 设成 `.continuousAutoExposure` → 手动 ISO/快门被顶掉
    （实测日志链：`11:45:10.006 手动 799/1/60 → 13.242 点按对焦 → 16.329「曝光=自动」`）。
    **判据：点按只是"重新对焦"，不是退出曝光手动的意图 —— 点按前取意图快照、点按后按快照重放**
    （曝光/白平衡/EV 同理；"仅测光"路径 `setExposurePointOnly` 自带 `.custom` 守卫不用管）；
    顺序：先写回 `.custom` **再**推 EV（`applyExposureBias` 见 `.custom` 会回切自动）
18. 🔴 **掉帧排查判据 = 重启 App 能不能立刻恢复**（2026-09-21 批六 ④ · Mac CB 初判）：
    热降频/系统节流不会因重启 App 快速恢复；App 侧泄漏（模式切换 / 静默预切换没拆净的 input/output）会随次数累积、
    重启清零。**做法：input/output 增删逐点埋计数（漏埋=计数错，比没有更坏）+ 活数超出该模式预期即 WARN +
    内存 `phys_footprint` + 每次切换耗时 + 5s 周期日志**；若只在长时间使用后出现且重启不能恢复 → 转发热/系统线
19. 🔴 **`git` 自写的 ref 在本机会静默丢失**（2026-09-21 实测可复现）：**`refs/` 子目录下的 ref 每次必丢**
    （worktree 的 `merge --ff-only` 报 Fast-forward、reflog 也记了，ref 文件却没了 → HEAD 悬空 /
    `git status` 全仓显示成新增；平铺的 `refs/heads/main` 不受影响）。**判远端只认 git 打印的引用更新或
    `git ls-remote`**；补法 = 绕开 git 用 python 把 SHA 直写 `.git/refs/heads/…` 与 `.git/refs/remotes/origin/main`

## 五、文档索引（按需再读某一节，不要一次全读）

| 文档 | 什么时候读 |
|---|---|
| `CONTEXT_HOT.md` | 项目状态总表：顶部「〇」是 Swift 进度快照（含 Mac 验证记录）+ Backlog；下方是原型迭代历史（参考） |
| `docs/04-交接单.md` | 需要**完整**工程纪律 / 已知风险 / 恢复方式时 |
| `docs/08-P2-Swift同步任务书.md` | 需要 **B 组完整路线**（B3-B5）、几何规约、验证约束时 |
| `docs/15-B1焦段切镜头方案.md` | 改**焦段 / 变焦**相关（映射、Ramp、会话稳定性、第 11 组自检） |
| `docs/16-B2-ISO快门白平衡刻度条方案.md` | 改**参数刻度条**（第 12 组自检、净可见账表、B2a/B2b 交付记录、冷启动崩溃） |
| `docs/17-焦段条加35mm档方案.md` | 改**焦段数据 / 档位数**（净可见上限 6 档、声明式扩展、宽度预算） |
| `docs/20-物理架构改造实施方案.md` | 改**会话形态 / 换设备 / 转场 / 回切**时（预检清单 7+3 在那） |
| `docs/21-批四修复与拍板项.md` | **批四**修复记录 + 问题 4② 三案（已拍板方案 A 并实施） |
| `docs/22-批五修复记录.md` | **批五**：静默路径 / 刻度细分（**口径已被批六作废**）/ 切模式参数 |
| `docs/23-批六方案与拍板.md`（**待写**） | **批六**：刻度复刻飓风 / 预热 / 点按重放 / 资源埋点 —— 精度依据与拍板在那 |
| `docs/00-启动包.md`（本文件） | **新会话唯一入口**（旧「新会话开场白」已并入本文件） |
| `docs/03` / `docs/09` / `docs/10~14` | 动布局预算 / 快门排 / 滤镜条·手势参数排·功能面板·格式芯片·**EV 回写环** |
| `docs/05-双机协作流程.md` / `README.md` | 与 CB/Mac 协作完整流程 / 验收清单 + Mac 从零跑起来六步 |

## 六、本机环境速记（Windows · WB 端）

- **无 Xcode / 无法编译 / 无模拟器**：只能靠**静态自检 + 变异测试**；编译与真机交给 Mac
- 两个自检：`node tools/check_swift.js LumenEdit` · `node tools/check_presets.js .`；变异：
  `python .workbuddy/mutation-test-batch4.py`（一次性脚本不提交；同步/镜像脚本同属一次性）
- **Git Bash 只剩 git**：`ls` / `cat` / `head` / `tail` / `grep` / `sleep` 全缺 →
  列文件用 Glob、读文件用 Read、匹配用 Grep；要看自检结论跑 `.workbuddy/run-checks.py`（只看 FAIL）
- **网络（两条通道都间歇）**：直连常 `reset` / 连 21s 超时、沙箱代理常 `502`，**但两条都成功过** → 只用
  `.workbuddy/push-when-open.py` 抢窗口（双通道轮换 + 动态读 env 端口）
- ⚠️ ref 静默丢失见第四节 19（补法在那）；Python 3.13：`C:\Users\edcee\.workbuddy\binaries\python\versions\3.13.12\python.exe`；
  Node 22：`…\node\versions\22.22.2-3\node.exe`（都托管优先）

## 七、收尾清单（每次交付前过一遍）

- [ ] 两个自检全过；新守卫已做**变异测试**（守卫自己也会写错）
- [ ] 改了 UI 几何 → 跑**净可见账**（第 12 组⑩）+ **宽度预算**（第 5 组）；镜像后 `git status` 核对**改动面**
- [ ] 提交信息写清：动机 / 改动 / 偏离方案点 / 验证与未验证 / 交给 Mac 的验收口径；收尾时更新**本文件第一、二、四节** + `CONTEXT_HOT` 〇/Backlog/下一步

## 八、Mac 侧复验备忘（真机验证要用，转给 CB 时直接贴）

- **截图**：`pymobiledevice3 developer dvt screenshot out.png`（PATH 加 `$HOME/Library/Python/3.9/bin`）；
  ⚠️ `idevicescreenshot` 在 iOS 26 **确定性损坏**（libimobiledevice #1465），别再试
- **装机**：`devicectl device install app --device 81FD8131-… /tmp/lumen-dd/Build/Products/Debug-iphoneos/LumenEdit.app`；
  **取日志**：`devicectl device copy from --domain-type appDataContainer --domain-identifier com.wokenday77.lumenedit --source Documents/lumen-debug.log`
- **签名**：Team `H7XJ2PBZJG`（证书 **7 天过期**，重装后去 设置→通用→VPN 与设备管理 重新信任）；
  连接抖动是常态（`Device is busy` / `Developer Mode is disabled` 假象**重试即可**）；
  **zsh 坑**：`git show 'abc~1':path` 的 `^` / `~` 要加引号；变量不自动分词（curl 参数别走变量拼）

## 九、验证口径速查（本端怎么"验"、Mac 怎么"核"）

**WB 端（Windows，无 Xcode）能做的只有静态**：

| 手段 | 命令 | 能查出什么 |
|---|---|---|
| 结构自检 | `node tools/check_swift.js LumenEdit` | 括号配平 / 顶层重名 / 状态引用完整性 / 几何账 / **净可见账** / 各组专属守卫（第 12 组现含单档刻度口径 + 物理/批三~五守卫；**批六 o1~o4 待补**） |
| 预设同源 | `node tools/check_presets.js .` | 滤镜/风格/场景/焦段/码率 + **刻度条三张表**；第 6 组现处「Swift 先行」WARN 态（差异清单 = CB 待办） |
| 变异测试 | `python .workbuddy/mutation-test-batch4.py`（一次性脚本不提交） | **新加的守卫真的会报错吗**（对副本注入坏数据 → 看是否出现预期 FAIL → 再跑未变异对照组） |

**Mac 端核法（读日志，不用量截图）**：

| 事项 | 日志里看什么 |
|---|---|
| 冷启动基线（已 ✅） | `白平衡增益尚未就绪（冷启动）→ 补发手动档状态`；「手动参数能力：曝光(custom)=… / 白平衡(锁定增益)=… / 对焦(锁定位置)=…」（三摄真机三个 false）；「变焦拓扑」`13→1.000 / 24→2.000 / 35→2.917 / 48→4.000 / 120→10.000` |
| **批五 · 静默 + 切模式（等复验）** | `静默换形态耗时 X.XXs`（缓存命中 0.3~0.8s，**不退转场**）；`切模式 commit 后瞬间档位` + `切模式后档位复查`（白平衡=手动）；`切模式复查：…（第 N 拍复查）→ 已按快照补写` 属**预期内** |
| **批六 · 刻度（等复验，口径已翻篇）** | 刻度**单档** 14pt 同高、**选中档 22pt 绿**、**无细分线**、**每档带标签**（不放大）、40pt 档距 ≈8~9 档可视；⚠️ 批五"细分线/五档高度"那套验收**作废** |
| **批六 · 预热（等复验）** | `预热开始：待探 N 颗…` → `预热探到格式：…` → `预热结束：耗时 X.XXs`；若见 `⚠️ 预热导致直播会话被打断 → …已放弃` = 闸门兜住（同时预览不该黑屏）；A/B 开关 `-lumen.camera.prewarm.disabled YES` |
| **批六 · 点按重放（等复验）** | 手动档下点取景器：`点按对焦后按意图重放手动曝光（ISO … / …）` + `点按对焦后档位：曝光=手动 / 白平衡=手动 / 意图=…`，且 UI **不**回自动；必要时 `点按对焦复查：…→ 已按快照补写` 属**预期内** |
| **批六 · 资源账（等复验）** | 每 5s 一行 `资源：模式 … input 活 N（累计增/删 a/b）· output 活 N（…）· 内存 NMB`；出现 `⚠️ 资源超出预期` = 泄漏实锤；复现脚本：开所有刻度条 → 切手动 → 照片/实况/log/视频 依次切 ×N 轮 |
| 遮幅 ⑤ / 拍照视场 ⑥（backlog） | `遮幅 1:1：… 上下黑边 174/202，窗中心 437 vs 437`；⑥ 照片比取景器**多出左右**（aspectFill 裁 ~39%），待 P2 同批 |

## 十、本文件与 `CONTEXT_HOT.md` 的关系（谁为准）

- **进度 / 下一件事 / 关键坑** → 以**本文件**为准；**原型迭代史 / Mac 验证记录全文 / Backlog 完整叙述** → `CONTEXT_HOT.md`；
  两边矛盾先查 git log 确认谁更晚更新，**再信更晚那份**；新会话只贴本文件（`CONTEXT_HOT` 按第五节索引查）

# LumenEdit · 启动包（新会话唯一入口）

> **用法**：新建对话 → 把本文件**整份**贴给 AI → 发送。
> **收尾纪律（2026-09-20 用户明确要求，AI 自动执行）**：只要有交付/修 bug/落盘，收尾必须
> ① 更新本文第一（进度）/ 二（下一件事）/ 四（坑）节 ② 本文控制在 **150-200 行**（超了删旧内容，
> 过时的不留）③ CONTEXT_HOT 的 〇/Backlog/下一步 同步 ④ 更新完 push ⑤ 记当日日志。
> **最后更新：2026-09-20 下午三（批四：批三 6 条全修——实锤 A 换设备残留归一 / 实锤 B 倍率方向 / 刻度条四档层级 + 松手待回读 / 转场边界（静默换形态）/ 切模式一律按快照重放 / 对焦锁定值守卫；自检⑲ n 段 15 条 + 变异 19/19，等 Mac 复验批四。问题 4② 点按语义待用户拍板）**

---

## 一、当前进度

- **A 组纯 UI：✅ 全部完成**（#6 场景·风格条 / #7 滤镜条 / #10 功能面板 / #11 格式芯片 / 底部三问题修复）· 2026-09-18 复验 10 项全过
- **B 组接线**：
  - **B1 焦段切镜头 + `Ramp`：✅ 过 Mac 真机复验**（焦段条 5 档 13/24/35/48/120，映射按镜头角色）
  - **B2 刻度条三件套（a 数据层 / b UI / 白平衡 7 连崩修复 / 冷启动修复 / 手动档能力置灰）：✅ 全交付且过 Mac 复验**（细节 `docs/16`；能力行「手动参数能力：…」在冷启动日志）
  - **B3 拍板全落**：物理会话**按需** / 手动档不锁焦段（参数搬运）/ 点按只测光（③）
  - **B3a EV 圆盘 / B3b 对焦圆盘：✅ 交付 + Mac 复验闭环（含 4 笔 [mac-fix]）**：两盘共用 **DialView**；
    对焦回读三件套 + 闸门 + 「自动对焦」开关 + **拍板 ③ 点按只测光**；🔴 正源修（backlog ⑩）：
    `.autoFocus` 完成后系统自置 `.locked` → **对焦档改意图驱动**（写入点只在两意图入口，⑱ 守卫）
  - **物理架构改造（docs/20）✅ 三笔交付 + Mac 复验核心全通（A/B/C/D½/F/G，零 ERR）+ 批二 5+7 问题已修**：
    ①形态状态机（`SessionForm`/`FocalTarget`/`performFocalSwitch` 六步+搬运+回滚）②分派矩阵（虚拟=ramp /
    物理同镜头 35↔48=ramp 到 mainCropFactor / 跨镜头=换设备；录制中跨镜头拒）③模糊转场（顺序触发）+ 回切防抖
    - **批四（本轮）· 批三 6 条全修**：
      - 🔴**实锤 A**：换设备先把新设备**归一到自动**（`normalizeToAutoLocked`）再按快照重放 —— 治
        "新设备自带上一轮 `.custom` 残留 → 推 EV 被回切 + WRN"（批三两次 WRN 根因）
      - 🟠**实锤 B**：跟手倍率方向改回**乘**（`translation.width * dragGain`，1.5:1）
      - 🟠**问题 1**：刻度条按原型实现**四档层级**（minor 10/.34 · **mid 15/.48** · major 22/.82 · preset 22/琥珀）
        + ISO 整档=mid、WB 100K=mid、**快门补半档 mid**、preset 修正 22pt；**松手后草稿保留到硬件回读同档**
        （治"卡点回弹/对应数值不对"）+ 0.8s 超时兜底
      - 🔴**问题 2**：切模式**一律按快照重放**（不再"只在被系统清掉时重放"）+ 两处**档位复查**日志
      - 🔴**问题 3**：转场边界 —— **静默换形态**（不置 `isLensSwitching`）：打开刻度条/按对焦入口**后台预切换**、
        回切虚拟也静默；仅用户点焦段跨镜头走转场；入口动作改挂 **`form` 边沿** + 1.2s 转场兜底
      - 🔴**问题 4①**：对焦锁定值有效性守卫（0.00 端点/非有限值不当"当前值"锁，保持自动 + toast）
      - 🔴**问题 4②（用户已拍板方案 A，本轮已实施）**：点按取景器 = **重新对焦 + 解除手动档**
        （与 iPhone 原生一致 = **永远有出路**）；**对焦盘开着时算误触** → 只测光、不解除
        ⚠️ 判据必须在 `dismissTransientPopovers()` **之前**读（那句会收掉盘 → 之后恒为假）
    - **自检 ⑲ n 段 15 条 + ⑱ h 改口径（方案 A）+ 变异 22/22 全过**
  - B4（格式选择器接线 #11）/ B5（参数导入 #13，等 P4）：⬜ 待做
- **Backlog（本轮新记，先不修）**：静默换形态只省"模糊"不省"耗时"（换 input 固有 0.6~0.75s）/
  预切换期间点跨镜头焦段会走换设备+转场 / 快门半档细线**不吸附** / `docs/20` 旧名 `applyFocalTarget` 漂移 /
  **取景器双指缩放**（pinch 连续变焦 —— 当前变焦入口只有焦段档位）
- **Backlog（历史）**：⑥ 取景器与成片视场不一致（预览裁左右 ~39%，与「实拍裁切 P2」同批）/
  🔴 **两个既有态 < 50%**：场景·风格展开 49.3% / 滤镜条展开 42.8%（844）—— 要动 147/144，**需拍板** /
  ⑩ 对焦档=用户意图 ✅ / ⑨ 圆盘样式 ✅ / ⑤ 遮幅待复核

## 二、下一件事

1. **Mac 复验批四（6 条 + 方案 A）**：① 换设备/切焦段后**无 WRN**、且日志出现「换设备后新设备档位复查：曝光=自动」；
   ② 跟手倍率**变灵敏**（不再要拖 1.5 倍距离）；③ 刻度条**外形有层级**（快门半档线可见 / WB 每 5 档一高 /
   松手不弹回）；④ 切模式（照片↔Live↔视频）手动档与 EV **保留** + 「切模式后档位复查」；
   ⑤ 转场边界：**只有切焦段**看到模糊（≤1s），点手动开关/点对焦/切模式**无转场**；
   ⑥ 120mm 关「自动对焦」若读数为 0.00 应被拒（toast + warn），不再锁死最近端；
   ⑦ **方案 A**：手动锁定 + 对焦盘**关着** → 点取景器**重新对焦** + toast「已解除手动对焦」；
   对焦盘**开着** → 只测光、**不解除**（防误触）。判据：任何一次点按都不该"没反应"
2. 补测：录制中跨镜头拒绝（上批无痕）
3. **下一批候选**（等拍板）：backlog 里两条既有态 < 50%（场景·风格展开 / 滤镜条展开，要动 147/144）；
   **取景器双指缩放**（新功能）

## 三、工程纪律（8 条 · 摘自 `docs/04`，完整版读那份）

1. **分层**：UI（View/VM）不碰设备；设备一切写操作只走 `CaptureDeviceConfigurator`
2. **唯一锁**：`beginConfiguration` / `lockForConfiguration` 只在 configurator（自检会拦）
3. **事件驱动不轮询**：硬件变化由会话 `@Published` 主动发布；例外只有每秒 `refreshSnapshot()`（只写日志）
4. **单一真源**：预设数据（滤镜/风格/场景/焦段/格式/**刻度条**）以原型为真源，`check_presets.js` 逐条比对
5. **浮层互斥逐点接线**：新增浮层必须同时进 `collapseOverlays()` 与 `dismissTransientPopovers()`，且**展开者自己不能调后者**
6. **push 纪律**：先 `git fetch` → **合并用 `git merge FETCH_HEAD`，绝不强推**；`git add` 按路径列出（**禁 `-A`**）；
   通道先试"清空代理变量直连"，失败再走加速器 `-c http.proxy=…`（端口动态，**别 --global**）
7. **节奏**：先出方案 → 用户确认 → 再写代码；做完一步汇报产出与遗留，等确认再下一步
8. **每件交付都要有自检 + 变异测试**：新守卫必须"注入坏数据"验证真会报错，并跑一次未变异对照组

**双机协作**：WB 改 Swift + 自检；**CB / Mac** 改 `prototype/index.html`（**WB 只读不写**）、编译、真机验证、`[mac-fix]`。

## 四、长期记忆里的关键坑（都是真踩过的）

1. 🔴 **Swift `catch` 不到 ObjC 异常** —— 只能在"入参是否合法"上防；`catch` 是幻觉。
   读硬件状态的 API 先问"它此刻处于有效状态吗"（`startRunning()` 是异步的）
2. 🔴 **`let` 带默认值的属性不进 memberwise init** —— 配置字段要用 `var`，
   否则传参报 "extra argument"（编译才暴露，代价一轮往返）
3. 🔴 **`AVCaptureDevice` 不暴露镜头绝对焦距** —— 别用 mm 相除；
   按**镜头角色**读设备（`virtualDeviceSwitchOverVideoZoomFactors` + 原生阶梯）
4. 🔴 **自检守卫的作用域**：断言"某函数里必须有 X"要用 **`methodBodyOf` 切方法体**（全文匹配会被别处同类代码满足=假绿）；**字符窗口**会被注释挤爆=假红；**比对顺序前先剥 `//` 注释**（注释里提到目标词会假 FAIL，本轮又踩一次）
5. 🔴 **两条检查脚本会"打架"** —— 断言语义跨两个文件时，只能放**两边都读**的脚本里
6. 🔴 **镜像/同步整目录 + 固定五步**：① D: fetch+merge → ② D:→工作区 → ③ 改 → ④ 工作区→D: → ⑤ add/commit/push。
   硬编码文件清单会**静默少同步**；顺序颠倒会覆盖别人的改动
7. 🔴 **几何账要写成可执行的检查**（读令牌 + 逐机型复算），文档只贴脚本打印的数字（手算表已两次漏项，各乐观 8.5pp）
8. **2pt 余量 = 危险信号**；中文字宽按 1.0em 推、拉丁/数字必须实测；SF Symbol 名写错静默留白（小图形用 `Path` 自画）
9. 🔴 **能力探测 API 要"与将要执行的那次写入"逐字对应**（2026-09-19 白平衡 7 连崩）：
   `isWhiteBalanceModeSupported(.locked)` / `isFocusModeSupported(.locked)` 在虚拟多摄上**误报 true**
   （SDK `AVCaptureDevice.h:538-541` 只允许锁 Current），写计算值抛 ObjC 异常 → 全进程 abort。
   正确：`isLockingWhiteBalanceWithCustomDeviceGainsSupported`（:1601）/ `isLockingFocusWithCustomLensPositionSupported`（:1110）
10. 🔴 **硬件状态回读 ≠ 用户意图**（2026-09-20 B3b 复验 ③，Mac 抓到的死锁）：`.autoFocus` 对焦一次后
    **系统自动转 `.locked`** → 按 `focusMode == .locked` 推断"用户锁了手动对焦"会死锁成永久"仅测光"。
    **判据：能被系统改写的状态只能当"读数"喂 UI；档位必须由用户操作路径显式记录**（⑱ 守写入点唯一）。
    ⚠️ 同族推论（本轮实锤 A）：**推断出来的值要守卫，用户显式给的值不守卫**（如"关自动对焦"＝推断 → 守卫 0.00 端点）
11. 🔴 **设备对象**是"实例级"状态容器 —— 复用/换设备都会带出 `.custom` / `.locked` 残留（2026-09-20 实锤 A）：
    `exposureMode` / `whiteBalanceMode` 只会被**当前挂着的那台设备**改写；别的镜头仍留着旧档位，
    而 `removeInput` / `addInput`、甚至**复用同一个 device 对象**都**不重置**它。
    所以"换设备后按旧快照重放"之前**必须先归一新设备**（`normalizeToAutoLocked`），否则推 EV 会撞上
    残留的 `.custom` → 被回切 + WRN（批三 2 次实锤；日志特征是"只搬白平衡 + 推 EV"、**没有**"手动曝光已重放"）。
    **判据：跨设备搬运 = 先归一，再重放；永远不要假设新设备的档位是干净的。**
12. 🔴 **形态切换的转场边界按"用户视角"分类，不按实现分类**（2026-09-20 用户要求 ③）：
    用户口径 = **只有点焦段档位**该看到转场（≤1s）；切手动 / 开对焦盘 / 切模式 / App 自己回切
    全部要"人眼无感"。做法：`applyFocalTargetSilently`（**不置 `isLensSwitching`** ⇒ 不显示模糊）
    承担所有"非用户焦段"的换设备（**后台预切换** + **回切虚拟**）；`beginFocalSwitch` 只服务焦段点击。
    ⚠️ 连带坑：入口动作**不能挂在 `isLensSwitching` 边沿**（静默路径不置它 → 表现为"点了没反应"），
    必须挂在 `form` 边沿 + 1.2s 转场兜底。⚠️ **静默省的是"模糊"、不是"耗时"** ——
    换 input 固有 0.6~0.75s 停顿仍在（模糊的价值正是盖住它），所以静默只用在用户没在等切换的时刻。
13. 🔴 **对焦读数 0.00 是"最近端 / 未收敛"，不是可锁的"当前值"**（2026-09-20 问题 4）：
    换到 120mm 后新设备 `lensPosition` 冷帧读回 0 → 关「自动对焦」把它锁成手动档 →
    焦点死在最近端、画面全跑焦，且此后点按全走"仅测光"**无出路**（批三 40 次点按实锤）。
    判据：**推断出来的值要守卫，用户显式给的值不守卫** ——
    "关自动对焦"是**推断**"就锁在现在这里" → 读数必须 `finite && > 0.01`，否则拒绝 + toast；
    而圆盘**显式**拖到 0.00 是用户真实意图 → 照写。
14. 🔴 **工具坑：并行编辑同一个文件会丢写入**（2026-09-20 本轮连踩两次）：
    一条消息里对**同一个文件**发多个编辑，会出现"都报成功、但只有部分落盘"——
    被丢的那处留下旧代码（本轮丢了 `prewarmFocalTarget` 改名，差一点带着编译错误提交）。
    **做法：同一文件的多次编辑必须串行；改完用 `git diff --stat` + grep 关键符号逐个核对。**

## 五、文档索引（按需再读某一节，不要一次全读）

| 文档 | 什么时候读 |
|---|---|
| `CONTEXT_HOT.md` | 项目状态总表：顶部「〇」是 Swift 进度快照（含 Mac 验证记录）+ Backlog；下方是原型迭代历史（参考） |
| `docs/04-交接单.md` | 需要**完整**工程纪律 / 已知风险 / 恢复方式时 |
| `docs/08-P2-Swift同步任务书.md` | 需要 **B 组完整路线**（B3-B5）、几何规约、验证约束时 |
| `docs/15-B1焦段切镜头方案.md` | 改**焦段 / 变焦**相关（映射、Ramp、会话稳定性、第 11 组自检） |
| `docs/16-B2-ISO快门白平衡刻度条方案.md` | 改**参数刻度条**（第 12 组自检、净可见账表、B2a/B2b 交付记录、冷启动崩溃） |
| `docs/17-焦段条加35mm档方案.md` | 改**焦段数据 / 档位数**（净可见上限 6 档、声明式扩展、宽度预算） |
| `docs/20-物理架构改造实施方案.md` | 改**会话形态 / 换设备 / 转场 / 回切**时（预检清单 7+3 在那） |
| `docs/21-批四修复与拍板项.md` | **本轮批四**的修复记录 + **问题 4② 三个候选方案**（待拍板） |
| `docs/00-启动包.md`（本文件） | **新会话唯一入口** |
| `docs/00-新会话开场白.md` | 旧版开场白（已并入本文件，只留指针） |
| `docs/03-布局调整方案.md` | 动布局总方案 / 高度预算时 |
| `docs/09-2-5快门排方案.md` | 动快门排时 |
| `docs/10` ~ `docs/14` | #7 滤镜条 / 底部手势与参数排 / #10 功能面板 / #11 格式芯片 / **EV 绑定回写环**（B3 要接同款纪律） |
| `docs/05-双机协作流程.md` | 需要与 CB/Mac 协作的完整流程时 |
| `README.md` | 验收清单 + Mac 从零跑起来六步 |

## 六、本机环境速记（Windows · WB 端）

- **无 Xcode / 无法编译 / 无模拟器**：只能靠**静态自检 + 变异测试**；编译与真机交给 Mac
- 两个自检：`node tools/check_swift.js LumenEdit` · `node tools/check_presets.js .`
  （变异：`python .workbuddy/mutation-test-batch4.py`，22 突变 / 一次性脚本不提交）
- 同步/镜像脚本（一次性、不提交）：`sync-from-main.py`（D:→工作区）/ `mirror-to-main.py`（工作区→D:）
- **Git Bash 只剩 git**：`ls` / `cat` / `head` / `tail` / `grep` / `sleep` 全缺 →
  列文件用 Glob、读文件用 Read、匹配用 Grep；要看自检结论跑 `.workbuddy/run-checks.py`（只看 FAIL）
- **网络（2026-09-20 实测变了）**：沙箱 env 里挂了坏代理 → git 走它会 `CONNECT tunnel failed 502`；
  **清空代理变量后直连 GitHub 可用**（`HTTP_PROXY= HTTPS_PROXY= http_proxy= https_proxy= no_proxy='*' git fetch`）。
  用户加速器（7892）**没启动时不必等它**；端口是动态的，别硬编码
- Python 3.13 在 `C:\Users\edcee\.workbuddy\binaries\python\versions\3.13.12\python.exe`（托管优先）
- Node 22 在 `C:\Users\edcee\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`

## 七、收尾清单（每次交付前过一遍）

- [ ] 两个自检全过；新守卫已做**变异测试**（守卫自己也会写错）
- [ ] 改了 UI 几何 → 跑**净可见账**（第 12 组⑩），并核**宽度预算**（第 5 组）
- [ ] 镜像后 `git status` **核对改动面**（只能多出本笔要交的文件）
- [ ] 提交信息写清：动机 / 改动 / 偏离方案点 / 验证与未验证 / 交给 Mac 的验收口径；收尾时更新**本文件第一、二、四节** + `CONTEXT_HOT` 〇/Backlog/下一步

## 八、Mac 侧复验备忘（真机验证要用，转给 CB 时直接贴）

- **真机截图**：`pymobiledevice3 developer dvt screenshot out.png`（PATH 加 `$HOME/Library/Python/3.9/bin`；已 pip3 install --user，走原生隧道无需 sudo）
  ⚠️ `idevicescreenshot` 在 iOS 26 **确定性损坏**（libimobiledevice #1465），重启/重插无效，别再试
- **装机**：`devicectl device install app --device 81FD8131-… /tmp/lumen-dd/Build/Products/Debug-iphoneos/LumenEdit.app`
- **取日志**：`devicectl device copy from --domain-type appDataContainer`
  `--domain-identifier com.wokenday77.lumenedit --source Documents/lumen-debug.log`
- **签名**：Team `H7XJ2PBZJG`（Personal，证书 **7 天过期**；过期重装后要在 设置 → 通用 → VPN 与设备管理 重新信任）
- **连接抖动是常态**：`Device is busy (Waiting to reconnect)`（build EXIT=70）与 `Developer Mode is disabled` 假象**重试即可**；`ddiServicesAvailable: true` 才是真状态
- **zsh 坑**：`git show 'abc~1':path` 的 `^` / `~` 要加引号；变量不自动分词（curl 参数别走变量拼）

## 九、验证口径速查（本端怎么"验"、Mac 怎么"核"）

**WB 端（Windows，无 Xcode）能做的只有静态**：

| 手段 | 命令 | 能查出什么 |
|---|---|---|
| 结构自检 | `node tools/check_swift.js LumenEdit` | 括号配平 / 顶层重名 / 状态引用完整性 / 各组件几何账 / **净可见账**（第 12 组⑩）/ 各组专属守卫（现 12 组，第 12 组含⑯虚拟多摄诚实边界） |
| 预设同源 | `node tools/check_presets.js .` | 滤镜 / 风格 / 场景 / 焦段 / 码率 / **刻度条三张表** 与原型逐条一致（现 6 组） |
| 变异测试 | `python .workbuddy/mutation-test-batch4.py`（本轮；一次性脚本不提交） | **新加的守卫真的会报错吗**（对副本注入坏数据 → 看是否出现预期 FAIL → 再跑未变异对照组） |

**Mac 端核法（读日志，不用量截图）**：

| 事项 | 日志里看什么 |
|---|---|
| 冷启动白平衡（已 ✅） | `白平衡增益尚未就绪（冷启动）…` → `白平衡增益已就绪（冷启动）→ 补发手动档状态` |
| 手动能力探测（日志行） | 冷启动一行「手动参数能力：曝光(custom)=… / 白平衡(锁定增益)=… / 对焦(锁定位置)=…」（三摄真机三个 false） |
| **批四 · 实锤 A / 问题 2（等复验）** | 换设备或切焦段后**不该再出现** `推 EV 时设备处于手动曝光档 → 已回切自动档`（WRN）；且必有一条 **「换设备后新设备档位复查：曝光=自动/手动 / 白平衡=… / 对焦=自动（显式降级） / EV=…」**；切模式后一条 **「切模式后档位复查（模式名）：…」** |
| **批四 · 转场边界（等复验）** | 打开刻度条 / 点「对焦」→ `静默换形态（后台挂设备 · 无转场）→ …` + `换设备完成（静默·无转场）…`；**画面无模糊浮层**。点焦段跨镜头才应看到 `换设备开始（模糊淡入中）` |
| **批四 · 对焦守卫 + 方案 A（等复验）** | 120mm 关「自动对焦」若读数 0.00 → WRN `关自动对焦被拒：对焦读数无效（0.00…`（不该再出现 `自动对焦已关 → 手动（当前值 0.00 锁定）`）；**手动锁定 + 对焦盘关着**点取景器 → `…点按取景器：解除手动档并重新对焦（方案 A）` + toast「已解除手动对焦」；**对焦盘开着**点取景器 → `…点按仅测光（不解除手动档，防误触）` |
| **批四 · 刻度条（等复验）** | 快门档间应有 14 条**半档细线**（15pt/48% 白）；WB 每 5 档一根 22pt 亮线、中间 4 根 15pt；松手只在 0.8s 内无回跳（超时会打 `刻度条草稿 0.8s 未与硬件回读对齐`） |
| B1 档位映射 | 冷启动一行「变焦拓扑」：`13→1.000 / 24→2.000 / 35→2.917 / 48→4.000 / 120→10.000`，**120 档 == switchOver[last]** |
| 刻度条（B2） | 点/切时 toast + 刻度条日志；**自动态禁止滑动**；松手吸附最近档 |
| 遮幅（backlog ⑤） | `遮幅 1:1：… 上下黑边 174/202，窗中心（全屏）437 vs 全屏中心 437` |
| 拍照视场（backlog ⑥，未修） | 照片比取景器**多出左右** —— 预览 aspectFill 裁掉 ~39%，待 P2 同批 |

## 十、本文件与 `CONTEXT_HOT.md` 的关系（谁为准）

- **进度 / 下一件事 / 关键坑** → 以**本文件**为准；**原型迭代史 / Mac 验证记录全文 / Backlog 完整叙述** → `CONTEXT_HOT.md`
- 两边矛盾：先查 git log 确认谁更晚更新，**再信更晚那份**；新会话只贴本文件（`CONTEXT_HOT` 按第五节索引查）

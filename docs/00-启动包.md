# LumenEdit · 启动包（新会话唯一入口）

> **用法**：新建对话 → 把本文件**整份**贴给 AI → 发送。
> **收尾纪律（2026-09-20 用户明确要求，AI 自动执行）**：只要有交付/修 bug/落盘，收尾必须
> ① 更新本文第一（进度）/ 二（下一件事）/ 四（坑）节 ② 本文控制在 **150-200 行**（超了删旧内容，
> 过时的不留）③ CONTEXT_HOT 的 〇/Backlog/下一步 同步 ④ 更新完 push ⑤ 记当日日志。
> **最后更新：2026-09-19 深夜（EV 三改复验通过 + B3 两盘拍板全落 + B3b 对焦圆盘交付（共享 DialView 重构））**

---

## 一、当前进度

- **A 组纯 UI：✅ 全部完成**（#6 场景·风格条 / #7 滤镜条 / #10 功能面板 / #11 格式芯片 / 底部三问题修复），
  2026-09-18 复验 10 项全过
- **B 组接线**：
  - **B1 焦段切镜头 + `Ramp`：✅ 过 Mac 真机复验**（8 步零 error；映射改"按镜头角色读设备"；
    焦段条 **5 档** 13/24/35/48/120，原型已同步）
  - **B2a 数据层 + 硬件 API + session 转发 + VM 状态：✅ 交付**（`15d0633`）；
    **B2b 刻度条 UI + 图标行高亮 + EV 面板收口 + 同槽互斥 + 焦段条让位：✅ 交付**（`09d4a8a`）
  - ✅ **B2a 冷启动崩溃 Mac 验证通过**（2026-09-19）：两条预期日志各一条，不再 abort
  - 🔴→✅ **白平衡手动开关 7 连崩已修**（Mac 报告，本轮交付等复验）：守卫用错探测 API ——
    `isWhiteBalanceModeSupported(.locked)` 在虚拟多摄**误报 true**，写计算增益抛 ObjC 异常；
    **三处守卫全换正确 API**（白平衡手动档 + 预设路径 → `isLockingWhiteBalanceWithCustomDeviceGainsSupported`；
    手动对焦 → `isLockingFocusWithCustomLensPositionSupported`，Mac 同类预警一并处理）
  - 🔴→✅ **架构级发现落地：B2 手动档能力置灰**（本轮交付等复验）：SDK `AVCaptureDevice.h:538-541`
    明文虚拟多摄不支持全部手动参数 → 三条刻度条手动开关按**能力探测**置灰（灰但仍可点、点了给原因；
    session 新发布两个支持位；冷启动日志多一行「手动参数能力：…」）
  - **B3 方案已拍板（三件全定，`docs/18` 头部）**：物理会话**按需** / 手动档仍可切焦段（参数搬运）/
    手动对焦锁定期间点按只测光
  - **B3a EV 圆盘 ✅ 已交付（2026-09-19 晚，等 Mac 复验）**：模态圆盘（±3 EV / 0.1 步进 / 点数值框归零，
    打开收整条底栈）；**参数排（EV 滑条面板）随原型改版退场**（删 `ExposurePanel` + `evUIRange`，
    原型 `index.html:2376` 明文）；拖动值复用 `docs/14` 编辑态闸门；自检 ⑰ 组新守卫 + 变异 6/6。
    ⚠️ 增删了文件 → Mac 侧 build 前 `xcodegen generate`
  - **B3b 对焦圆盘 ✅ 已交付（2026-09-19 深夜，等 Mac 复验）**：两盘共用 **DialView**（重构，
    三条交互约定自动继承；`ExposureDialView.swift` 已删 → Mac `xcodegen generate`）；
    **能力分派**（拍板 ①·A）：虚拟点「对焦」= toast 不开盘，物理架构后自动点亮；
    对焦回读三件套补齐 + `focusEditingChanged` 闸门 + 「自动对焦」开关（硬件真值回读）+
    **拍板 ③ 点按只测光**落地；自检新增 **⑱ 组**（9 断言）+ b3b 变异 4/4。
    **方向语义记档**：两盘"盘跟手"一致、值方向相反（EV 顺时针=减小 / 对焦顺时针=增大）——
    镜像布局的物理自洽，非 bug（用户已拍板维持）。
  - B4（格式选择器接线 #11）/ B5（参数导入 #13，等 P4）：⬜ 待做
- **当前卡点：等 Mac 复验本轮**（白平衡开关不崩 + 置灰 + 手动能力日志行）；B3 拍板
- **Backlog**：
  - ⑥ 取景器与成片视场不一致（预览 aspectFill 裁左右 ~39%）→ 与「实拍裁切 P2」同批
  - 🔴 **两个既有态在"安全区高"口径下低于 50%**：场景·风格展开 **49.3%** / 滤镜条展开 **42.8%**（844 上）
    —— 要动 147 / 144 两个既有高度（牵着 `147.0 = 147.0` 恒等式），**需拍板**
  - ⑤ 遮幅 14pt 已修（`8b2dec5`，仍等 Mac 复验）/ ④ 浮层互斥已关（`15d0633`）
- **B1 期间顺手修掉的三个真问题**：映射锚点错（基准 12mm 不是 13mm）/ 会话就绪日志打两遍 /
  `let` 带默认值不进 memberwise init（Mac 抓到）

## 二、下一件事

1. **等 Mac 复验（三笔一起）**：
   - B2 修复笔：白平衡开关不崩 + 置灰 + 能力日志（✅ Mac 已落盘 `598d4cd` 通过）
   - B3a 笔：EV 圆盘（✅ Mac 已落盘 `651cc5c` 三改复验 4/4）
   - **B3b 笔（最新）**：点「对焦」→ toast「手动对焦：当前多摄虚拟设备不支持 · 点取景器
     任意位置仍可自动对焦」+ warn；冷启动能力行多出 对焦(锁定位置)=false；点取景器对焦照常；
     EV 盘回归（`docs/18` 8.4 节 4 项）；⚠️ `xcodegen generate` 后编译
2. **第二步 架构改造**（B3 之后）：物理设备（按需挂载）+ 参数搬运 + 模糊转场 → 重做 B1 变焦 →
   打开 B2 手动档 → **点亮 B3b 对焦盘**（能力探测翻 true 自动生效）

## 三、工程纪律（8 条 · 摘自 `docs/04`，完整版读那份）

1. **分层**：UI（View/VM）不碰设备；设备一切写操作只走 `CaptureDeviceConfigurator`
2. **唯一锁**：`beginConfiguration` / `lockForConfiguration` 只在 configurator ——
   别处出现 `setExposureModeCustom` / `ramp` / `setWhiteBalanceModeLocked` 都是错（自检会拦）
3. **事件驱动，不轮询**：硬件状态变化由会话**主动发布**（`@Published`），UI 只订阅；
   例外：每秒的调试快照 `refreshSnapshot()`（只写日志，不驱动 UI）
4. **单一真源**：预设数据（滤镜/风格/场景/焦段/格式/**刻度条**）以原型为真源，
   由 `tools/check_presets.js` 逐条比对；Swift 侧绝不抄第二份
5. **浮层互斥逐点接线**：新增浮层必须同时进 `collapseOverlays()` 与 `dismissTransientPopovers()`，
   且**展开者自己不能调后者**（会把刚展开的自己收掉）
6. **push 纪律**：先 `git fetch`；**合并用 `git merge FETCH_HEAD`，绝不强推**；
   `git add` 按路径明确列出（**禁 `-A`**）；走加速器 `-c http.proxy=http://127.0.0.1:7892`（不 --global）
7. **节奏**：先出方案 → 用户确认 → 再写代码；做完一步汇报产出与遗留，等确认再下一步
8. **每件交付都要有自检 + 变异测试**：新加的守卫必须"注入坏数据"验证真的会报错，
   并跑一次未变异的对照组（否则等于没加）

**双机协作分工**：WB（本端）改 Swift + 自检；**CB / Mac 侧**改 `prototype/index.html`
（**只读参考，绝不修改**）、编译、真机验证、`[mac-fix]`。

## 四、长期记忆里的关键坑（都是真踩过的）

1. 🔴 **Swift `catch` 不到 ObjC 异常** —— 只能在"入参是否合法"上防；`catch` 是幻觉。
   读硬件状态的 API 先问"它此刻处于有效状态吗"（`startRunning()` 是异步的）
2. 🔴 **`let` 带默认值的属性不进 memberwise init** —— 配置字段要用 `var`，
   否则传参报 "extra argument"（编译才暴露，代价一轮往返）
3. 🔴 **`AVCaptureDevice` 不暴露镜头绝对焦距** —— 别用 mm 相除；
   按**镜头角色**读设备（`virtualDeviceSwitchOverVideoZoomFactors` + 原生阶梯）
4. 🔴 **自检守卫的作用域**：断言"某函数里必须有什么"要用 **`methodBodyOf` 切方法体**，
   全文匹配会被**别处同类代码**满足（假绿）；**字符窗口**会被注释挤爆（假红）
5. 🔴 **两条检查脚本会"打架"** —— 断言语义跨两个文件时，只能放**两边都读**的脚本里
6. 🔴 **镜像/同步必须整目录**，且顺序固定：**① D: fetch+merge → ② D:→工作区 → ③ 改 →
   ④ 工作区→D: → ⑤ add/commit/push**。硬编码文件清单会**静默少同步**；顺序颠倒会覆盖别人的改动
7. 🔴 **几何账要写成可执行的检查**（读令牌 + 逐机型复算），文档只贴脚本打印的数字 ——
   手算表已两次漏项（`docs/11` 漏常驻条 / `docs/16` 漏顶栏块，各乐观 8.5pp）
8. **2pt 余量 = 危险信号**（`docs/11` 的 50.2% 就是 2pt 余量），宁可压高度换 6pt
9. **中文字宽可按 1.0em 推，拉丁/数字必须实测**（「120 mm」实测 40.0pt，估算窄 25%）
10. **SF Symbol 名写错 → 静默留白**（编译与自检都拦不住）；小图形用 `Path` 自画更稳
11. 🔴 **能力探测 API 也要选对 —— "支持模式" ≠ "支持自定义值"**（2026-09-19 白平衡 7 连崩）：
    `isWhiteBalanceModeSupported(.locked)` / `isFocusModeSupported(.locked)` 在虚拟多摄上
    **误报 true**，SDK 明文（`AVCaptureDevice.h:538-541`）虚拟设备只允许锁 Current ——
    写计算值抛 ObjC 异常（Swift 拦不住）。正确探测：
    `isLockingWhiteBalanceWithCustomDeviceGainsSupported`（:1601）/
    `isLockingFocusWithCustomLensPositionSupported`（:1110）。
    **判据：问能力的 API 要与"将要执行的那次写入"逐字对应，别用同族近似 API 替代。**
    （自检第 12 组⑯守着三处方法体；变异测试 5/5 抓住）

## 五、文档索引（按需再读某一节，不要一次全读）

| 文档 | 什么时候读 |
|---|---|
| `CONTEXT_HOT.md` | 项目状态总表：顶部「〇」是 Swift 进度快照（含 Mac 验证记录）+ Backlog；下方是原型迭代历史（参考） |
| `docs/04-交接单.md` | 需要**完整**工程纪律 / 已知风险 / 恢复方式时 |
| `docs/08-P2-Swift同步任务书.md` | 需要 **B 组完整路线**（B3-B5）、几何规约、验证约束时 |
| `docs/15-B1焦段切镜头方案.md` | 改**焦段 / 变焦**相关（映射、Ramp、会话稳定性、第 11 组自检） |
| `docs/16-B2-ISO快门白平衡刻度条方案.md` | 改**参数刻度条**（第 12 组自检、净可见账表、B2a/B2b 交付记录、冷启动崩溃） |
| `docs/17-焦段条加35mm档方案.md` | 改**焦段数据 / 档位数**（净可见上限 6 档、声明式扩展、宽度预算） |
| `docs/00-启动包.md`（本文件） | **新会话唯一入口** |
| `docs/00-新会话开场白.md` | 旧版开场白（已并入本文件，只留指针） |
| `docs/03-布局调整方案.md` | 动布局总方案 / 高度预算时 |
| `docs/09-2-5快门排方案.md` | 动快门排时 |
| `docs/10` ~ `docs/14` | #7 滤镜条 / 底部手势与参数排 / #10 功能面板 / #11 格式芯片 / **EV 绑定回写环**（B3 要接同款纪律） |
| `docs/05-双机协作流程.md` | 需要与 CB/Mac 协作的完整流程时 |
| `README.md` | 验收清单 + Mac 从零跑起来六步 |

## 六、本机环境速记（Windows · WB 端）

- **无 Xcode / 无法编译 / 无模拟器**：只能靠**静态自检 + 变异测试**；编译与真机交给 Mac
- 两个自检：`node tools/check_swift.js LumenEdit` · `node tools/check_presets.js .`
  （变异测试：`python .workbuddy/mutation-test-b1.py`，一次性脚本不提交）
- 同步/镜像脚本（一次性、不提交）：`sync-from-main.py`（D:→工作区）/ `mirror-to-main.py`（工作区→D:）
- **Git Bash 缺 `grep` / `sleep` / `head`** → 文本匹配用工具，别用命令
- Python 3.13 在 `C:\Users\edcee\.workbuddy\binaries\python\versions\3.13.12\python.exe`（托管优先）
- Node 22 在 `C:\Users\edcee\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`

## 七、收尾清单（每次交付前过一遍）

- [ ] 两个自检全过；新守卫已做**变异测试**（守卫自己也会写错）
- [ ] 改了 UI 几何 → 跑**净可见账**（第 12 组⑩），并核**宽度预算**（第 5 组）
- [ ] 镜像后 `git status` **核对改动面**（只能多出本笔要交的文件）
- [ ] 提交信息写清：动机 / 改动 / 偏离方案点 / 验证与未验证 / 交给 Mac 的验收口径
- [ ] 收尾时更新**本文件第一、二、四节** + `CONTEXT_HOT` 的 〇 / Backlog / 下一步

## 八、Mac 侧复验备忘（真机验证要用，转给 CB 时直接贴）

- **真机截图**：`pymobiledevice3 developer dvt screenshot out.png`
  （PATH 加 `$HOME/Library/Python/3.9/bin`；已 pip3 install --user，自动走原生隧道无需 sudo）
  ⚠️ `idevicescreenshot` 在 iOS 26 **确定性损坏**（libimobiledevice issue #1465），重启/重插无效，别再试
- **装机**：`devicectl device install app --device 81FD8131-… /tmp/lumen-dd/Build/Products/Debug-iphoneos/LumenEdit.app`
- **取日志**：`devicectl device copy from --domain-type appDataContainer`
  `--domain-identifier com.wokenday77.lumenedit --source Documents/lumen-debug.log`
- **签名**：Team `H7XJ2PBZJG`（Personal，证书 **7 天过期**；过期重装后要在
  设置 → 通用 → VPN 与设备管理 重新信任）
- **连接抖动是常态**：`Device is busy (Waiting to reconnect)`（build EXIT=70、不编译）与
  `Developer Mode is disabled` 假象都是抖动，**重试即可**；`ddiServicesAvailable: true` 才是真状态
- **zsh 坑**：`git show 'abc~1':path` 的 `^` / `~` 要加引号；变量不自动分词（curl 参数别走变量拼）

## 九、验证口径速查（本端怎么"验"、Mac 怎么"核"）

**WB 端（Windows，无 Xcode）能做的只有静态**：

| 手段 | 命令 | 能查出什么 |
|---|---|---|
| 结构自检 | `node tools/check_swift.js LumenEdit` | 括号配平 / 顶层重名 / 状态引用完整性 / 各组件几何账 / **净可见账**（第 12 组⑩）/ 各组专属守卫（现 12 组，第 12 组含⑯虚拟多摄诚实边界） |
| 预设同源 | `node tools/check_presets.js .` | 滤镜 / 风格 / 场景 / 焦段 / 码率 / **刻度条三张表** 与原型逐条一致（现 6 组） |
| 变异测试 | `python .workbuddy/mutation-test-b3a.py`（本轮；一次性脚本不提交） | **新加的守卫真的会报错吗**（对副本注入坏数据 → 看是否出现预期 FAIL → 再跑未变异对照组） |

**Mac 端核法（读日志，不用量截图）**：

| 事项 | 日志里看什么 |
|---|---|
| 冷启动白平衡（上轮崩溃修复，**已验证 ✅**） | 先一条 `白平衡增益尚未就绪（冷启动）→ 色温先给兜底值，就绪后补发`，后一条 `白平衡增益已就绪（冷启动）→ 补发手动档状态` |
| **白平衡 7 连崩修复 + 手动档置灰（本轮，等复验）** | 点白平衡/感光/快门手动开关：**不崩**；toast「当前多摄虚拟设备不支持手动白平衡/曝光 · 切物理镜头后开放」；warn 一条「点 … 手动开关被拒」 |
| 手动能力探测（日志行） | 冷启动一行「手动参数能力：曝光(custom)=false / 白平衡(锁定增益)=false / 对焦(锁定位置)=false」（三摄真机应三个 false） |
| **EV 圆盘（B3a，已复验 4/4）** | 点「曝光补偿」：底栏整条消失、圆盘右半出屏、指针钉 9 点；toast 在上半区；拖动 0.1 步进 + toast「曝光补偿已归零」（点数值框）；互斥两方向（`docs/18` 8.4 节） |
| **对焦圆盘（B3b，最新）** | 点「对焦」：toast「手动对焦：当前多摄虚拟设备不支持 · 点取景器任意位置仍可自动对焦」+ warn 一条；点取景器对焦照常可用（对焦盘真机可视等物理架构后） |
| B1 档位映射 | 冷启动一行「变焦拓扑」：`档位=13→1.000 / 24→2.000 / 35→2.917 / 48→4.000 / 120→10.000`，且 **120 档 == switchOver[last]** |
| 刻度条（B2） | 点/切时 toast + 刻度条日志；**自动态禁止滑动**；松手吸附最近档 |
| 遮幅（backlog ⑤） | `遮幅 1:1：… 上下黑边 174/202，窗中心（全屏）437 vs 全屏中心 437` |
| 拍照视场（backlog ⑥，未修） | 照片比取景器**多出左右** —— 预览 aspectFill 裁掉 ~39%，待 P2 同批 |

## 十、本文件与 `CONTEXT_HOT.md` 的关系（谁为准）

- **进度 / 下一件事 / 关键坑** → 以**本文件**为准（每次收尾更新，是最新的）
- **原型迭代历史 / Mac 侧验证记录全文 / Backlog 的完整叙述** → `CONTEXT_HOT.md`（它很长，按需查）
- 两边若出现矛盾：先查 git log 确认谁更晚更新，**再信更晚那份**；别凭印象二选一
- 新会话开场只需贴**本文件**；`CONTEXT_HOT` 不用整份贴（太长），按第五节索引按需读

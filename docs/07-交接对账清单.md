# 项目完成清单（交接对账版）

> **项目**：LumenEdit · 自用 iOS 相机 / 修图 App
> **生成时间**：2026-09-16
> **生成环境**：Windows（无 Mac、无 Xcode、无编译能力）
> **文档用途**：**给另一台机器上的 AI 做交接对账**。文件路径与大小均为磁盘实读，可直接逐条核对。
> **对账口径**：本文档只描述**磁盘上真实存在的状态**，不含任何"应该能跑"的推测。凡未编译、未真机验证的，一律归入「未验证」章节。

---

## 〇、30 秒速览

| 维度 | 结论 |
|---|---|
| 代码规模 | **32 个 `.swift`** + 6 个资源 + 7 篇文档 + 1 个原型 HTML + 5 个工具脚本 |
| 已完成 | P1a 照片闭环、P1b-1 Live Photo 拍摄（**仅代码层面**）；网页原型两轮对标 |
| 未完成 | P1b-2 视频录制 → P7，共 7 个阶段 |
| 未验证 | **全部 32 个 Swift 文件从未编译过**；全部真机功能从未跑过；原型观感未经用户确认 |
| 最大卡点 | 没有 Mac；git 仓库 **0 次提交**；GitHub **未配置远端、从未推送** |
| 建议首步 | Windows 侧就能做的：git 首次提交 + 修 README 计数 + 拍板设计项；**然后才是**解锁编译 |

**环境事实（别假设错）**：用户没有 Mac，Mac 是租的/借的；用户不懂 Swift 开发；大体积工具链安装（Swift for Windows ~1GB、浏览器自动化）已被用户取消。

---

## 一、已完成 · 完整文件清单（按目录分组）

### 1.1 工程与工具（根目录）

| 文件 | 大小 | 作用 |
|---|---|---|
| `project.yml` | 1,848 B | **XcodeGen 工程定义，唯一的工程真源**；`.xcodeproj` 由它生成，不入库 |
| `.gitignore` | 575 B | 忽略 `*.xcodeproj/`、`build/`、`DerivedData/`、本地签名配置、`tools/*.txt` |
| `.github/workflows/build.yml` | 2,193 B | push 即在 GitHub `macos-15` runner 上无签名编译一次（公开仓库分钟数无限） |
| `README.md` | 16,963 B | 在 Mac 上从零跑起来、P1a+P1b-1 验收清单、架构约定、云端编译校验 |
| `CONTEXT_HOT.md` | 7,797 B | 对话续接摘要（对话超限时整份贴出，用于开新对话续接） |

### 1.2 资源 `LumenEdit/Resources/`（6 个）

| 文件 | 大小 | 作用 |
|---|---|---|
| `Info.plist` | 2,447 B | 4 条权限用途说明、仅竖屏、`arm64`、启动画面、状态栏样式 |
| `PrivacyInfo.xcprivacy` | 1,398 B | 隐私清单：UserDefaults / 文件时间戳 / 磁盘空间 三项必需理由 API |
| `Assets.xcassets/Contents.json` | 63 B | 资源目录根 |
| `Assets.xcassets/AppIcon.appiconset/Contents.json` | 215 B | App 图标声明（单尺寸 1024） |
| `Assets.xcassets/AppIcon.appiconset/AppIcon1024.png` | 38,813 B | 实际图标位图（由 `tools/make_app_icon.py` 生成） |
| `Assets.xcassets/AccentColor.colorset/Contents.json` | 695 B | 强调色（浅色 / 深色两套） |

### 1.3 App 层（3 个）

| 文件 | 大小 | 作用 |
|---|---|---|
| `App/LumenEditApp.swift` | 1,515 B | `@main` 入口。装配依赖容器、启动日志系统、把前后台事件转发给相机会话 |
| `App/AppEnvironment.swift` | 2,012 B | 全局依赖容器（单例）：`PermissionManager` / `RenderContext` / `ThumbnailCache` / `CaptureSessionController`。**保证全局只有一个 session、一个 CIContext** |
| `App/AppRouter.swift` | 3,875 B | 根路由 + 设置页（调试浮层开关、权限总览、日志导出/预览） |

### 1.4 DesignSystem 层（4 个）

| 文件 | 大小 | 作用 |
|---|---|---|
| `DesignSystem/Theme.swift` | 2,373 B | 颜色 / 间距 / 圆角 / 尺寸 / 字体令牌 |
| `DesignSystem/Haptics.swift` | 2,165 B | 触感反馈封装（生成器复用 + 全局开关） |
| `DesignSystem/Components/ParameterSlider.swift` | 5,652 B | 通用参数滑块：吸附步长、数值点击归位、居中填充、触感刻度 |
| `DesignSystem/Components/ModeSelector.swift` | 3,467 B | 模式切换条（未实现模式置灰加锁）+ `CaptureThumbnail` 相册缩略图 |

### 1.5 Core 层（10 个）

**`Core/Utils/`（4 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Core/Utils/Log.swift` | 7,846 B | 日志中枢：OSLog **+ App 沙盒文件双写**、200 条环形缓冲、512KB 自动裁剪。**侧载运行没有控制台，这是唯一排障手段** |
| `Core/Utils/Numeric+Clamp.swift` | 2,711 B | 数值钳制 / 吸附 / 去 NaN（硬件参数写入前必过）+ `FormatText` 显示格式化 |
| `Core/Utils/DebugHUD.swift` | 5,401 B | `CameraDebugSnapshot` + 屏幕调试浮层（点一下展开看最近日志、可导出） |
| `Core/Utils/DeviceStorage.swift` | 2,448 B | 剩余空间查询、按码率估算可录时长、低空间判断 |

**`Core/Permissions/`（1 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Core/Permissions/PermissionManager.swift` | 5,443 B | 相机 / 麦克风 / 相册读写 四路权限三态机，显式 continuation 包装系统回调 |

**`Core/ImagePipeline/`（3 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Core/ImagePipeline/RenderContext.swift` | 3,405 B | 全局唯一 `CIContext`（Metal 后端、固定工作色彩空间）。P4 起大量使用 |
| `Core/ImagePipeline/ColorSpaces.swift` | 2,356 B | 色彩空间约定（线性 sRGB 工作空间 / 输出 P3），避免"导出后颜色变了" |
| `Core/ImagePipeline/ThumbnailCache.swift` | 5,617 B | 缩略图缓存：**不解码原图**的降采样，以及无权限时的降级 |

**`Core/MediaIO/`（2 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Core/MediaIO/PhotoLibraryWriter.swift` | 8,160 B | 相册入库唯一出口：照片 / 视频 / **Live Photo 成对提交** |
| `Core/MediaIO/AudioSessionManager.swift` | 6,177 B | 音频会话激活与释放（录制场景类别、中断通知、释放时通知其它 App 恢复） |

### 1.6 Camera 层（15 个）

**`Camera/Session/`（5 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Camera/Session/CaptureSessionMode.swift` | 2,147 B | 模式枚举（`photo` / `livePhoto` / `video`）+ 每个模式的麦克风需求 + **实现状态开关**（当前 photo / livePhoto 为 `true`，video 待 P1b-2） |
| `Camera/Session/CaptureCapabilities.swift` | 5,714 B | 能力探测：设备回退链、格式挑选、帧率范围、编码格式。**不写机型判断** |
| `Camera/Session/CapturePreset.swift` | 3,279 B | `EditRecipe → CapturePreset → 硬件` 的中间结构，字段一次定义完整 |
| `Camera/Session/CaptureDeviceConfigurator.swift` | 13,088 B | **全工程唯一允许 `lockForConfiguration()` 的地方**，负责顺序与钳制 |
| `Camera/Session/CaptureSessionController.swift` | 22,943 B | 会话生命周期、模式切换动态增删 output、参数入口、调试快照（**工程内最大的文件**） |

**`Camera/Output/`（4 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Camera/Output/CaptureResult.swift` | 1,722 B | 三种采集产物的归一化模型 |
| `Camera/Output/PhotoCaptureService.swift` | 12,077 B | 拍摄门面：settings 模板复制、原始字节直取、等 `didFinishCaptureFor` 才收尾；Live Photo 的两半转发给配对方 |
| `Camera/Output/LivePhotoCaptureService.swift` | 8,695 B | Live Photo 拍摄编排：临时 MOV URL、写入 uniqueID 的 settings、收拢两个回调、产出配对结果 |
| `Camera/Output/LivePhotoAssembler.swift` | 5,565 B | 配对状态机：按 uniqueID 暂存两半，齐了才交付；顺带清理失败留下的临时文件 |

**`Camera/Hardware/`（1 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Camera/Hardware/CameraControlButton.swift` | 2,890 B | 相机控制按钮全按快门（`AVCaptureEventInteraction`），自管启停。**⚠️ 两处 SDK 签名待编译确认** |

**`Camera/UI/`（5 个）**

| 文件 | 大小 | 作用 |
|---|---|---|
| `Camera/UI/PreviewView.swift` | 4,605 B | `UIViewRepresentable` 包 `AVCaptureVideoPreviewLayer` + 点按对焦手势 |
| `Camera/UI/FocusIndicatorView.swift` | 1,291 B | 对焦方框动画（用 token 驱动，同位置连点也会重播） |
| `Camera/UI/ExposurePanel.swift` | 1,809 B | 曝光补偿面板 |
| `Camera/UI/CameraViewModel.swift` | 9,699 B | 相机页状态机：权限流程、保存、提示条、模式校验 |
| `Camera/UI/CameraView.swift` | 12,037 B | 相机主页（含权限引导页、会话失败重试条、快门按钮） |

### 1.7 文档 `docs/`（7 篇）

| 文件 | 大小 | 作用 |
|---|---|---|
| `docs/01-项目结构与第一阶段方案.md` | 33,234 B | 项目结构、分层方案、P1 阶段设计（最详尽的方案文档） |
| `docs/02-风格与滤镜清单.md` | 27,468 B | 10 款风格 + 14 款滤镜的完整参数（原型预设数据与它同源） |
| `docs/03-布局调整方案.md` | 13,576 B | 全屏取景 + 浮层式的布局推导与 50% 净可见面积底线 |
| `docs/04-交接单.md` | 12,392 B | 工程纪律 8 条、会话配置顺序、Live Photo 成对提交等铁律 |
| `docs/05-双机协作流程.md` | 9,335 B | Windows 写码 + Mac 编译的双机协作流程 |
| `docs/06-项目完成清单.md` | 12,506 B | 进度快照（阶段看板 / 验收清单 / 风险），面向"看进度" |
| `docs/07-交接对账清单.md` | 本文档 | 面向"文件级对账"的交接清单 |

### 1.8 原型 `prototype/`（1 个）

| 文件 | 大小 | 作用 |
|---|---|---|
| `prototype/index.html` | 80,980 B | **单文件、零外部依赖**的网页版界面原型。含内联 SVG 占位画面、10 风格 / 14 滤镜数据、浮层互斥布局引擎、调试 HUD。双击即开 |

### 1.9 工具 `tools/`（5 个）

| 文件 | 大小 | 作用 |
|---|---|---|
| `tools/check_prototype.js` | 14,847 B | **原型自检（8 组）**：JS 语法、标签配平、CSS 配平、数据完整性、SVG、布局预算与互斥规则、对标元素齐备性。零第三方依赖 |
| `tools/check_swift.js` | 6,622 B | Swift 结构 / 语法级自检（无 Mac 环境下的替代手段） |
| `tools/make_app_icon.py` | 4,123 B | 纯标准库生成 1024×1024 App 图标（不依赖 Pillow），换配色改常量重跑 |
| `tools/check-report.txt` / `check-report-prototype.txt` / `check-swift-report.txt` | 1,703 / 1,911 / 373 B | 自检报告产物（被 `.gitignore` 忽略，可随时重新生成） |
| `tools/_cleanup.txt` / `_wrap.txt` | 1,122 / 795 B | 历史遗留的临时产物（非代码，可删） |

### 1.10 其它

| 文件 | 大小 | 说明 |
|---|---|---|
| `.workbuddy/memory/2026-09-14.md` | 31,424 B | AI 记忆文件。**⚠️ 当前已被 `git add` 暂存，建议移出版本控制** |
| `.workbuddy/memory/2026-09-15.md` | 8,380 B | 同上 |
| `.workbuddy/memory/MEMORY.md` | 5,101 B | 同上 |
| `06-项目完成清单_2026-09-16.zip` | 6,383 B | 上一版清单的打包件（交付物，非源码） |

### 1.11 对账基准数字（照这个数）

| 类别 | 应有数量 |
|---|---|
| `.swift` 源文件 | **32**（App 3 · DesignSystem 4 · Core 10 · Camera 15） |
| 资源文件 | 6 |
| `docs/*.md` | 7 |
| `prototype/*.html` | 1 |
| `tools/` 脚本（`.js` / `.py`） | 3（另有 5 个 `.txt` 报告/临时件） |
| 根目录配置文件 | 5（`project.yml` / `.gitignore` / `README.md` / `CONTEXT_HOT.md` / `.github/workflows/build.yml`） |

> **已知文档缺陷**：`README.md` 写「Camera 层（13 个）」「合计 30 个 Swift 文件」，**实际是 15 个与 32 个**（表里条目完整，只是表头计数写错）。请在交接时以本文档数字为准。

---

## 二、未完成 · 待办事项（P1b-2 → P7）

### P1b-2 · 视频录制 ⬜ 下一批

- `MovieCaptureService`：基于 `AVCaptureMovieFileOutput` 的录制编排
- 录制指示器：计时 / 红点 / 剩余可录时长（`DeviceStorage` 已备好估算能力）
- 录制中禁止拍照、禁止切模式
- 录制结束落盘相册（`PhotoLibraryWriter` 已支持视频写入）
- `CaptureSessionMode.video` 的 `isImplemented` 打开，UI 解锁
- 原型侧已就位：模式条「视频」入口（当前置灰 + 挂锁 + 原因 toast）

### P2 · 手动参数与硬件控制 ⬜

- ISO / 快门 / 白平衡 **手动档**（自动档与手动档互斥）
- 镜头切换与变焦（原型焦段条 13/24/48/120mm 对应的真实实现）
- 格式与帧率选择
- 相机控制按钮**轻按滑杆**（`AVCaptureEventInteraction` 的半按手势）
- 修图页预设注入（`EditRecipe` 正式引入）
- 设置页补齐：简易模式持久化、毛玻璃开关、网格开关

### P3 · 媒体导入 ⬜

- `PHPicker` 接入
- `PHAsset` 类型识别
- Live Photo **双资源解析**（照片 + 配对视频）

### P4 · 修图引擎 ⬜

- 10 项基础调整（参数清单见 `docs/02`）
- ≥ 10 款滤镜（`docs/02` 已定 14 款）
- 实时预览（走 `RenderContext` 的唯一 `CIContext`）
- 照片导出（`docs/02` 的色彩空间约定）

### P5 · Live Photo 编辑与配对导出 ⬜

- `PHLivePhotoEditingContext` 逐帧处理
- 配对导出（保持 Live 属性，不能退化成静态图）

### P6 · 视频编辑 ⬜

- 裁剪 / 变速 / 逐帧滤镜
- MOV 导出且保留音频

### P7 · 人像 / 景深 ⬜

- 状态：**未排期**，可选项

### 跨阶段待办（非功能）

- [ ] `CaptureSessionMode` 增加 `logLive` case（原型已有第 4 个模式入口，Swift 端只有 3 个）
- [ ] **原型 → Swift UI 同步**（详见第四节 4.2）

---

## 三、未验证 · 写了但没跑过的

### 3.1 编译层面（✅ 2026-09-16 已解决）

- ~~**全部 32 个 `.swift` 文件，从诞生到现在从未被编译器看过一眼**~~ → **已在 GitHub Actions 编译通过**
  （macos-15 + Xcode 16，无签名编译，74 秒，**零 error、零代码 warning**）。
- **首批编译暴露的两个真实错误，均已修复**（留档避免重复踩）：
  1. **`AVCaptureEventInteraction` / `AVCaptureEvent` 属于 AVKit，不是 AVFoundation** ——
     只 `import AVFoundation` 会报 `cannot find type ... in scope`，已补 `import AVKit`。
     同时确认初始化签名**只有** `init(handler:)` 与 `init(primary:secondary:)`，
     **不存在 `primaryAction:` 标签**（那是 SwiftUI `onCameraCaptureEvent` 的参数名）。
  2. **`AVCapturePhotoSettings.uniqueID` 是只读属性**（由系统分配），不能自己赋值 ——
     已改为「运行时认领」：首个回调到达时记下系统 ID，之后校验一致性，迟到回调按 ID 不匹配丢弃。
- ~~**GitHub Actions 从未跑过**~~ → **已跑通 3 轮**，第 3 轮 success。
  仓库 `github.com/wokenday77/LumenEdit`（公开），push 即自动校验。

### 3.2 真机层面（模拟器全部不可信）

模拟器**没有摄像头**，以下项目在模拟器上完全无从验证：

- 整个相机模块（会话能否起来、预览能否出画面）
- 点按对焦是否真的生效
- 曝光补偿是否真的改变画面
- 相机控制按钮（`AVCaptureEventInteraction`，仅 iPhone 16 及以后）
- HEIC 原始数据直存后的画质与 EXIF 完整性
- **Live Photo 是否被系统识别成一对**（成对提交只有在真机相册里才能验证）
- **Live Photo 的音轨**（麦克风权限 + 音频会话）
- 拍摄功耗与发热

**P1a / P1b-1 的真机验收项一条都没勾过**（完整清单见 `README.md` 第三节），至少包括：
- 冷启动权限流程与取景出画时间
- 点按对焦的焦点实际变化
- 连拍 20 张不崩不卡
- Live Photo 长按能播放动图、**不是"一张静态图 + 一段孤立视频"**
- 连拍 10 张 Live Photo 不串号（验证配对状态机）
- 拍完 tmp 目录无 `lumen-live-*.mov` 残留

### 3.3 原型层面

`prototype/index.html` 通过了自检脚本的 **8 组检查**（语法 / 配平 / 数据完整性 / 布局预算 / 对标元素齐备性），但**这是结构级验证，不是视觉验证**。以下必须人眼看：

- 整体观感（顶栏两行 / 焦段药丸 / 底部七图标行 / 快门排四件套 与参考图的差距）
- **鼠标下能否拖动**：参数排 EV 竖滑块、简易模式 EV 横条（用的是 `pointerdown/move/up + setPointerCapture`，理论可用，未实测）
- 点「Log 实况」「视频」是否置灰 + 原因 toast
- HUD 各状态的净可见取景高度是否 ≥ 50%（脚本算得最差 51.6%，但那是按 CSS 静态算的）
- 新增件逐个点击是否有反馈：闪光灯 / 网格 / 影调预览 / 31 GB / 镜头切换 / 风格方块
- 焦段药丸选中态是否"转白底黑字"

### 3.4 设计项拍板（2026-09-16 已定，不再阻塞 Swift 端）

- [x] **模式条保留 4 个入口**：照片 / 实况 / Log 实况 / 视频。`Log 实况`是产品差异化，**先置灰不实现**（挂锁 + 点击给原因）
- [x] **「实况」统一用同心圆图标**：模式条早已是同心圆；**取景器角标已从「实况」文字改为同心圆**（黄底黑图标，明亮画面上仍醒目），与系统相机一致
- [x] **HUD 保持收在「更多」菜单**：默认关闭；不做常驻小胶囊，不加键盘快捷键

---

## 四、当前卡点

### 4.1 git 状态（2026-09-16 更新：首次提交已完成）

| 项 | 值 |
|---|---|
| 仓库 | `D:\AI 工作存储\iPhone相机\.git` |
| 当前分支 | `main`（原 `master` 已改名，与 GitHub 默认一致） |
| **提交数** | **1** —— `00303f1`「P1a + P1b-1 交付：照片闭环与 Live Photo 拍摄」 |
| 入库文件 | **55 个**（32 Swift + 6 资源 + 7 docs + 1 原型 + 3 工具 + 6 根配置） |
| 工作区 | 干净（`git status --short` 为空） |
| 远端 | **仍无** —— 等 GitHub 仓库地址与凭据 |
| `.xcodeproj` | **不存在**（正确行为，由 `xcodegen generate` 生成，且已被 `.gitignore` 忽略） |

**回退能力已具备**：`git checkout .` 丢弃工作区改动；`git reset --hard 00303f1` 回到首次提交。

**本次一并处理完的清理项**（原文的"已知待清理项"已全部解决）：
- `.workbuddy/` 已加入 `.gitignore`，并用 `git rm -r --cached -f` 移出版本控制（**磁盘文件保留**，只是不再跟踪）
- 新增 `.gitattributes`：`* text=auto eol=lf` —— 统一 Windows 编写 / macOS 编译之间的换行符，避免每次跨机 diff 出现"整个文件都变了"的噪音
- 根目录 `*.zip` 文档打包件已忽略
- `tools/*.txt` 自检报告与临时件仍被忽略（可随时用脚本重新生成）

### 4.2 GitHub 推送状态

- **从未推送过**：无远端配置、无提交、无仓库地址记录。
- 结论：`.github/workflows/build.yml` 目前是**沉睡状态**，云端编译校验这条"没有 Mac 也能确认能不能编译"的唯一通路**尚未打通**。

### 4.3 Mac 侧准备情况

| 项 | 状态 |
|---|---|
| Mac 设备 | **用户没有 Mac**，是租的或借的；当前不可用 |
| Xcode 16.0+（需 iOS 18 SDK） | 未知 / 未确认 |
| XcodeGen（`brew install xcodegen`） | 未知 / 未确认 |
| 签名 Team（Apple ID） | 未定 |
| Bundle Identifier | **未定**，README 提醒**必须改成全局唯一**（默认 `com.lumenedit.app` 大概率被占用） |
| 开发者账号 | 预计用免费 Apple ID（签名 **7 天**有效，到期闪退需重装） |
| 设备连接 | iPhone 需开**开发者模式**（要重启手机）、信任本机 |
| 工程生成 | 未执行（`.xcodeproj` 不存在） |

### 4.4 其它环境约束

- 大体积工具链安装**用户不接受**（Swift for Windows ~1GB 已取消；浏览器自动化下载也已取消）→ 本地只能做**零依赖自检**
- 云端 Mac **无法 USB 连接 iPhone** → 只能做编译校验，**不能真机调试**

---

## 五、下一步建议（按顺序，别跳步）

### 第 1 步 · Windows 侧就能做（不需要 Mac，今天可完成）

1. **git 首次提交，锁住现状** —— 这是当前唯一的"回退保险"
   - 先把 `.workbuddy/` 加进 `.gitignore` 并 `git rm -r --cached .workbuddy`
   - 再 `git add -A && git commit`
   - 建议提交信息里写清"P1a + P1b-1 代码交付 / 原型两轮对标完成"
2. **修正 `README.md` 计数错误**：Camera 层 13 → 15、Swift 总计 30 → 32
3. **用户拍板三个设计项**（见 3.4）—— 不拍板，Swift 端 UI 同步无法开工

### 第 2 步 · 打通"能编译"这条关键路径（二选一）

- **方案 A（推荐，成本低）**：建一个 GitHub 仓库 → `git remote add` → push → 看 Actions 能不能编过。能把 32 个文件里的**纯编译错误**一次性暴露出来（公开仓库分钟数无限，可反复推）。
- **方案 B（更彻底但要设备）**：拿到 Mac → `xcodegen generate` → 改 Bundle ID / 选 Team → ⌘R → 把报错原文贴回来逐个修。

> 这一步不完成，后面每写一行代码都是"盲写"。**优先级高于任何新功能。**

### 第 3 步 · 原型 → Swift 端 UI 同步

前提：第 1 步的设计项已拍板。要同步的（原型上已定版，Swift 端全无）：

- 模式条四入口 + "实况"图标 → `CameraView.swift` / `ModeSelector.swift`
- `CaptureSessionMode` 增加 `logLive` case（置灰 + 原因）
- 顶栏两行（L/R 电平表、闪光灯·网格·更多、影调预览、剩余存储）
- 焦段条 13/24/48/120mm
- 底部图标行 7 项（前置 / 对焦 / 白平衡 / 感光 / 快门速度 / 曝光补偿 / 设置）
- 快门排四件套（缩略图 · 快门 · 镜头切换 · 风格预览方块）

### 第 4 步 · P1b-2 视频录制

`MovieCaptureService` + 录制指示 + 录制中禁止拍照，打开 `CaptureSessionMode.video`。

### 第 5 步 · 按主线顺序推进

P2（手动参数 / 镜头变焦）→ P3（媒体导入）→ **P4 修图引擎**（产品核心）→ P5（Live Photo 编辑）→ P6（视频编辑）→ P7（人像，可砍）。

### 每次改动后的固定自检（纪律）

```bash
# 原型改完必跑
node tools/check_prototype.js prototype/index.html tools/check-report-prototype.txt

# Swift 改完必跑
node tools/check_swift.js LumenEdit
```

---

## 六、给接手方 AI 的对账动作清单

按顺序执行即可完成对账：

1. **文件数对表**：确认 `.swift` = 32（App 3 / DesignSystem 4 / Core 10 / Camera 15）、资源 6、`docs/*.md` = 7、`prototype/*.html` = 1
2. **跑两个自检脚本**（零依赖，Node 即可），确认原型 8 组全过
3. **核对 git**：`git rev-list --all --count` 应为 `0`（若接手时已提交，说明第 1 步已被执行，需同步本清单的第 4/5 节）
4. **核对未验证清单**：不要看到代码就认为"已完成"——第 3 节列的东西**全部没有跑过**
5. **读文档顺序**：`CONTEXT_HOT.md`（交接摘要）→ `docs/07`（本文档）→ `docs/04-交接单.md`（工程铁律）→ `docs/01`（整体方案）

---

## 七、工程铁律（改动时必须遵守，详细见 `docs/04-交接单.md`）

1. **分层依赖单向向下**：UI 不直接碰 AVFoundation / Photos，全部经服务层
2. **`EditRecipe` 是唯一真源**（P4 引入）：同一份参数同时驱动相机预设注入、修图实时预览、Live Photo 逐帧、视频逐帧
3. **会话配置顺序不可调整**：`beginConfiguration` → `sessionPreset = .inputPriority` → `addInput` → 按模式 `addOutput` → `commitConfiguration` → **之后**才写 `activeFormat` / 帧率
4. **`CaptureDeviceConfigurator` 是唯一允许 `lockForConfiguration()` 的地方**，所有硬件参数写回前必须钳制（越界是**抛异常**，不是被忽略）
5. **Live Photo 必须成对提交**：照片与配对视频放**同一个** `PHAssetCreationRequest`，拆成两次系统认不出一对
6. **UI 层不实现"点了没反应"**：未实现的功能必须置灰并给出原因
7. **权限申请按需触发**：相册写入在第一次保存时才申请，麦克风在切到需要录音的模式时才申请

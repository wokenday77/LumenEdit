# B3b 方案：对焦圆盘（#8 后半）· 共享盘体重构 + 能力分派

> **状态：已拍板（2026-09-19 晚）并实施完毕；2026-09-20 正源修（`backlog ⑩`）等 Mac 复验**。
> 拍板记录：① 虚拟设备呈现 = **A（toast 不开盘）**（"和 B2 手动开关同款诚实边界；盘 UI 代码
> 全落地——架构改造后自动点亮，零改动"）；② 方向语义**维持现状**（"两盘值方向相反、
> 盘跟手完全一致，这正是镜像布局的物理自洽"——用户原话）。
>
> ## ⚠️ 2026-09-20 正源修：对焦档状态 = **用户意图**，不从硬件回读推断（`backlog ⑩`）
>
> **根因链**（Mac 复验 ③ 抓到，真机实测）：SDK `.autoFocus` **对焦一次完成后系统自动转
> `.locked`** → B3b 的"回读三件套"按 `focusMode == .locked` 推断"用户锁了手动对焦" →
> 点按一次后 `isFocusManual` 永久为真 → 点按全走"仅测光"（该路径不碰 focusMode）→
> **死锁：点按对焦永久失效**。Mac 最小修 `670970b`（能力门：不支持手动对焦的设备返回 nil）
> 只在虚拟设备上精确恢复行为，**架构改造后能力 = true，必复发**。
>
> **正源修（本笔）**：
> 1. `session.manualFocus` 语义改为**用户意图态**：写入点**只有** `setManualFocus(lensPosition:)`
>    （盘上拖动 / 关自动开关）置值、`setAutoFocusMode()` 置 `nil`；**`publishManualState`
>    绝不写它**（自检⑱"同源污染守卫"：写入点出现在意图入口之外当场 FAIL）。
> 2. **回读只喂 `currentLensPosition`**（读数，自动/手动都跟硬件；自动对焦进行中读数自己走）。
> 3. `configurator.manualFocus(of:)` **删除**（含最小修的能力门）——回读推断是污染源，别加回来。
> 4. VM `focusEditingChanged` **删掉 `guard !isFocusAuto` 拦截**：第一次拖动就是"进入手动
>    对焦"的意图表达（此刻 `manualFocus` 还是 nil），拦截 = 拖动永远无效；自动档下盘面已被
>    `DialView` 的 `autoMode` 锁住，到不了 VM。
> 5. 自检⑱ f/g 同步：负向断言"configurator 不得再有 manualFocus(of:)"、"publishManualState
>    不得写 self.manualFocus"、"闸门不得含 isFocusAuto"；变异 **M5**（publishManualState 塞回读
>    赋值）—— **5/5 抓住** + 对照组绿。
>
> **教训（通用）**：**硬件状态回读 ≠ 用户意图**。能被系统自己改写的状态（focusMode、
> exposureMode）只能当"读数"，不能当"档位"；档位必须来自用户操作路径的显式记录。
>
> **交付记录（2026-09-19 晚，两笔提交）**：
> - 第 1 笔（重构）：`ExposureDialView.swift` → **`DialView.swift`**（DialGeometry 参数化 +
>   `DialConfig` 两盘工厂 + `DialView` 共用视图 + 开关行槽位）；Theme 令牌 `evDial*` → `dial*`、
>   新增 `dialAutoSwitch*` 五个 + `dialSwitchOn(#30d158)/Off(#39393d)`；EV 行为零变化
>   （自检 12 组 + b3a 变异 6/6 回归全过）。
> - 第 2 笔（接入）：`DialConfig.focus` 接入数据链与入口 —— `focusEditingChanged` 闸门 +
>   `$currentLensPosition` 回写订阅、`focusDialTapped` 能力分派、`focusAutoToggled` 开关、
>   拍板 ③ 点按分流（`setExposurePointOnly`）、互斥收口 5 样、ToolIconRow 文档表更新。
>   自检新增 **⑱ 组** + b3b 变异 4/4（正源修后 5/5）。
> - Mac 复验：B3b 3/4 → ③ 回归（见上根因链）→ Mac 最小修 `670970b` + 落盘 `c7aba88`/`5a29085`
>   → **正源修本笔**。对焦盘真机可视/拖动等物理架构改造后一次验收。
>
> 前置：B3a 已交付且过 Mac 三次复验（`8d956c2` / `651cc5c`，4/4）；三条交互约定已由
> [mac-fix] `f482339` 落地并记入 `CONTEXT_HOT` backlog ⑨（半透明盘底 0.45/0.50/0.55 ·
> 字号按 400 坐标系缩放 · 相对位移拖拽模型）。

---

## 〇、结论先行（TL;DR）

1. **抽共享组件 `DialView`**（Mac 在 `ExposureDialView.swift:216` 留的架构指示）：
   盘底 / 刻度环 / 数字 / 指针 / 盘心 / 数值框 / **相对位移拖拽模型**全部进共享组件，
   EV 与对焦只差一份 `DialConfig`（mirror / 指针角 / 值域 / 档位分级 / 文案格式 / 圆心锚点）。
   **三条约定的"自动继承"就是这条重构** —— 不是复制三行注释提醒自己同步。
2. **能力分派**（拍板点 ①）：虚拟多摄上点「对焦」图标 = **toast 说明、不开盘**
   （与 B2 手动开关同款诚实边界）；盘 UI 随本件代码全落地，真机可视与拖动写硬件
   等物理镜头架构改造后**自动点亮**（能力探测 `isLockingFocusWithCustomLensPositionSupported`
   已在守卫里）。
3. **方向语义：保持现状**（"顺时针滑 = 值减小"）—— 意见与依据见第四节。
4. **新增对焦数据链**：对焦此前没有回读链（曝光/白平衡有），本件补齐
   `configurator.manualFocus(of:)` → session `@Published` → VM 派生 + `focusEditingChanged`
   编辑态闸门（`docs/14` 同构）+ 「自动对焦」开关由**硬件真值回读**驱动（不本地记账）。

---

## 一、与 EV 圆盘的共用关系（为什么是重构而不是复制）

Mac 三改后的 `ExposureDialView.swift` 已经是"正确答案的形状"：相对位移拖拽、点击锚定不改值、
字号乘 scale、半透明盘底、两个 `.position` 前置的真机坑注释。复制一份 `FocusDialView`
等于把这份正确答案**抄一遍**——三条约定将来任何一条再调（比如盘底透明度再改），
就要人肉同步两处。Mac 的注释原话："届时应抽共享盘底，不要抄第二份"。

**重构形态**：

```
Camera/UI/DialView.swift          ← 新（由 ExposureDialView.swift 泛化重构而来）
  ├─ DialGeometry                 ← 纯几何（mirror / pointerDeg 参数化；半径体系不变）
  ├─ DialConfig                   ← 两盘差异全部收进这一个值（见第二节规格表）
  └─ DialView                     ← 盘底 / 刻度环 / 数字 / 指针 / 盘心 / 数值框 / 拖拽 / 开关行槽位
Camera/UI/FocusDialEntry.swift?   ← 不需要独立文件：CameraView 直接 DialView(config: .focus, …)
ExposureDialView.swift            ← 删除（内容并入 DialView；CameraView 的 EV 调用点改 config: .ev）
```

- **自动继承的机制**：盘底透明度、字号 ×scale、`dragAnchor` 相对位移模型、两个
  `.position` 前置坑 —— 全部只存在于 `DialView` 一处；两盘 config 不同而已。
- **回归风险与兜底**：重构动的是刚复验通过的组件 —— 兜底三层：
  ① 自检⑰的断言对象同步改为 `DialView.swift`（断言语义不变）；
  ② 变异测试脚本 `mutation-test-b3a.py` 锚点同步（并补对焦侧突变）；
  ③ Mac 复验 EV 盘回归（4 项快速口径：拖动顺滑顺走 / 点击不改值 / 字号 / 归零）。
- ⚠️ `ExposureDialView.swift` 文件名消失 → **Mac build 前需 `xcodegen generate`**（增删文件）。

## 二、两盘差异规格表（全部对齐原型，DialConfig 逐项来源）

| 维度 | EV 盘（`config: .ev`，现状不变） | 对焦盘（`config: .focus`，本件新增） | 原型依据 |
|---|---|---|---|
| mirror | `true`（露左半、右半出屏） | `false`（露右半、**左半出屏**） | `DIALS.focus.mirror:false`；圆心第 2 格 |
| 圆心 X | `md + xs + 5.5×格宽`（iconEV 第 6 格） | `md + xs + 1.5×格宽`（iconFocus 第 2 格，393 屏 ≈96.8，左裁 ≈26.2pt） | `--fd-center-x:83.1` = pad+1.5×格宽 |
| 指针角 | 270°（9 点，数值框盘左） | **90°（3 点，数值框盘右）** | `fdPointerDeg` |
| 值域 / 步进 | ±3 / 0.1 / 61 档 | **0 ~ 1** / 0.01（显示两位）/ 51 档 | `DIALS.focus`: count 51, min 0, max 1 |
| 档位分级 | 主 7（每 1EV）/ 中 6 / 副 | **主 11（每 0.1）/ 中 20 / 副 20**（`i%5==0` 主、`±1` 中） | `tickKind` |
| 数字文案 | `+1.5` / `0` / `−1.5` | `0.0 … 1.0`（一位小数，`toFixed(1)`） | `numText` |
| 数值框 | 盘左、**点击归零**（`+1.5 EV`） | **盘右、不可点**（`0.56`；≥0.995 显示 **∞**） | `.fd-val` 无 click 绑定；`fmt: val>=0.995?'∞'` |
| 盘下开关行 | 无 | **「自动对焦」开关行**（sw 29pt + label，盘下 gap 12） | `.fd-wrap` 第二个 flex 项 / `.fd-auto` |
| hub | ⊖ + 「曝光补偿」 | **◎（圆点 + 虚线圆 + 四向短线）+ 「对焦」**——自画 Path，不用 SF Symbol | `.fd-hub` svg |
| toast | 「拖动圆盘调 ±3 EV，点数值归零」 | 「手动对焦：拖动圆盘调焦（0.0 近 → ∞ 远）」 | 原型 toast 文案 |
| 几何复算 | 已在自检⑰ | 开关行**底沿距屏底 ≥ 0**：`centerY + 123 + gap12 + 29 ≤ 容器高`（读令牌复算，844/375 都验） | `--fd-gap:12` / `--fd-auto-h:29` |

**开关行语义（对焦盘专属）**：
- **开（自动对焦）**：调 `configurator.setAutoFocus()`（已存在，连续自动优先）；UI 锁定手动拖动
  （原型 `focusAuto → focusFromPoint return` 同款）；**不需要**原型的模拟动画（900ms 随机过渡
  是"原型没有真硬件"的代偿）——真机上 `lensPosition` 会被系统实时更新，**回读链驱动的读数
  自己会走**，比模拟更有质感。
- **关（手动）**：值停在当前处，恢复拖动。
- 开关状态**从硬件真值回读**：`focusMode == .locked` = 手动；`.continuousAutoFocus / .autoFocus` = 自动。
  不本地记账（与曝光/白平衡回读同纪律）。

## 三、数据链与编辑态闸门（本件补齐对焦的"回读三件套"）

曝光/白平衡已有"configurator 读 → session 发布 → VM 派生"三件套 + `docs/14` 闸门；
**对焦此前一条都没有**。本件补齐（全部照抄既有结构，不发明新形状）：

1. `CaptureDeviceConfigurator.manualFocus(of:) -> Float?`：`focusMode == .locked ? lensPosition : nil`
   （与 `manualExposure(of:)` 同构；顺带在**点按对焦**后由既有 `publishManualState` 自然覆盖）。
2. `CaptureSessionController`：`@Published private(set) var manualFocus: Float?`，在
   `publishManualState` 里随其它真值一起发布；**拍板 ③ 的执行点也在这里的调用方**
   （手动锁定期间点按对焦 → `focusTapped` 分流：只 `setExposurePoint` 不动焦，VM 层判
   `manualFocus != nil`）。
3. VM：`isFocusManual` 派生 + `focusEditingChanged(_:)`（`lastPushedLensPosition` +
   拖动期不回写 + 只接受最后推送值 —— `docs/14` 同构）+ `focusDialValueChanged`。
4. 自动对焦开关状态 = `manualFocus == nil`（派生，同 `isISOShutterAuto` 的做法）。

⚠️ **点按对焦的分流**（拍板 ③ 落地点）：现有 `setFocusAndExposurePoint` 会把曝光和**对焦**
一起打回自动 —— 手动锁定期间 VM 层改调"只测光"路径（`setFocusAndExposurePoint` 拆分出
仅曝光变体，或在 configurator 加参数），** hardware 层不动焦**。这是 2.4 表"显式降级"之外的
另一条点按语义，实施时一并落。

## 四、方向语义：**建议保持现状**（用户问询的意见）

**结论：保持"顺时针滑 = 值减小"，不改镜像。** 三条依据：

1. **数学上它被"顺滑顺走"锁死**：刚复验通过的相对位移模型是"盘面跟手"
   （ΔRing = Δcss，2026-09-19 用户拍板 + Mac 4/4 复验）。盘面旋转 R = 90 − 180v，
   ΔR = Δcss ⟹ **Δv = −Δcss/180** —— 手指顺时针，值必然减小。要反转只有两条路：
   a) 盘面逆着手势转 —— 直接违背刚拍板的"跟手"直觉；b) 数字排列镜像成 −3 上 / +3 下 ——
   偏离原型 `DIALS.ev`（两盘"0 在正下、满值在正上"的镜像约定），原型与参考图都要跟着改。
2. **物理隐喻本来就是"拖盘读数"，不是"旋钮"**：这是老式收音机调谐盘那类交互——盘跟手转、
   读数看刻度排列；"顺时针=增大"是**旋钮**（固定刻度、转指针）的直觉。两个隐喻都真实存在，
   本项目从 B3a 起选的就是前者（数字刻在盘上、盘跟手）。
3. **B3b 的自洽推论**：对焦盘 mirror=false（数字排列天然镜像），同一模型下
   **对焦盘"顺时针滑 = 值增大"（0 近 → 1 远）** —— 两盘"值方向"相反、但"盘永远跟手"
   完全一致。这是镜像布局的物理自洽：两颗转盘上刻的数字方向相反，拖起来读数方向当然相反。
   若强行统一"顺时针=增大"，必有一盘盘面逆手转 —— 那才是真正反直觉的。
4. 成本收益：改镜像要动原型 + 数字排列 + 重新复验，收益只是"方向跟另一类隐喻一致"；
   EV 是双向对称参数（0 居中），试错成本极低（拖 1 秒就知道方向），不值得动。

（若你仍要改：改动点 = `DialConfig` 里数字排列翻转 + 原型 `DIALS.ev` 同步 + 复验，
工程量约半天，写在档案里备查。）

## 五、拍板点（2 个）

1. **虚拟设备上的呈现**（建议 A）：
   - **A. toast 不开盘**（建议）：点「对焦」→ toast「手动对焦：当前多摄虚拟设备不支持 · 切物理镜头后开放」
     —— 与 B2 手动开关同款诚实边界，零虚假交互；盘 UI 代码随本件全落地，架构改造后自动点亮。
   - B. 开盘可看、拖动无效（每次拖动 toast 说明）—— 形态可视但每次拖动都在"演示一个无效交互"。
2. **第 2 格图标行为切换**：现在点「对焦」= focusHintTapped（提示"点取景器任意位置对焦"）。
   B3b 改为按能力分派：不支持 → 上述 toast（**保留一句**"点取景器任意位置仍可自动对焦"，
   因为点按对焦现在就有用）；支持 → 开盘。物理架构落地后 toast 分支自然消亡。

## 六、自检计划（守卫全部要做变异测试）

| # | 守卫 | 放哪 |
|---|---|---|
| 1 | **盘底唯一来源**：`FocusDialView` 不存在——两盘都渲染自 `DialView`；全仓 `RadialGradient` 盘底只出现在 `DialView.swift` 一处（负向断言，**剥注释后匹配**） | 新增或第 12 组⑰ |
| 2 | **三条约定在共享组件内**：`dragAnchor`（相对位移）/ `13 * scale`（字号）/ 0.45 盘底透明度都在 `DialView.swift` 且只有一份 | 第 12 组⑰ 扩 |
| 3 | **mirror 分派**：对焦调用传 `mirror: false` / 指针 90° / 值域 0…1 / count 51；EV 传 `mirror: true` / 270° / ±3 / count 61 | 新增 |
| 4 | **对焦写硬件唯一入口**：`setFocusModeLocked` 只在 `CaptureDeviceConfigurator`（铁律 2 延伸，现第 12 组⑤只查曝光/WB，补对焦） | 第 12 组⑤ 扩 |
| 5 | **入口能力分派**：VM 对焦入口方法体内含 `isLockingFocus…Supported`（经 session 支持位）+ toast + DebugLog（留痕） | 第 12 组 |
| 6 | **回读三件套**：`manualFocus(of:)` 存在（configurator）+ session `@Published manualFocus` + VM 派生（第 12 组② 同款三段） | 第 12 组② 扩 |
| 7 | **闸门同构**：`focusEditingChanged` 含 `lastPushedLensPosition` + 回写守卫 | 第 8 组同款 |
| 8 | **开关行几何**：读 Theme 令牌复算"开关行底沿距屏底 ≥ 0"（393/375 双机型） | 第 12 组⑰ 同款 |
| 9 | **点按对焦分流**：手动锁定期间点取景器不调"对焦+测光合并"路径（拍板 ③ 的可执行化） | 第 12 组 |
| 10 | 变异测试：M-对焦 mirror 传错 / M-盘底透明度改回 0.78 / M-拖拽模型换回绝对定位 / M-开关行越屏 —— 4 突变全 FAIL + 对照组绿 | 一次性脚本 |

## 七、实施顺序（拍板后）

1. **重构**：`ExposureDialView.swift` → `DialView.swift`（DialConfig 化，EV 行为零变化）——
   自检⑰断言对象同步 + 变异脚本锚点同步，先单独提交（可独立回滚）
2. **对焦盘接入**：`DialConfig.focus` + CameraView 调用 + VM 数据链（回读三件套 + 闸门）
   + 入口能力分派 + 点按对焦分流 —— 第二笔提交
3. Mac 复验：EV 盘回归 4 项 + （架构改造后）对焦盘全量
4. 净可见账/宽度预算：对焦盘同为模态不占底栈行，无新账；开关行几何进⑰

# CONTEXT_HOT · 对话续接摘要

> 更新：2026-09-22 上午（**批六 ② 收尾完成**：两轮真机否决后改走**候选 2「冷探测快路径」→ 复验通过**；
> 守卫 **o1/o3/o4/o5/o7/o8** + **变异 17/17**；`check_presets` **全过**（CB 已同步原型、第 6 组收紧））
> —— **细节全部在 `docs/23`**（尤其第八节：真机已答 / 三处口径修正 / 复验跑法坑 / 观察项裁决）：
> **① 刻度条复刻飓风（方案 A）**：档距三条统一 40pt / 单档 14×1.33pt 每档同高（**五档层级整组退场**）/
> 选中档 22pt+加宽 2pt+绿当指示器（三角指针+竖线退场、气泡保留）/ **删档间细分线** / **每档全标签**（`labelAt` 退场）；
> 快门 15→**42 档**（补 1/3 档 + 影院值 `1/96`·`1/120`，**删 `1/12000`**）；ISO 末档 = **设备实读 maxISO（12096）**。
> 🔴 遗留：白平衡 `10000K` 估算余 0.31pt → Mac 实测一眼（**未答**）。
> **② 冷启动 4.27s → 已收尾**：**删除设备级预热全家**（私有会话必然抢直播设备 → raw3 必打断；两轮否决，
> 启动黑屏 13~15s）→ 改 **Live 优先序**（`420v` 优先 + 面积降序；真机全表反推命中位次 **12→1**；
> 探测 **3.74s → 0.39s / −89.6%**、端到端 4.32s → **1.01s**）+ **探测闸门**（打断直接置闩 → 循环收手 →
> 兜底格式且不写缓存）+ **观察者提前到 `startInternal()` 顶部**（补启动窗口缺口）+ **资源账修正**
> （预期以 `audioInput != nil` 为事实源 + 配置窗口抑制 + 连续 2 次去抖）。**③ 点按重放 / ④ 资源埋点：代码完、待专项复验。**
> **剩**：候选 2 收尾 ✅（**c 口径拍死 ≤1.1s** / **d3 阈值拍死 = 单次 applyFormat 0.3~1.0s**）+ **⑤ 6-① 待用户线索**；**①③④ 专项 ✅ 已验（`ee1953c`：① 红线未命中 / ③ 过 / ④ 306 次连切零告警）**
> （② 已两轮 ❌ → 拍板**候选 2「快路径」**（`f05f9ad` + WB 修 `a898a21`）→
> **候选 2 已真机复验：快路径 ✓ · b/e/f/g ✓ · c **达**（0.97~1.01s；口径已修正 ≤1.1s，**已拍死**）· d ✅ 已验（`d276aa3` 注入设施：
> d1/d1-b/d2/d4/d5 全过 + d3 过附阈值建议）· **第 3 件（观察者提前注册 `d3c3cd7`）✅ 已验（d6 / d6-b 过）**）**，
> 见下「候选 2 复验记录」「判据 d 复验」「第 3 件复验」；
> 白平衡 `10000K` 已实测：**净余量 4pt（红线未命中）**）。
> **⑰ ISO/快门联动已结案（2026-09-22 拍板 A「不做」）**：Mac SDK 取证钉死「无单轴 setter / `exposureMode` 无半自动档」
> → 现状 = 系统上限；SDK 原文留档（虚拟多摄不支持 `.custom` / 换镜头可能清 duration·ISO / EV 第三轴）见 ⑰。
> ~~CB 同步原型 4 项~~ ✅ **已完成**（`6e7ac66`，`check_presets` 第 6 组已收紧为逐条全等、全绿）。
> 批五 3 条已 push（`64e1d0b`）**Mac 复验已过（见下方 Mac 侧同步）**，其"刻度更细"验收口径**已被批六作废**。
> **【Mac 侧同步（远端 f5cfda7~94aa359 落盘）】**批五已过 Mac 复验：3 条全过——静默路径**零退转场**（首次 4.27s =
> 每设备冷探测 / 缓存命中 **0.45~0.48s**·虚拟 0.99~1.03s）；`hair` 五档（WB 主档间 **8 条**）；切模式 WB 保持
> （复查 ×4「白平衡=手动」）；补测录制中跨镜头拒绝 ✓。⚠️ 批五"hair 五档"验收**已被批六方案 A 作废**。
> 详见 Mac 快照段「批五复验记录 + 用户 4 问题」与「批四复验记录」。
> **【Mac 批六复验 · ② ❌ 不过（2026-09-21 夜 · 真机）】**预热**从未执行** → 用户路径仍付 **4.29s** 冷探测（原始 bug 完好）。
> 证据（**全新安装清空格式缓存** + `startInternal` 跑满两次的子会话，共 109 行）：**预热相关日志 0 行**
> （连「预热跳过：N 颗镜头都在缓存里」都没有）；`startRunning()` ×2；`会话就绪·焦段已对齐` ×2；
> 21:40:53 `⚠️ 直播会话被打断：其它(1)`（= `videoDeviceNotAvailableInBackground`，**良性**）；
> 用户开刻度条 → 21:41:04 `静默换形态耗时 **4.29s**`（= 每设备首次冷探测，正是 ② 要消掉的那 4.27s）。
> **根因（代码级钉死）**：`startPrewarmIfNeeded()` 判据含 `state == .running`，但调用点在 `startInternal()`
> **1682 行**、而 `state = .running` 在 **1697 行**（同函数、且 `publish` 是**异步回主线程**）→ **每次都在写
> `.running` 之前读**；冷启动又会有一次"后台/不活跃→前台"往返（本次留下 `其它(1)` 痕迹）→ `stopInternal`
> 把 state 打回 `.idle` → 第二次调用同样读到旧值 ⇒ **两次都静默早退**（判据在当前调用点不可达）。
> ⚠️ 打断闸门本身干净（只在真有预热时才 `prewarmAbandoned = true`，日志亦然）。
> **建议修法（待 WB 定夺）**：① 判据换**同步标志**（`configurationSucceeded && session.isRunning`）——
> 单这一行就能让**第一次**调用（会话刚 running、首帧还没来）开始预热 = 设计意图；② 或把调用挪到
> `publish { state = .running }` 之后；③ 打断闸门**排除良性 reason**（`videoDeviceNotAvailableInBackground`），
> 否则冷启动那次良性后台打断会把整场预热废掉；④ 补 reason 映射（raw 1/4/6 现打印为「其它(N)」，排查时看不出是什么）。
> **①③④ 未验**（按用户口径：② 不过则停，后面三条不验）；**CB 同步原型 4 项 ✅**（`6e7ac66`：ISO 12096 /
> 快门 42 档 / slot 40 / `labelAt` 退场改全标签 + 单档刻度 CSS 14×1.33；`isSwiftAheadOfPrototype` 改回 `false`
> → `check_presets` 第 6 组收紧为逐条全等、现全绿）。
>
> **【Mac 批六 ② 重验（修复版 `0cfe5fb` / 修复 `55eab6f`）· 2026-09-21 深夜 · 真机】❌ 不过（判据 b 红灯）**
> - **前置**：远端真值 `0cfe5fb` ✓；两个枚举符号在 iOS 26 SDK **都在**（`AVCaptureSession.h:88` raw 1 / `:91` raw 4）
>   → 白名单 case 保留 ✓；`check_swift` / `check_presets` 全过、编译 0 error。
> - **判据 a（日志齐）— 部分过**：`预热跳过：开关已关`（Run A）×2 = 两次 `startInternal` **各留一行** ✓（P0-3 生效）；
>   Run B：`预热决策：开始预热，待探 3 颗（Back Ultra Wide / Back / Back Telephoto）` → `预热探到格式` ×3 →
>   `预热结束：耗时 **11.38s**`（**远超"应 4~5s"**）；⚠️ 测试手法：用启动参数
>   `-lumen.camera.formatProbeCache COLD`（arguments 域优先级最高 → `dictionary(forKey:)` 读到 nil → 回落 `[:]`）
>   模拟空缓存，**不用卸载重装**（重装会连带重置"开发者信任 + 相机授权"，代价高）。
> - **判据 b（不得打断/不得黑屏）— ❌ 不过（红线原样命中）**：
>   `⚠️ 直播会话被打断：视频设备被另一个客户端占用（raw 3）` **×3**（07.235 / 07.797 / 12.205 —— 正好对应三颗设备挂载）；
>   每次打断持续到该颗探测完成（≈0.3s / 4s / 5s）→ 预热窗口内预览被系统打断 ≈9~10s（用户必看得见）；
>   **且「已拆除预热」从未出现** → 闸门**结构性失效**（见根因②）。
> - **判据 c（4.29s → 0.3~0.8s）**：Run A 对照组 **4.41s** ✓（冷探测复现，用户当场反馈"太长"）；
>   Run B 后缓存已写热 → 待用户再开一次条测量（本轮数据见下）。
> - **根因①（方案级）**：预热私有会话挂的 constituent **正是直播会话的设备** → 系统必然打断直播（3/3 颗命中，
>   doc/23 §2.7 的风险不是"可能"是"必然"）→ 当前方案在真机上是**负收益**：用户路径的 4.29s 换来
>   **启动即被打断 ~11s、预览冻结**，与用户口径「让用户感受不到 / 不得 raw 3 / 不得黑屏」直接冲突。
> - **根因②（闸门自锁）**：放弃动作走 `prewarmQueue.async`，排在**正在跑**的预热循环之后；循环收尾已先置
>   `isPrewarming=false` / `prewarmSession=nil`（:1961-1962）→ 放弃块醒来时 guard 失败 → **静默返回**
>   ⇒ **P0-4/P0-5 兜底在真机上无效**（"已拆除预热"永远不会出现）。修法方向：放弃动作**不能排在预热队列后面**
>   （改成通知线程直接置原子标志 + 循环内每个候选前检查；`AVCaptureSession.stopRunning` 本身线程安全），
>   或用可取消的 `DispatchWorkItem`。
> - **建议方向（待拍板）**：① 预热窗口挪到**首帧之前**（用户还看不到画面 → 打断无感）；② 不预换会话，
>   改让「41 候选冷探测」本身变快（缓存未命中时先走 `activeFormat` 级试探，而不是逐个 `applyFormat` 真跑）；
>   ③ 只预热最常用一颗并把窗口压到 ~4s；④ 闸门本身按根因②修好（无论选哪条都要修）。
> - **①③④ 未验**（按用户口径：② 过之后才验）。
> - 🔴 **新 bug（2026-09-21 深夜 · 用户报告）：模式切换卡死（疑似 ② 引入）**——"按模式条从左到右切，切到 log 卡顿、
>   到视频完全黑屏、必须清后台；触感卡死期间消失"。**现场已在会话 2 抓到**（22:24:11→ 22:24:59）：
>   ① `22:24:11.818` 用户切「实况」→ `:11.820` **预热又开跑一趟 11.39s**（缓存 key 含模式 → 切模式=新 key 未命中
>   → **预热会在用户操作中途重新开张**）；② 该窗口内用户连切 Log 实况/视频 → **raw 3 打断 ×4**、`切模式耗时 0.00s/0.01s`
>   （异常）；③ **「让路」0 行** —— 让路信号只由**焦段换设备**置位，**不覆盖切模式** → 无人退让；
>   ④ 5s 周期账在 `22:24:59.164` 断流（之后静默至用户清后台）。**判定 = 疑似 ② 引入**（结构上不可能更早：
>   ② 前预热从不运行，15 会话 0 行预热日志）。留档：`reference/batch6-runB-2026-09-21/`（`模式切换卡死现场-会话2尾部.log`
>   原样 256 行 + `现场摘要.md`「追加①：模式切换卡死现场」含时间线/四点回答/机制/复现协议）。
> - 📈 **会话 3 全范围切换分析（22:25:00→22:31:56 · 63 次切换 · 无预热）**：切换全成功（**min 0.13 / 中位 0.33 / p90 0.54 / max 0.77s**，无一次 ≥1s）；
>   **in 活 2（23/21）/ out 活 1（64/63）全程配平**（无"挂了没拆"）；5s 周期账不断流（**本场没卡死**）；
>   内存 **26→42.6MB 不回落**（峰值 148.4MB 瞬态 @22:25:06，视频链路建立期）→ 有累积形态。
>   ⇒ **"卡死"目前只在有预热在场时出现过**（会话 2）；"掉帧"需 WB 从代码侧看（逐帧率不进日志，最可能是每次切模式重配 outputs 0.13~0.77s 在高频下叠加）。
>   ⚠️ **勘误**：先前报过的两个"卡顿尖峰（#41 7.80s / #45 4.99s）"是**配对错误假象**（已核原始窗口：那两段健康）—— 可靠口径只有 app 自打的 `切模式耗时` + ④ 计数/内存账。
>   留档：`会话3-高频连切-原始.log`（1364 行）+ 摘要「追加③」。
> - 🔴🔴 **启动黑屏 = 预热窗口（2026-09-21 22:44/22:45 客观实测 A/B，用户报"刚启动时取景器内全黑"）**：
>   量取景器区灰度 → **预热开：黑 ≈13~15s**（覆盖预热 11.17s 全段+~1.5s，期间 `预览层已绑定会话` 但画面全黑）；
>   **预热关：只黑 ≈4~5s**（启动首帧）⇒ **预热带来额外 ≈9s 黑屏**。代表图 3 张已留档。
>   ⚠️ 顺带暴露**既有项**：无预热时启动首帧本身也要黑 **4~5s**（与 ② 无关，但"让用户感受不到"也要一起看）。
> - 📊 **P-b：`device.formats` 全表已采集**（临时诊断 `FormatTableDump.swift` + 控制器一行调用，**取到数据即删**；
>   原始数据 `device-formats-全表.log` 386 行）：超广角 **61 个格式 / 命中 #59**、Back **67 / #65**、Tele **61 / #59**
>   —— **三颗命中档都在元数据排序尾部（倒数第 3）**，且三颗命中档 `dims|subType|fps` 完全一致
>   （`4032x3024|875704438|1-30`，420v；其后的 `875704422`（420f）**未命中** → Live 能力与**像素格式**绑定）。
> - 📁 **现场留档**：`reference/batch6-runB-2026-09-21/`（原始日志 `b6-runB.log` + 「现场摘要.md」——
>   raw 3 三次时间点 / `预热结束：11.38s` / 三颗挂载顺序与逐颗耗时 / **候选 2 的三条读数**：
>   ① Live 命中格式**永远是最后一个候选**（提前到第 1 位 → Back 3.44s→≈0.3s、三颗合计 11.38s→≈0.9s）；
>   ② 三颗胜出格式描述子完全相同（可用兄弟设备预测首试）；③ 单次 `applyFormat` 代价非常数
>   （超广角 ≈1ms/次 vs Back·Tele ≈0.3s/次，差 ~300 倍）。启动参数模拟冷缓存的手法也记在该摘要里。
> **【Mac 批六 · ② 替代「候选 2 快路径」真机复验（`f05f9ad` + WB 修 `a898a21`）· 2026-09-22 凌晨】**
> - ✅ **快路径生效**：物理 24mm 冷探测 `候选 41 个，applyFormat **1 次**，耗时 **0.39s**（Live 优先序）`
>   → `换设备完成（静默·无转场）→ 物理单摄（24 mm）…耗时 **1.01s**`。
>   A/B 对照组（`-lumen.camera.formatProbe.fastpath.disabled YES` + `formatProbeCache COLD`，同 41 候选）：
>   `applyFormat **12 次**，耗时 **3.74s**` → `换设备完成…耗时 **4.32s**` ⇒ **探测 −3.35s（−89.6%）·
>   端到端 −3.31s（−76.6%）**；且控制组**复现**了原始 4.29~4.41s bug ✓（对照有效）。
> - 🟡 **c 达（口径修正 · 已拍死）**：静默换形态 **0.97s / 1.01s**（两次独立会话）vs 原目标 ≤0.9s（差 0.07~0.11s）
>   探测只占 0.39s，**剩 ~0.6s 在会话装配段**；相对 4.41s 基线 **−78%**。
> - ✅ **b / e**（本场）：**raw3 = 0 / 0 配额告警**；资源账 5s 不断流（input 活 1·累计增2删1、output 活 1·增1删0、
>   内存 25.8~26.8MB 平稳）；`换设备后新设备档位复查` 正常（新设备显式降级 → 曝光/白平衡/对焦=自动）。
> - ✅ **f / g**（同晚早前实测）：每次切模式震动 ✓；焦段 13mm **0.96s** / 120mm **0.99s** / 35mm **0.50s** /
>   13mm 二次 **0.56s**（≤1.5s ✓）；无 0.00s 异常切换。
> - ⚠️ **d 未验**：冷探测「另一个客户端」打断**前台不可触发**（主摄常开 → 无抢占）→ 建议加 debug 开关
>   （raw3 注入）再验"让路 / 收敛"。
> - 📌 **复验手法（留档 · 踩坑）**：A/B 靠启动参数（`fastpath.disabled YES` + `formatProbeCache COLD`），
>   **武装后不可再手点 App 图标** —— 无参启动会顶掉参数（本轮踩过一次：00:01:35 手点 → 探测仍走快路径 1.01s）。
> - 📁 证据：`reference/batch6-candidate2-2026-09-22/`（`ab-slowpath-4.32s.log` / `ab-armed-fastpath-off.log`
>   / `handrelaunch-fastpath-1.01s.log`）。
> **【Mac 批六 · 判据 d 复验（WB 注入设施 `d276aa3`）· 2026-09-22 凌晨 · 真机】✅ d1 / d1-b / d2 / d4 / d5 全过 · d3 过（附阈值建议）**
> - **跑法必须改两处**（WB 脚本原样跑会失败，已实测）：
>   ① `formatProbeCache COLD` 会让注入**落在启动探测**上 —— 而观察者 `observeInterruptionsIfNeeded()`
>   在 `startRunning` 前才注册（`:1705`），启动探测发生在**会话配置期间** ⇒ 通知**无人收、静默丢弃**
>   （实测：注入行有、②④ 全无）。
>   ② 快路径 ON 时照片模式**第 1 个候选即胜出 → 直接 break**，而闸门检查在每圈**开头** ⇒ 注入**结构性无效**；
>   必须加 `-lumen.camera.formatProbe.fastpath.disabled YES`，才有第二圈经过闸门。
>   ✅ **正确跑法**：`COLD 空跑一趟`（抹键 → 24mm 变冷）→ `暖启动 + 注入参数 + 关快路径`（启动命中缓存、
>   **不消耗注入**）→ **点「対焦」切物理 24mm**（冷键探测 = 注入生效点）。
>   ⚠️ 触发点只有 **启动配置** 与 **换设备（物理镜头切换）**：切模式不走、焦段内变焦（13mm 曾是虚拟设备内
>   `1.00×`）也不探测 —— 所以「対焦」是唯一可靠触发点。
> - **d1 ✅**（raw 3 同步）：`注入 → ⚠️ 被打断（raw 3） → 冷探测：候选 41 个 / applyFormat **1 次** / 0.26s
>   → ⚠️ 已在第 **1** 次候选处收手，采用兜底格式（不写缓存）`；该镜头**无**「已缓存并落盘」✓
>   （`换设备完成 0.85s`；兜底代价 = `Live Photo 能力=false`）。
> - **d1-b ✅**：紧接无参重启 + 再切同一镜头 → 仍 `冷探测：候选 41 个，applyFormat 1 次，耗时 0.39s`
>   （**不是**命中缓存）⇒ **没留脏缓存** ✓；且该次干净探测 `Live=true` **自愈** ✓。
> - **d2 ✅**（raw 1 良性）：`打断原因属良性（进后台）→ 不触发探测闸门` → 探测继续
>   （`applyFormat 1 次 / 0.45s`）→ `已缓存并落盘`、**无收手** ✓（Live 能力保住 ✓）。
> - **d3 ✅（附阈值建议）**：d1 同步 ②→④ = **0.000s** ✓；d5 异步 = **0.703s**（**超**脚本「≈0.3s」估计 ✗）——
>   结构正确（闩在**在飞候选跑完后的下一个边界**被收手），真实上界 = **单次 applyFormat 代价（0.3~1.0s，非常数）**
>   → 建议阈值改成"≤ 单次 applyFormat 代价"。
> - **d4 ✅**（真 raw 1）：探测窗口内切后台 → `⚠️ 直播会话被打断：后台不可用（良性）（raw 1）` +
>   `不触发探测闸门` + 探测跑完（`applyFormat 12 次 / 0.80s`）+ `已缓存并落盘`、无收手 ✓
>   （时间戳重叠：打断 19.857 ∈ 探测窗口 [19.07, 19.872]）。
> - **d5 ✅**（async YES）：`注入（后台线程延迟 50ms）` → +52ms `⚠️ 被打断` → `applyFormat **2 次** / 1.02s`
>   → `已在第 **2** 次候选处收手（不写缓存）` ⇒ **异线程置闩下一边界即时可见** ✓。
> - 🔴 **产品侧观察（建议 WB 看）**：**启动探测窗口内观察者尚未注册** ⇒ 若真发生 raw3（别的客户端抢设备），
>   启动探测**不会被闸门收手**（会继续 churn ~12 次）。窗口小（startRunning 之前、主摄未跑）但结构存在 →
>   建议把 `observeInterruptionsIfNeeded()` 提到**配置之前**（或在配置入口注册）。
> - 📌 收尾：`DebugProbeInterrupt.swift` + 控制器那 1 行调用**尚在**（验收已过 → 可删；`o8` 兜"删了声明引用还在"）。
> - 📁 证据：`reference/batch6-d-facility-2026-09-22/`（6 份：误跑 COLD / 修正跑法收手第1次 / 无脏缓存重探 /
>   良性不收手 / 异步收手第2次 / 真 raw1 探测跑完）。
> **【Mac 批六 · 第 3 件复验（观察者提前注册 `d3c3cd7`）· 2026-09-22 上午 · 真机】✅ 过（d6 + d6-b）**
> - **d6（脚本原样：COLD + raw3 + at 1，不加 `fastpath.disabled`）**：`【debug】注入打断通知：raw 3` →
>   `⚠️ 直播会话被打断：视频设备被另一个客户端占用（raw 3）` **← 修前这一行是【没有】的（通知无人收）** ✓
>   → `冷探测：候选 **31** 个（**虚拟多摄**，脚本写的 41 是物理镜头的笔误）/ applyFormat 1 次 / 0.00s` →
>   **无收手 + 有「已缓存并落盘」**（脚本预期的 ④ 未出现）。
>   ⚠️ **原因 = 结构性**：快路径 ON 时第 1 个候选即胜出 → 直接 `break`，而闸门检查在**每圈开头**
>   ⇒ 成功路径不经过检查点（与 d1 首轮同一条机制）——不是修复没生效。
> - **d6-b（补跑：同参数 + `fastpath.disabled YES`）**：注入 → 打断（raw 3）→
>   `冷探测：候选 31 个，applyFormat 1 次` → `⚠️ 已在第 1 次候选处收手，采用兜底格式（不写缓存）`
>   → `会话配置完成，Live Photo 支持=false`（兜底 ✓）→ `startRunning()` ⇒ **启动窗口的闸门真验出来** ✓✓
> - ✅ **启动不变慢**：d6 `日志启动 10:53:03 → 会话就绪 10:53:06.6`；d6-b 就绪 **0.65s**；探测本身 0.00s ✓
> - ✅ **不黑屏（定量）**：d6 截屏取景器区 **均值 54.3 / 标准差 9.0 / 极值 (31,117)** vs 旧黑屏对照
>   `31-b6-cold.png` **0.6 / 0.5 / (0,3)**、锁屏 177.1 ⇒ 有真实画面 ✓
> - 🟠 **观察项（低优先，建议 WB 定）**：打断若落在**最后一个成功候选**上，结果**仍会写缓存**
>   （闸门只在循环开头检查，成功 break 路径不经过）→ 若口径是"被打断期间试出的结果一律不算"，
>   需在成功 break 前补一次闩检查。
> - 📁 证据：`reference/batch6-d6-2026-09-22/`（d6 / d6-b 两份原始日志）。
> **【Mac 批六 · ①③④ 专项复验 · 2026-09-22 中午 · 真机（`ee1953c`）】✅ ① 过（红线未命中）· ③ 过 · ④ 过；⑤ 本轮未复现（待用户线索）**
> 前置：`ee1953c` ✓（设施已删 ✓ / 观察者仍在 `:1605` ✓）· `check_swift` / `check_presets` 全过 · 编译 0 error · 装机 ✓
> **① 刻度条 `10000K` 红线 —— 未命中 ✅**（截图 1206×2622 @3x 像素实测）：
> - pitch **120px = 40.0pt** ✓（刻度中心 266/386/506）；普通档刻度 **41px = 13.7pt**、选中档 **65px = 21.7pt 绿 (104,205,103)** ✓；
>   标签底 ↔ 刻度 ≈15px 级 ✓（令牌换算 13pt）—— **批六单档口径实测全中** ✓
> - **`10000K` 实测宽 117px = 39.0pt**（WB 估算 39.69pt，偏大 0.7pt）；左邻 `9900K`（5 字符）99px = 33.0pt；
>   **净间距 13px = 4.33pt > 0 ⇒ 不碰** ✓（公式 120−(99+117)/2 = 12px ✓ 吻合）
> - 真正的约束是 **`40 − (w左+w右)/2`**（不是"标签宽 ≤ 40pt"）；WB 表里 6 字符档**只有 `10000K`**，其邻档必是 5 字符 ⇒ 最坏余量 ≈ **4pt** ✓
> - **状态无关性**：标签按 `index×slot` 定位、`if step.showsLabel` 无状态分支 ⇒ 四个字距（正常 / -1 / +1 / 最高）几何恒等 ✓；
>   两端 40pt 渐隐 mask 不触及 `10000K`（x 448–564，远离边缘 ✓）
> - ⚠️ **自纠记录**：我一度误判"刻度线没渲染" ✗ —— **采样反证**：选中档 x=506 y2032–2092 绿线、普通档 y2056–2092 灰线
>   ⇒ 刻度线**在**（1.33pt 细，叠亮取景画面时不显眼）· 记此以免后人重踩。
> **③ 点按重放 ✅ 过（4 次点按 + 切模式 / 切焦段全链）**：
> - `点按对焦后按意图重放手动曝光（ISO 1000 / 1/80s）` ×4 + `点按对焦后档位：曝光=手动 / 白平衡=自动 / 意图=曝光手动·白平衡自动` ⇒ **点按不顶掉手动** ✓
> - 切模式：`切模式：手动曝光已按快照重放` + `复查（实况/视频）：曝光=手动` ✓；换设备：`搬运：手动曝光已重放` + `复查：曝光=手动` ✓
>   （含 `新设备带上一轮手动档残留 → 已归一到自动档再重放` 分支 ✓）
> - 对焦手动支线：`点按仅测光（不解除手动档）`（盘开着）/ `解除手动档并重新对焦`（收起后）✓ 两分支均对；`手动档下 EV 已拦下` ✓
> **④ 埋点 ✅ 过（306 次连切）**：
> - **306 次**切换 / 115s：**0 次「⚠️ 资源超出预期」** ✓；**input 增/删 40/39 → 活 1** ✓ · **output 307/306 → 活 1** ✓
>   （307 = 306+1 逐笔吻合）；资源账 153 行**最大间隔 5.0s**（不断流）✓；**0 次打断** ✓；
>   耗时 min 0.23 / 中位 **0.30** / p90 0.51 / max **0.59s**（**0 次 ≥1s**）✓
> - 内存 24.3 → 峰值 30.9 → **收敛 29.8~30.9**（无单调增长 ✓）；`档位复查 306 次 / 0 补写` ✓
> - ⚠️ 本轮曝光为**自动态**（用户未设手动）⇒ 306 次复查全 `曝光=自动` ✓ 属预期，**不能**当 ⑤ 的证据。
> **⑤ 6-①「手动档又回自动」—— 本轮未复现 ⚠️**：判定表四栏**全阴性**（无 raw≠1 打断 0 行 / 资源账 max 5.0s 无断流 /
> 复查 0 补写 / 无 `点按对焦后档位：曝光=自动`）；**待用户补**：① 哪个参数回自动（曝光/白平衡/对焦/EV）② 触发动作
> ③ 多久后看到 ⇒ 拿到线索即按判定表抓一轮（**先定性、别先写码**）。
> - 📁 证据：`reference/batch6-special-2026-09-22/`（③ step3.log / ④ step4.log / ① 标签行放大图 + 原始截图）。

> 新会话开场：**整份贴 `docs/00-启动包.md`**（本文件按需查，不再整份贴）。
> 配套文档：`docs/01~04`（方案与交接）、`README.md`（运行与验收）、
> `docs/08`（Swift 同步任务书 · **当前主任务书**）、`docs/00-启动包.md`（**新会话唯一入口**）。

---

## 〇、状态速览（2026-09-18 · 新会话先读这段）

> 本文件**上半段（〇）是 Swift 端进度快照（WB 维护）**；下半段（一~九）是 **CB 的原型迭代历史，只读参考**，不必改写。
> Swift 端**当前主任务书是 `docs/08`**，新会话**启动包是 `docs/00-启动包.md`**（开场**只贴这一份**，本文件按需查），两者配套读。

### 进度基线
- ✅ **B3b 对焦圆盘已交付（2026-09-19 深夜）→ Mac 复验通过（2026-09-20，③ 经最小修复验 ✅）→
  ✅ 正源修完成（2026-09-20，WB）→ **Mac 复验 ✅**（28 次点按全走对焦点、零仅测光、零 ERR/WRN）**
  （③ 回归根因：B3b「回读三件套」把硬件 `.locked` 当用户意图 → 死锁；**正源修已做**：
  `manualFocus` 改**用户意图态**——写入点只在 `setManualFocus` / `setAutoFocusMode` 两个入口、
  `publishManualState` 禁写（⑱ 同源污染守卫）、`manualFocus(of:)` 已删（含 Mac 最小修能力门）、
  VM `focusEditingChanged` 的 isFocusAuto 拦截已删（第一次拖动=进入手动的意图）；
  通用教训：**硬件状态回读 ≠ 用户意图**。详见 `docs/19` 头部正源修小节）：两盘共用 **DialView**
  （`ExposureDialView.swift` 泛化重构——Mac 架构指示"抽共享盘底不要抄第二份"落地；
  Theme 令牌 evDial*→dial*，新增 dialAutoSwitch* 五个 + dialSwitchOn/Off 色分派）。
  **能力分派（拍板 ①·A）**：虚拟多摄点「对焦」= toast「手动对焦：当前多摄虚拟设备不支持 ·
  点取景器任意位置仍可自动对焦」不开盘（B2 手动开关同款诚实边界），物理架构后自动点亮。
  **对焦回读三件套补齐**（此前对焦没有）：`manualFocus(of:)` → session `manualFocus` /
  `currentLensPosition`（自动档也有效，自动对焦中读数自己走）/ `isManualFocusSupported`
  → VM 派生 + `focusEditingChanged` 闸门（docs/14 同构）+ 「自动对焦」开关（关=当前读数
  锁定，原型同款；不做模拟动画）。**拍板 ③ 落地**：手动锁定期间点取景器**只测光不动焦**
  （`setExposurePointOnly`：不碰 focus POI/mode、不打回手动曝光）。两盘互斥收口 5 样。
  自检新增 **⑱ 组**（9 断言：盘底 RadialGradient 全仓唯一（剥注释）/ 三约定唯一份 /
  两盘工厂分派 / setFocusModeLocked 唯一入口 / 能力分派 / 回读三件套 / 闸门 / 点按分流 /
  开关行几何余 22pt）+ **b3b 变异 4/4**（对照组绿）；b3a 变异 6/6 重构回归全过。
  **方向语义记档**：两盘"盘跟手"一致、值方向相反（EV 顺时针=减小 / 对焦顺时针=增大）——
  镜像布局的物理自洽，非 bug（用户拍板维持）。详见 `docs/19`。
- ✅ **B3 三件拍板（2026-09-19 晚）+ B3a EV 圆盘已交付 —— Mac 真机复验通过**（含 3 处 [mac-fix]，详见 Mac 快照段）：
  拍板：①物理会话**按需**（自动态保留虚拟多摄平滑变焦）②手动档仍可切焦段（跨切换点参数搬运，
  对焦落回连续 AF）③手动对焦锁定期间点按**只测光不动焦**。
  B3a 交付：EV 圆盘**模态**浮层（±3 EV / 0.1 步进 61 档 / 点数值框归零 / 指针钉 9 点"刻度动指针不动" /
  圆心钉「曝光补偿」格、右半出屏）——**动手前先读了原型真源，两条初稿假设被推翻**：
  圆盘不是同槽互斥行而是模态（打开收**整条底栈**）；**参数排（EV 滑条面板）随原型改版退场**
  （原型 `index.html:2376` 明文 → 删 `ExposurePanel.swift` + `Float.evUIRange`，EV 范围 ±2→±3）。
  拖动值**复用 `docs/14` 编辑态闸门**（`evDialValueChanged` → `exposureEditingChanged`，不另起炉灶）；
  toast 在圆盘层内抬到 38% 高（原位被盘盖住，原型 `.dial-on .toast` 同款）。
  自检：[8]/[12]⑥ 改写 + **⑰ 新组**（闸门/归零/点别处收起/几何账读 Theme 复算）；
  变异测试 6/6 + 对照组全绿（守卫编写当场自纠两个 MEMORY 老坑复发：负向判据命中注释 → 剥注释；
  "默认 false"被互斥赋值行满足 → 声明行锚定）。`ParameterSlider.swift` 保留（暂无调用点的通用组件）。
  详见 `docs/18` 第八节（含 Mac 验收口径 8.4）。
  **Mac 复验与 3 处 [mac-fix]（2026-09-19 晚）**：① `bf56968` 编译错 —— `Path(tickPath(...))`
  （`SwiftUICore.Path` 无 `init(Path)`，`tickPath` 已返回 Path 可直接 `.stroke`）；② `b0fd439` **拖不动**
  （`.gesture` 挂 `.position` 之后坐标空间错位，值钉 −3.0）+ 盘底半透明（0.78/0.84/0.88 → 0.45/0.50/0.55）；
  ③ `0d8d00a` **数字"排序紊乱"**（同源坑：`.rotationEffect` 也必须挂 `.position` 之前）。
  7 颗数字极坐标实测全对（详见 Mac 快照段）。
  **docs/18 已按 Mac 纸面预检修订 7+3 条**（回切策略 2.2b / 前置 2.3b / 跨模式 2.7 / EV 搬运与对焦
  显式降级 2.4 / 搬运顺序 commit→applyFormat→搬运 2.3 / 物理 zoom 表 2.5 / 转场顺序触发 3.1 /
  糊的可能是黑 3.2 / 自检补 #8-#10）。
- ✅ **白平衡手动开关 7 连崩已修 + B2 手动档能力置灰（`b1bce6b`）—— Mac 真机复验通过**
  （白平衡手动开关**连点 14 次零崩溃** / 置灰 + 点击 toast 生效 / 冷启动能力探测日志正确 /
  `setManualFocus` 守卫落地；详见 Mac 快照段）：
  ① Mac 报告的根因 = 守卫用 `isWhiteBalanceModeSupported(.locked)`（虚拟多摄上误报 true，
  写计算增益抛 ObjC 异常）；**三处守卫全换成正确探测 API**（白平衡手动档 + 预设路径
  `isLockingWhiteBalanceWithCustomDeviceGainsSupported`、手动对焦
  `isLockingFocusWithCustomLensPositionSupported` —— Mac 同类预警一并处理）。
  ② **架构级发现**（Mac：SDK `AVCaptureDevice.h:538-541`）：虚拟多摄不支持全部手动参数
  （`.custom` 曝光 / 自定义对焦锁定 / 自定义增益锁定）→ **B2 三条刻度条的手动开关按能力探测置灰**
  （灰但仍可点，点了 toast 说明 + warn 留痕；session 新发布 `isManualExposureSupported` /
  `isManualWhiteBalanceSupported` 两个支持位；冷启动日志多一行「手动参数能力：…」）。
  ③ 自检第 12 组新增 **⑯ 虚拟多摄诚实边界**（5 条断言），变异测试 5/5 抓住、对照组全绿。
  ④ **B3 方案已出稿 `docs/18`**（对焦与 EV 圆盘 + 物理镜头架构施工图 + 模糊转场）。
  用户拍板：**架构方向 A（物理设备）+ 手动模式下不锁焦段 + 「模糊→清晰」转场 +
  物理会话按需切换**（自动态保留虚拟设备平滑变焦，2026-09-19 晚）。
  ⚠️ **Mac 纸面预检报 7 处待补/待改**（回切策略 / 搬运顺序 / 物理档位映射表 / EV 搬运 /
  对焦档位语义 / 转场顺序触发 / 自检缺 3 条）—— 详见 Mac 快照段，`docs/18` 修订后复审。
- P1a 照片闭环 / P1b-1 Live Photo / P1b-2 视频录制 ✅（真机验证过）
- P2 数据层（`LumenEdit/Presets/` 6 文件 + `tools/check_presets.js`）✅ 与网页原型逐条一致（14 滤镜 / 10 风格 / 5 场景 / 4 焦段）
- **P2 UI 骨架 2-1 → 2-5b ✅ 全部交付且过 Mac 真机验证**（含 2-5a 红方块修复、冷启动死区修复）
- **A 组 #6 场景/风格条（`58f10e7`）✅ 验证通过**（2026-09-18 用户截图 + 日志）：折叠胶囊「场景 人像 · 风格 柔暖人像」✓；展开两块横滑 ✓；场景胶囊选中 accent 底深字 ✓；选风格后滤镜重置（日志「滤镜已重置为「无」」×2）✓；展开时焦段条消失 ✓；**参数行（「高光 −20…」）完整可见未被 147pt 行底裁切 → 不需要提 148** ✓；快门排风格方块内层三色渐变 ✓。风格卡描边 2px 目测有、像素级未定（场景亮色干扰）。场景互斥待办：曝光补偿面板在展开态仍显示（规格里"收起 EV 圆盘/参数刻度条"未建，现暂不构成问题）
- **A 组 #7 滤镜条（`f817e0f` + 合并 `bd5fab3`）已交付，方案已确认**（2026-09-18 用户逐条确认，
  三处刻意偏离全部接受，见 `docs/10`：面板四角统一 18 / 滑入+淡出过渡 / 手势挂预览层）——
  **✅ 已过 Mac 真机验证（2026-09-18，10 项全过）**：上划灵敏（阈值放宽后生效）/ 14 卡横滑 84×84 /
  白描边选中 + 再点取消回原片 / 呼出时场景·风格收成胶囊 + 焦段条消失 / 两级上划 / ⤢ 放大态隐藏 /
  点按对焦不被吞 / 参数排展开收起与 EV 吸附（−0.0 过零吸附实锤）。像素级测量受明亮场景干扰未做的：
  风格卡描边 2px+发光的精确宽度（目测有）。互斥待办：曝光补偿面板展开态与场景·风格的互斥由
  `exposureCompensationTapped` 静默重置实现（无 toast），**WB 已接两条 UX 反馈待修**：toggleSceneStyle 补 toast/日志、
  曝光补偿展开 toast 说清连带收起
- **A 组 #10 功能面板（⠿，7 格）已交付（`ecfbe74`）✅ 过 Mac 真机验证（2026-09-18 下午，8/8）**：
  模态浮层（盖住底栏、不入底栈）/ 4 列网格 7 格 / 面板高 241.5pt / 7 钮依次浮入（每个错 40ms）/
  **7 图标全渲染无空白**（SF Symbol 静默留白坑未复发）/ 画幅比**真遮幅**（4:3→16:9→1:1 循环，
  日志每档 toast）/ 实况真角标 / HUD 真开关 / 设置 → 页开 + 面板收起 / 冷启动 5 项记得
  （`lumen.camera.fn.*` 落盘实锤）/ 简易模式入口诚实 toast（#12 未交付）。方案 `docs/12`（用户选 A 去掉阶段标记）
- **A 组 #11 视频格式芯片：已交付（`e3f426e`）✅ 过 Mac 真机验证（2026-09-18 下午）—— A 组收官**
  （`Presets/VideoFormatCatalog.swift` + `Camera/UI/FormatChipView.swift`）。要点：芯片**顶替**右上三图标
  （`isRecordingBased`）/ `minWidth` **引用 `topBarSideWidth`(73)**（模式条居中靠两侧等宽）/
  右上角选择器（分辨率 3 + 帧率 4）/ 默认 `4K · 30`（用户拍板：芯片显示的必须是真的）/
  存储胶囊文本与**预留宽度随模式切换**（录制类 `≈ 23h 59m · 999.99 GB`）/
  与功能面板双向互斥 + `dismissTransientPopovers()` 作为"点别处收起"的唯一入口。
  自检新增第 10 组 7 条。B 组才真重设 `activeFormat`
  **验证实锤**：选择器逐项点击 8 次 toast 全留痕 / 选 1080p·30 重开选择器文案一致 /
  **冷启动持久化**（14:12:58 杀 App → 14:13:52 重启 → 选择器 toast `4K · 30` 与关闭前最终选择一致）/
  存储胶囊首屏定宽（启动截图 51.01 GB 无跳变）/ 互斥时间线不重叠 / toast 边界「重设采集格式属 B 组」每次选择都说明
- **底部三问题修复（`ab6e575`）✅ 已过 Mac 真机验证（2026-09-18，10 项全过）**：上划灵敏 / 参数排默认收起 /
  EV 面板开关化（点「曝光补偿」展开收起 ×6 次）/ 竖滑无干扰 / 横滑吸附（−0.0 过零吸附实锤）/
  互斥与下划收净 / 对焦不被吞；净可见从约 43%（**低于 50% 底线**）拉回约 57% ✅
- **第二轮真机反馈的三个修正（2026-09-18 二次提交 `2cd0859`）✅ 已复验通过**（用户实测 + 日志）：
  ★ **挂载点**：起手故意落在滤镜条卡上 → 照样识别（日志「场景·风格已展开（人像 · 糖果粉调）· 滤镜条已收起」）；
  两级上划 0.56s 内连续成立（12:41:13.921 → 14.481）✅
  **提前触发**：滑到一半即响应，不必松手 ✅  **EV 范围**：±2.0 全档位（toast 报 +1.0/+2.0）+ 每档「咔」触感 ✅
  两条 UX 留痕（胶囊/方块、曝光补偿的连带说明）✅
- 🔴→✅ **复验抓到的回归：横拖 EV 误触发下划**（已修，`docs/11` 第五节）：参数排当场收起
  （日志实锤 12:41:13.381 `[ui] toast: 已收起参数排`，紧跟着被迫重新上划）。
  **根因**：手势挂整页 ZStack（`simultaneousGesture`）+ 提前触发 → 横拖 EV 时手指的自然纵向抖动
  （≥16pt）即满足下划条件；而 `exposureEditingChanged(_:)` 虽已存在却**没接进手势闸门**
  （连带发现它此前**吃掉了 `isEditing` 参数**，编辑态从未被记录 —— 闸门无从判断）。
  **修法两道闸门**：① 精确闸门 `!viewModel.isExposureEditing`（EV 拖动期间整页手势禁言）
  ② **方向锁**（一次拖动只在最初 6pt 定一次主轴，之后不改判 —— 不依赖控件上报编辑态，
  横滑滤镜卡条 / 场景胶囊条 / 未来新控件一并兜住）。上划下划共用同一判定路径，两个方向一并堵住。
- 🔴→✅ **EV 条反复跳动 = 绑定回写环（已修，`docs/14`）**：Mac 侧与 WB 两侧独立得出同一结论
  （"否定档位密度论，判定为绑定回写环 —— 滞后比值是环路增益"）。三条查证：
  ① `CameraViewModel.attach():184-187` 把 `session.$exposureBias` 写回滑条值，**无任何守卫**（环路回边）；
  ② `ParameterSlider` 的 thumb/填充/数字**全部**派生自 `value`（不是渲染源不同步，是 value 被外部改写）；
  ③ 拖动期确实没挂起回写。
  **修法**：拖动期 `guard !isExposureEditing` 不回写（松手同步一次靠"先复位编辑态再推硬件"的既有顺序
  自然成立）+ **只接受"与最后推送值一致"的回写**（`lastPushedExposureBias`，丢弃拖动尾部队列里
  积压的旧值，解决"松手后跳一下"；切模式/会话就绪时清空该记录）。
  自检第 8 组 +3 条（回写守卫 / 最后推送值比对 / 滑条渲染源唯一），现 **13 条**。
  常量 0.75 / 0.15 / 28 维持 —— 环路修好后再评手感
  ① **手势挂载点错了**（"上划两次只能呼出一个"的根因，Mac 侧日志诊断确认：状态被
  `toggleSceneStyle` / `exposureCompensationTapped` **静默改写** + 滤镜条展开占掉原起手区）：原挂
  `previewLayer`，滤镜条一展开就占掉用户上次起手的那块区域 → 第二次上划的触摸被滤镜条吃掉 →
  **改挂整页 ZStack**（`simultaneousGesture`，落在任何子视图上都能识别；横滑由 `|dx| ≤ |dy|×1.5` 排除）；
  ② **判定移到 `onChanged` 提前触发**（原来必须"滑到位并松手"才有反馈 = "僵硬、要用力"的主因），
  加 `swipeDidTrigger` 防一次长滑连跨两级；位移 18 → 16pt；
  ③ **EV 吸附不明显不是算法问题，是范围**：设备报 -8…+8 = 48 档 / 每档 7pt →
  新增 `Float.evUIRange = ±2`，滑条取「UI ∩ 设备」= **13 档 / 每档约 28pt**（写硬件频率也降到 1/4）。
  自检第 8 组现已 **10 条**（含：挂载点必须在 ZStack / 提前触发 / EV 范围取交集 /
  状态改写留痕 / 误触两道闸门）。完整记录见 `docs/11`（第五节根因账，第六节验收清单）
- **第四轮：两个敏感度微调 → 复验后的二次调参（`659ddbd` → `e2b81f2`）✅ 已复验通过（2026-09-18 下午，3/3）**：
  Mac 侧定性"**机制对，量不够**"，按实测收三个常量 ——
  `tickThrottle` 0.055 → **0.15s**（55ms 在"每档 288ms 跨一档"下形同虚设，快扫约 17 声/秒；
  改后 ≤6.7 声/秒，慢拖每档照响零精度损失）/ `hysteresisRatio` 0.6 → **0.75** /
  `swipeMinTravel` 20 → **28pt**（≈6.2mm；20pt≈4.4mm 实测仍在敏感侧，预期 5~6mm）。
  起手 14 / 时长 1.2s / 起点 0.25 / 横向裕度 1.5 不动。
  自检新增**量级守卫**（`tickThrottle ≥ 0.1s`、`hysteresisRatio ≥ 0.7`）——
  补上"机制在但量太小"这个盲区。复验口径见 `docs/11` 第七节 ③
  **复验结果**：慢拖每档一响清晰可数 ✅ / 快扫一段只响 2~3 声 ✅ / 上划需要明确的一下（斜滑·慢滑不受影响）✅
- B 组接线（焦段切镜头 + Ramp / ISO·快门·白平衡刻度条 / 对焦与 EV 圆盘）：**A 组已收官
  （2026-09-18，复验落盘 `2255a23`）**，路线见 `docs/08`「B 组执行顺序」：
  **B1 焦段切镜头 + `Ramp`**（✅ **已过 Mac 真机复验（2026-09-19，8 步零 error，`8481c66` 收口版装机）**；
  方案 `docs/15` + 修订：**按镜头角色读设备**（真机 `switchOver=[2.000, 10.000]` 推翻
  旧的 `mm ÷ 13` 纯算术 —— 见 Mac 快照段"B1 复验记录"）/ 切镜头**不重建会话** /
  ramp 在唯一锁内 / 会话就绪后按档位对齐一次 / 不可用档位**灰但仍可点**）。
  **⚠️ 核心事实（后续文档一律以此为准）**：真机 `virtualDeviceSwitchOverVideoZoomFactors = [2.000, 10.000]`
  ⇒ 反推 `2.0 × base = 24mm`、`10.0 × base = 120mm` ⇒ **虚拟基准是 12mm，不是 13mm**。
  旧 `mm ÷ 13` 让整表偏小 8%，且 **120mm 档的 9.231 落在 `switchOver[1] = 10.0` 之下**
  → 系统**不切长焦**、只用主摄数码放大（糊），而 UI 显示"已切到 120mm"。
  现在按角色读：13→超广角原生 / 24→广角原生 / 35→广角×35/24 / 48→广角×2 / 120→长焦原生，
  **基准之争就此消失**；复验第 ③ 项实测「**120mm 画质锐利 —— 长焦真切上了**」✅
  （旧映射算出 9.231 < switchOver 10.0，**根本切不上长焦**，只会主摄数码放大）；
  **35mm 档 = 纯主摄裁切**（2.917 落在 `switchOver[0]=2.0` 与 `[1]=10.0` 之间 → **不换镜头、不跨切换点**，复验确认）✅
  → **焦段条已改 5 档（13 / 24 / 35 / 48 / 120）**，方案 `docs/17`；原型侧也已同步（CB `d9cf708`），
  两边数据完全一致、无"先行扩展"（`check_presets` 第 4 组逐档全等）。
  ⚠️ 复验带出 **2 条新 backlog**（⑤ 1:1 遮幅 14pt → **已修**；⑥ 取景器与成片视场不一致 → 记档待批），
  见 Mac 快照段）
  → **B2 ISO/快门/白平衡刻度条：✅ 全件交付**（B2a 数据层+硬件 API+session 转发+VM 状态 `15d0633`；
    B2b 刻度条 UI+图标行高亮+EV 面板收口+同槽互斥+焦段条让位+净可见账表 `09d4a8a`；方案 `docs/16`）
    → **B3** 对焦与 EV 圆盘（#8，🔷 **下一件**，
  ⚠️ 圆盘编辑态要接与 `docs/14` 回写环**同款**的闸门）→ **B4** 格式选择器接线（#11，
  走已有 `CaptureDeviceConfigurator.applyFormat`，码率表换真机推荐值）→ **B5** 参数导入（#13，**等 P4**）。
  **B2 已交付（`15d0633` + `09d4a8a`）→ 下一件 B3**（守"先方案 → 确认 → 再写代码"的节奏）

### Mac 侧真机验证快照（2026-09-19 更新 · Mac CB 维护，新会话接着用）

**git 状态**：Mac 侧最新提交 = `[mac-fix] 7bd53b0`（`FocalPreset` 的 `isSwiftExtension` let→var 编译修复 + 自检⑭同步，已 push）。
远端已含 B1 全部提交：`78e6242` B1 落地 / `cb13ce9` 第二轮修订（映射改按角色读设备）/
`d5494a1` 遮幅诊断日志 / `aac31d4`+`ce1e015`+`8481c66` 35mm 五档收口 / `d9cf708` CB 原型五档。
A 组 4 笔（`fe88bd6` / `e2b81f2` / `ecfbe74` / `e3f426e`）**已于 2026-09-18 全部过 Mac 真机复验**。

**B1 复验记录（2026-09-19 · iPhone 16 Pro / iOS 26.6 · `8481c66` 收口版全绿后装机）**：

8 步验收（用户操作 + 日志对账，全程零 error）：

| 步 | 结果 | 关键证据 |
|---|---|---|
| ① 13↔120 往返 | ✅ 耗时接近 | 16:33:46~53 六轮双向，toast 全对；ramp 固定 0.35s 结构上等时 |
| ② 连点乱点 | ✅ 停最后点的档 | 16:34:06~13.957 共 23 连点（0.2~0.6s 间隔）零错误 |
| ③ 120mm 画质 | ✅ **锐利**（用户截图目视） | 硬件落点 10.00× = 长焦原生阶梯（`原生阶梯=[1.000, 2.000, 10.000]`），长焦真切上 |
| ④ 录制中切档 | ✅ 工作 | 16:35:18 开始录制 → 16:35:23 切 48mm → 16:35:27 录制完成 39.8MB 入库 |
| ⑤ 24mm 拍照 | ⚠️ 拍成但**视场不一致**（见 backlog ⑥） | 4032×3024 / 1.83MB 入库 |
| ⑥ 视频↔照片保留 | ✅ 保留 | 16:38:34→35 来回切换；多次「会话就绪·焦段对齐」按当前档回推硬件 |
| ⑦ 变焦中接拍照 | ✅ 不崩 | **点 35mm 后 185ms 快门（ramp 进行中）**，照片正常入库 |
| ⑧ 1:1 遮幅 | ✅ 开 5.3s | 诊断数据见 backlog ⑤ |

**35mm 五档（`8481c66`）**：冷启动日志 `档位=13→1.000 / 24→2.000 / 35→2.917 / 48→4.000 / 120→10.000 · 不可用=无` ✅；
药丸像素实测 5 颗各 44.7pt 宽、组左 72.7 / 右 329.3、**两侧余量各 56.7pt 完全对称**，无重叠无裁切 ✅

**B1 带出的核心事实修正（供后续引用）**：真机 `switchOver=[2.000, 10.000]`、
`原生阶梯=[1.000, 2.000, 10.000]`、`constituents=3 · 角色=[超广角,广角,长焦]`。
**虚拟基准是 12mm 不是 13mm**（2.0×12=24 ✓，10.0×12=120 ✓）——旧 `mm ÷ 13` 映射会让
120mm 档落在 9.231 < switchOver 10.0，**根本切不上长焦**；现按角色读设备已修。

**Mac 侧 [mac-fix] 两条（都已 push）**：`156abd9` `storageText` 重复声明（#11 引入）；
`7bd53b0` `isSwiftExtension` 必须是 `var`（`let` 带默认值不进 memberwise init，
`FocalCatalog:157` 报 extra argument；自检第 11 组⑭ 正则已同步 var 口径）。

**B2 修复复验记录（2026-09-19 晚 · `b1bce6b` · Mac 真机）**：

| 项 | 结果 | 关键证据 |
|---|---|---|
| 白平衡手动开关不再崩 | ✅ | **连点 14 次**（19:25:06~25s）全部「被拒 + toast」留痕，进程存活，**崩溃目录零新增**（9 份报告全是修复前，最新 18:25:35） |
| B2 手动档置灰 + toast | ✅ | ISO 被拒 ×3 + toast「当前多摄虚拟设备不支持手动曝光（ISO / 快门） · 切物理镜头后开放」；白平衡同款 ×14；快门条同判据（未单独点，已被 ISO 证明）；视觉置灰代码核实 `ParameterStripView:89-90/301-316`（灰但仍可点） |
| 冷启动能力探测日志 | ✅ | `手动参数能力：曝光(custom)=false / 白平衡(锁定增益)=false` + **「尚未就绪→兜底→补发」两条齐**（上一轮缺失的第二条这次出现） |
| `setManualFocus` 守卫 | ✅ | 纸面核实：`setManualWhiteBalance` / `setManualFocus:231` / `CaptureCapabilities:448/457` 三处全换正确探测 API |

**两个新发现（2026-09-19 晚）**：① **互斥留痕（好事）**：刻度条 ↔ 参数排「连带收起」有日志
（`参数排展开 … · 连带收起 白平衡 刻度条` 双向，19:25:59~26:01 三次往返全对）——状态改写不再静默；
② **小记账**：「白平衡增益尚未就绪」冷启动**同毫秒打两遍**（19:23:11.412 ×2），
疑似 `publishManualState` 冷启被调两次（轻微，未修）。

**B3a 复验记录（2026-09-19 晚 · EV 圆盘 · `0d8d00a` 收口版）—— 通过 ✅**：

- **7 颗数字（暗场景程序化实测）**：−3.0@139.4° / −2.0@166.4° / −1.0@192.6° / **0**@222.8° /
  +1.0@250.6° / +2.0@280.2° / +3.0@315.0° —— 与 +1.4 EV 理论布局（138/168/198/228/258/288/318）
  逐项吻合；间距≈30°、跨度 180°、**"0" 正确渲染为单字形**（`ev == 0 ? "0"` 特判生效）。
- **拖动**：全量程跟手（0.0 → −3.0 → +1.4 实测跨全量程），0.1 步进 + 触感 ✓；
  **点数值框归零 ✓**（两项手感用户已确认）。
- **指针 9 点不动 + 绿高亮段在指针下**（+1.0 与 +2.0 之间 = +1.4 位置）✓；数值框 "+1.4 EV" ✓。
- **半透明实测**：亮场景盘内 RGB≈68 vs 盘外≈200（透出+压暗）；透明度 0.45/0.50/0.55（用户拍板口径）。
- **3 处 [mac-fix]（全部已 push）**：① `bf56968` 编译错 —— `Path(tickPath(...))`（`SwiftUICore.Path`
  无 `init(Path)`）；② `b0fd439` **拖不动** = `.gesture` 挂 `.position` 之后（坐标空间错位 → 值钉 −3.0）
  + 盘底半透明；③ `0d8d00a` **数字"排序紊乱"** = `.rotationEffect` 同样必须挂 `.position` **之前**
  （否则旋转绕盘心 → 位置角 2θ+180、间距翻倍 60°、−3/+3 在 90° 重叠）。
- ⚠️ **通用坑（本件两条 bug 同源，WB 记档）**：SwiftUI 的 `.position` 会把视图包进"占满父级"的容器，
  **其后挂的修饰器/手势都作用在容器上**（旋转绕容器中心、手势坐标变整页坐标）——组件内"自转/摆位"
  类修饰器一律**先 `.rotationEffect` 再 `.position`**，手势一律挂在 `.position` **之前**。
- "0.0" 疑点已撤销（上一轮为裁剪越界误读；本轮未复现）。

**EV 圆盘三改复验记录（2026-09-19 晚五 · `f482339` + 约定落盘 `c8be2df`）—— 通过 ✅**
（用户拍板三项修改；真机实测 4/4）：
① **顺滑顺走** ✅ 手指沿盘顺时针滑 → 盘面**顺时针跟手 1:1** 转（拖拽从原型"绝对定位"改为
   **相对位移模型**：首次触摸只锚定、ΔRing=Δcss、Δv=−Δcss/180）；
② **点击不改值** ✅（首次触摸只记锚点，零位移松手不推值；数值框归零独立手势不受影响）；
③ **字号缩小** ✅ 像素前后对照（同脚本量新旧截图）——刻度数字单块高 **27→17px**
   （= 8/13 预期比 0.615 ✓；根因：原型刻度数字是 SVG `<text>`（字号 13 是 400 坐标系
   用户单位，随容器缩放到 246pt 盘 ≈8pt），**Swift 曾误写死 13pt 偏大 62%** → 改 `13*scale`）；
   数值框文字墨迹 **34→28px**（= 13/16 预期比 0.8125 ✓；`evDialValueFontSize` 16→13 **用户拍板
   偏离原型**，原型 `.ev-val` 是 16px）；
④ 数值框点归零 + 拖动触感正常 ✅；日志 3 开 3 收、零 error。
⚠️ **语义副作用（用户已认可）**：相对位移模型下**"顺时针滑 = 值减小"**（数字顺时针递增 + 盘面
跟手 = 物理转盘语义）；若要"顺时针滑 = 值变大"须镜像数字排列（−3 上 / +3 下），未采纳。
📌 B3b 对焦盘落地时必须同步的三条口径见 backlog ⑨（半透明盘底 / 字号按 400 坐标系缩放 /
相对位移拖拽模型 + 方向约定）。

**B3b 复验记录（2026-09-20 · `4b79e46`）—— 3/4 通过，③ 点按对焦回归（已定位+最小修）**：

| 项 | 结果 | 证据 |
|---|---|---|
| ① 点「对焦」→ toast + warn、不开盘 | ✅ | 3 次 `WRN 点「对焦」手动圆盘被拒` + toast，日志**无**对焦盘展开行 |
| ② 冷启动能力行 | ✅ | `手动参数能力：… / 对焦(锁定位置)=false` |
| ③ 点取景器对焦照常 | ❌ **回归** | 圈出现但**永不合焦** → 根因链见下 |
| ④ EV 盘回归（DialView 共享重构） | ✅ | 开/收 ×2 留痕（11:38 / 11:41）；拖动·归零实测无退化（该两项无日志点） |
| 拍照 | ✅ | 11:39:39 快门 → 1.17MB 入库 →「已保存到相册」；全程零 ERR |

**③ 根因链（硬证据，WB 正源修依据）**：
- **SDK 明文**（`AVCaptureDevice.h:1053-1054`）：`.autoFocus` = "autofocus once and then **change
  the focus mode to AVCaptureFocusModeLocked**" —— 一次性 AF 完成后**设备自动转 `.locked`**；
- B3b「回读三件套」`manualFocus(of:) = focusMode == .locked ? lensPosition : nil`
  （`CaptureDeviceConfigurator.swift:257-260`）把**硬件状态**当**用户意图**用；
- `focus(atDevicePoint:)` 设完 `.autoFocus` 后立刻 `publishManualState`（`CaptureSessionController`
  :578）→ 读到 `.locked` → `manualFocus` 非 nil（:562）→ `isFocusManual`（`CameraViewModel:106`）= true；
- 之后所有点按走「仅测光、不动焦」（`CameraViewModel.focusTapped` :577-585，拍板 ③），
  而该路径**不碰 focusMode** → `.locked` 永久保留 → **死锁：点按对焦永久失效**；
- 日志实锤：11:38:01.904/.908 两次点按还走「对焦点 →」；**11:38:10 起 10+ 次点按全为**
  「手动对焦锁定中 → 点按仅测光」；
- ⚠️ **与虚拟设备无关**：物理架构落地后照样犯（任何一次点按对焦都会把 app 打进"手动锁定"态）。

**Mac 最小修（已 push）**：`CaptureSessionController.swift:562` → `self.manualFocus = manualFocusOK ? focus : nil`
（本机 `对焦(锁定位置)=false` → 手动对焦进不去 → 状态恒 nil，精确恢复 B3b 前行为）。
**最小修后复验（2026-09-20）✅**：20 次点按（不同位置/近远切换）**全部**走「对焦点 → (…)」、
**零**「手动对焦锁定中 → 点按仅测光」、零 ERR；拍照 1.81MB 正常入库 —— ③ 通过，
**B3b 复验全项清零**（正源修仍待 WB）。

**正源修复验（2026-09-20 · `0884879`）✅ 全闭环**：28 次点按**全部**走「对焦点 →」、
零「仅测光」、零 ERR / 零 WRN；EV 盘回归 4 项（开 → 数值框归零 ×2 → 点别处收起 + 拖动实测）✅；
拍照 1.71MB 入库 ✅。代码侧核对：`manualFocus` 写入点只剩两处**意图入口**
（`CaptureSessionController` :494 进手动 / :515 退手动；:585 明写"绝不能在此赋值" + 自检⑱ 守）；
`manualFocus(of:)` 已删（`CaptureDeviceConfigurator` :252「别加回来」）；
`focusEditingChanged` 首拖拦截已删（`CameraViewModel` :1256）。
⏳ **挂起**：对焦盘拖动链（首拖即进手动档）—— 虚拟设备上对焦盘不打开，等物理架构改造后回归。
**WB 正源修（已转）**：「是否手动对焦」改由 **UI 意图**（对焦圆盘锁焦状态）驱动，硬件回读只喂
`currentLensPosition`；`isFocusAuto`（`CameraViewModel:109`）同源污染需一并处理。
⚠️ **通用教训（已记长期记忆）**：**硬件状态回读不能直接当用户意图用** —— 评估任何"回读当状态"
的设计时先问：这个硬件值会不会在正常操作中自发出现？（`.custom`/白平衡 `.locked` 不会 → 回读安全；
对焦 `.locked` 会 → 回读不安全）

**物理架构改造复验记录（2026-09-20 · `975be60` 三笔 + Mac 4 处编译修复 `b04c346`）**：

**docs/20 §十 逐项**：

| 项 | 结果 | 证据（日志时间戳） |
|---|---|---|
| A 进手动（转场 → 物理 → 手动档立即可用） | ✅ | 13:12:29 点 ISO 手动开关 → 切物理 → 13:12:34 手动档生效 |
| B 搬运（ISO/WB/EV + 对焦降级） | ✅（部分未触发） | **WB 重放 5600K** ✓（13:16:26/33 跨镜头后保留）/ EV 重放 ✓ / 对焦降级 ✓；ISO/快门搬运本次未触发（换设备前没设过 ISO） |
| C 回切（2s 防抖） | ✅（慢） | 「全手动档退出 2s → 切回虚拟」×4 + 拓扑回虚拟 ✓ |
| D 边界 | ✅ 一半 | **录制中 48↔35 同镜头变焦 ✓**（13:18:08/09/12，成品 220MB 入库）；跨镜头拒绝未试到 |
| E 排队 | — 未触发 | — |
| F ★ 对焦盘拖动链 | ✅ **点亮**（B3b 挂起项闭环） | 换设备后圆盘自动打开（13:11:11.566）；拖动后「手动对焦锁定中→仅测光」出现（13:11:23 / 13:17:18）→ 手动档真的进去了 |
| G Live×13mm | ✅ **支持**（拍板③数据） | `换设备完成 → 物理单摄（13 mm）… Live Photo 能力=true`（13:16:26）——**预置灰不必要** |

**全场零 ERR**（仅 2 条音频中断 WRN —— 来电/闹钟，无关）。

**5 个问题（按严重度，已转 WB）**：

🔴 **问题 1 · 换设备太慢（4.5~11.9s 糊屏）**：虚拟→物理 **4.5~4.7s** / 物理→虚拟 **11.3~11.9s** /
物理→物理 **9.1s**。根因：每次换设备都跑 `applyPreferredFormatLocked` 的「逐个 apply + 读 Live 能力」
循环（日志实测 **12~41 次** `格式已应用`，每次 300~800ms），全程被模糊盖住。
**修法：缓存 (设备, 模式) → 已探测成功的格式**（或把上次成功的 `4032x3024 1-30fps 4:3` 排首位再探测）；
该循环本是冷启动一次性口径，不能每次换设备都跑（缓存后转场回落到 ~1-2s）。

🔴 **问题 2 · 刻度条初值守卫缺失（严重）**：刚换设备后立即切手动 → 初值取到垃圾值
（13:13:20.946「初值取设备当前值 **ISO 0 / —**」→ 硬件被写进 **ISO 54 / 1/71429s** 极端曝光）。
**修法：初值有效性守卫**（ISO/秒数无效 → 回落档位表默认值，或等一帧取新设备真值）。

🟠 **问题 3 · 刻度条回弹一档（实锤）**：拖动判据 = `|position − last| ≥ 0.75`（滞后），松手吸附 =
独立 `round(position)` —— **不同源**。日志：13:12:35.513 拖动至 ISO 800 → 13:12:35.587
**松手吸附到 640**；且拖动中气泡值可比指针下刻度差最多 0.75 档（ISO 35pt / WB 20pt）→「卡点不准、
不灵敏」。**修法：上报与吸附同源**（都用 `round(position)`；0.75 → 0.5 或用「变化即上报 + 现有
0.15s 节流」防连响）。

🟠 **问题 4 · 视频模式下 Live 判据恒 false**：视频模式照片输出不挂载 → `isLivePhotoCaptureSupported`
恒 false → 35mm 换设备把 **41 个候选全跑完**（9.1s，4:3 试完试 16:9），最后落到第一个候选
（`Live Photo 能力=false`）。**修法：探测前判「当前模式是否需要 Live」**（视频/Log 直接走视频格式链）。

🟡 **问题 5 · 会话重启后物理档位对齐静默失败**：13:15:05 / 13:18:31 两次 startRunning 后
「**焦段 24mm / 48mm 在本机没有对应镜头，未推硬件**」→ UI 显示 24/48mm、硬件没动（档位与视场不一致，
"静默失败"类）。**修法：物理分支用 `zoomFactorOnPhysicalDevice` 补映射**。

**补充（同批交 WB）**：
- **跨镜头拒绝未验**：录制中没试过点 13/120（日志无痕）——需补测一条
- **刻度条初值不在档位表**（ISO 1007、快门 1/33 直接进气泡）→ **显示侧吸附**（气泡与指针用同一个
  "最近档"；硬件初值保持不动，画面不跳）

**物理架构改造 · 第二批复验记录（2026-09-20 深夜 · WB `09ecbfc` 修上批 5 问题后）**：

| 项 | 结果 | 证据 |
|---|---|---|
| ① 换设备耗时 | ✅ 缓存命中 **0.59~0.75s**（7 次）/ 物理→虚拟 1.38s；⚠️ 每个 (设备,模式) **首次仍 4.4~4.7s** 冷探测 | 日志对账 |
| ② 切手动初值 | ✅ 有效值（ISO 934 / 1/40s、1723 / 1/36s），不再 ISO 0 | 守卫 + warn 留痕 |
| ③ 刻度条回弹 | ✅ 同源 `round` 落地：拖 5668→5200→5300→5400→5500→5400→5300 **逐档一致无跳档** | 日志 |
| ④ 视频模式切镜头 | ✅ **0.82 / 0.91 / 1.06s**（单次 apply 直达，Live=false 为预期） | 日志 |
| ⑤ 重启后档位对齐（**重验**） | ✅ **三次会话重启（切后台↔前台）全对**：物理 24mm→**1.00×** / 虚拟 48mm→4.00× / 物理 35mm→**1.46×**（`mainCropFactor`）；**零**「没有对应镜头」 | 日志 |
| ⑥ 录制中跨镜头拒绝（补测） | ✅ 4 次拒绝（24→13 / 24→120）+ toast；录制中 35/48 同镜头 ramp ✓；成品 336MB 入库 | 日志 |

**待办清单（7 条，下一批转 WB）**：

🔴 **1 · EV 重放杀手动曝光**：4 次 WRN 实锤（13:51:55 / 13:52:18 / 13:52:22 / 13:55:22）——
`applyExposureBias` 见 `.custom` 就强制回 `.continuousAutoExposure`，而 reapply 顺序是「搬 ISO → 搬 EV」
→ **手动档被自己搬的 EV 杀掉** → 手动状态清零 → 2s 后连会话都回切虚拟。
**违反 docs/18 §2.4 明文**（手动档下 EV 只保留在状态里、不推硬件）。
修法：手动档下不推 EV（或 EV 重放排在手动曝光重放之前、仅自动档执行）。
（重验 ⑤ 那轮未复现——触发条件 = **手动档已激活时跨镜头切换**）

🟠 **2 · 刻度条「对应数值」**：气泡显示硬件真值（934 / 1/40s / 5668K），档位表里没有这些值
→ 与指针下的刻度对不上。修法：**显示侧吸附**（气泡与指针同用 `nearestStep`；硬件初值保持不动画面不跳）。

🟠 **3 · 刻度条「灵敏度」**（**用户已拍板**）：现状半档才换档 + 档距 ISO 46pt / 快门 56pt / WB 26pt
→ 小幅拖动无反应（实锤：1 秒内连推 5 次同值 ISO 3600；快门条同款）。
**拍板方向：提高跟手倍率 = 1.5:1**；**安全边界：跟手倍率不超过 2:1**。

🟠 **4 · 刻度条细密度（三条都改 · 参考图口径）**：
| 条 | 要求 |
|---|---|
| 白平衡 | 主档每 **500K**（带数字 4000K/4500K/…），细刻度每 **100K**（主档间 4 条细刻度等分） |
| ISO | **主档之间加细刻度** |
| 快门 | **主档之间加细刻度** |

🔴 **5（补充）· 自动/手动切换要瞬发**：现状切手动触发转场（切物理设备）。
技术矛盾已确认：手动参数只能物理设备 → 必然换设备 → 必然有画面跳变。
两案：**A1 真瞬发**（不转场、画面跳一下）/ **A2 极短转场**（压到 **0.3~0.5s** 盖住跳变）。
**用户倾向 A2，请 WB 确认**（含 0.3~0.5s 在设备切换链路上的可行性）。

🔴 **6（补充）· 切模式时参数保持**：现状切模式重建会话 → **手动参数被重置为自动**；要改成**保留**。
（注：代码注释声称"手动档本身会活下来：换的是 output 不是 device"，日志里也见手动档活过一次
照片→视频切换（13:55:18）→ 需 WB 复现定位；注意与 🔴1 的"EV 杀手动"表症相似，别混修）

🟡 **7 · 松手同值重复写入**：松手时无条件再推一次同值（日志噪声 + 一次多余硬件写）→ 同值不推。

**批三复验记录 + 用户补充问题（2026-09-20 深夜 · WB `f677d30` + Mac [mac-fix] `f622066`）**：

| 项 | 结果 | 证据 |
|---|---|---|
| 🔴1 EV 不杀手动 | ⚠️ **未修完** | 搬运链已修（新增「手动曝光档激活 → 跳过 EV 重放」✓）；**仍有 2 次 WRN**（15:15:00.218 / 15:15:44.524）→ 实锤 A |
| 🔴6 切模式参数保持 | ⚠️ 未触发 | 3 次切模式（15:16:46/49/52）时手动档**未激活** → 需在手动档激活下复测 |
| 🟠2 气泡与指针同档 | ✅ 代码落地 | `bubbleText` 吸附到 `steps[currentStepIndex].value`（`ParameterStripView:315`） |
| 🟠3 灵敏度 1.5:1 | ❌ **方向反了** | `translation.width / 1.5` = 更钝（应 `× 1.5`）→ 实锤 B |
| 🟠4 细刻度 | ⚠️ 代码落地但**外观没变化**（用户实测） | 半档线已加（ISO/快门：1px / 20% 白 / 半高）；白平衡未加（注释称 100K 已覆盖）→ 疑：细线太淡 + 档距偏疏 + WB 无层级区分 |
| 🟡7 同值不推 | ✅ | 「与当前硬件值一致 → 跳过重复写入」 |
| 🔴5 A2 转场 | ✅ 令牌 | `dialBlurIn 0.10 / dialBlurOut 0.15` |

**实锤 A · EV 杀手动残留场景**：判据只看**旧设备快照**，而**新设备会自带上一轮遗留的 `.custom`**
（手动档退出只改当前设备 mode，其他镜头仍留 `.custom`；device 对象 remove/re-add 不重置）。
证据：15:15:00 / 15:15:44 两次 WRN 均「只搬白平衡 + 推 EV」且日志**无**「手动曝光已重放」。
修法：换设备/搬运时**先把新设备归一到自动档**再按快照重放；或 EV 推送前判**新设备实际 `exposureMode`**。

**实锤 B · 跟手倍率方向写反**：`translation.width / dragGain(1.5)` —— 指尖要拖 1.5 倍距离才走一档
（比改前更钝）。「提高跟手倍率」= 指尖 1pt → 盘面 1.5pt，应 **`× 1.5`**（上限 2）。
（WB 注释「1 秒连推 5 档太灵敏」系误读——那 5 次是**同一个值**重复推，证据指向「不灵敏」。）

**用户补充 4 问题（2026-09-20）**：
1. **刻度条还是不对**：刻度值对不准 / 卡点不准确 / 卡点回弹 / 对应数值不对 / **外形没有变化（细密度没生效？）**
   → 与实锤 B（方向）+ 🟠2（吸附力度）+ 🟠4（视觉层级）同批修。
2. **切焦段和模式，参数还是会默认自动**（🔴1 + 🔴6 未修完的合并表现）。
3. **转场该有/不该有的边界没理清**：要求 = 只有**切焦段**出现转场且 ≤1s；**切自动/手动、点对焦按钮、切模式 → 人眼无感**。
   现状：点对焦按钮会触发转场（拍板① 入口升级所致）、其他入口也走同一转场；时长仍被设备硬切换
   （0.6~0.75s）拖着（当前 ≈0.1+0.6~0.75+0.15 ≈ 0.85~1.0s，接近 1s 上限）。
   建议方向：非焦段入口**预切换/后台切换**（打开刻度条或按下对焦入口时先把物理设备挂好 → 真正切换时无转场）。
4. **120mm 焦段下无法触碰对焦（新）**：根因 = 15:15:02 用户在**对焦圆盘里关掉「自动对焦」**开关
   → 「自动对焦已关 → 手动（**当前值 0.00 锁定**）」→ 锁在 0.00（最近端）画面跑焦；此后**所有点按
   都走「仅测光、不动焦」**（拍板③；本场 40 次）→ **无出路恢复对焦**。
   修法：① 锁定值有效性守卫（0.00 端点不许当"当前值"；取不到保持自动 + toast）；
   ② 点按在手动锁定下的行为需**用户重新拍板**（预期"点按应能恢复对焦"，或至少明显提示 + 一键恢复自动）。

   **批四复验记录（2026-09-20 夜 · `f548f32` = 批四 6 条 + 方案 A）**：

   | 组 | 结果 | 证据 |
   |---|---|---|
   | ① 实锤 A（EV 杀手动残留） | ✅ **0 次** WRN | 全场日志 |
   | ② 实锤 B（跟手倍率） | ✅ 代码已改 **`* 1.5`**（批三 `/1.5` 已修正） | `ParameterStripView:451` |
   | ③ 刻度条 | ✅ 松手吸附全为档位值、**与拖动最后值逐项吻合**（800/1200/1600…）= 无回弹、无表外值；细密度已四档层级（用户仍要更密 → 新问题 2） | 日志对账 |
   | ④ 参数保持 | ✅ **切焦段保留**（WB 4895K、ISO 跨镜头都保住）；**切模式未测**（本场无模式切换 → 新问题 3） | 日志 |
   | ⑤ 转场边界 | ⚠️ 切焦段 **0.53~0.58s** ✓ ≤1s；**非焦段入口退了转场** ✗（→ 新问题 1） | 日志 |
   | ⑥ 120mm 对焦守卫 | ✅「自动对焦已关 → 手动（**当前值 0.59** 锁定）」不再 0.00 | 22:03:33.692 |
   | ⑦ 方案 A | ✅ 两类日志都在、时序正确（盘开着→仅测光；盘关着→解除+对焦） | 22:03:34 / 22:03:39 等 |

   **新 3 条问题（交 WB）**：

   🔴 **1 · 静默路径退转场（转场太长 + 卡顿）**：根因 = **每会话首次换设备的冷探测 4.2~5.0s > 静默超时
   1.2s → 必然退转场**（日志实锤：22:03:26.710「静默换形态（后台挂设备 · 无转场）」→ 22:03:30.893
   「换设备完成（静默·无转场）」+ 同一时间戳「换设备开始（模糊淡入中）」= 退转场双行）。
   修法：① **冷启动后台预热**（启动后静默探测三颗镜头格式，把每会话首次 ~4s 消掉 → 静默路径可 0.6s 内完成）；
   ② 换设备期间**冻结上一帧**（消「卡顿」观感）。
   目标：只有点焦段有转场（≤1s）；点对焦 / 开刻度条 / 切自动手动 / 切模式 → **人眼无感**。

   🟠 **2 · 刻度条标点要更细更多**：在批四四档层级（minor 10 / mid 15 / major 22 / preset 22）之上**再加密**
   （标点更细、更多）；细分口径按参考图（主档间 4 条细刻度）。

   🔴 **3 · 手动模式下切模式仍恢复自动档**：**日志未覆盖**（本场 22:03–22:07 一次模式切换都没有）→
   需 WB 自测确认批四「切模式一律按快照重放」是否真生效；用户补测后 Mac 对账。

   **顺带**：「首次冷探测 4.4~5.0s」现在不再只是观感问题——**它直接导致静默路径退转场**，
   建议与问题 1 的冷启动预热一起修。

   **批五复验记录 + 用户 4 问题（2026-09-21 · `d83f470`）**：

   **批五 3 条 + 补测：日志对账全过**：

   | 项 | 结果 |
   |---|---|
   | ① 静默路径不退转场 | ✅ 全场**零**「模糊淡入中」；耗时：首次 **4.27s**（每设备首次冷探测）→ 之后 **0.45~0.48s（物理）/ 0.99~1.03s（虚拟）**；格式缓存**已落盘**（启动日志「已缓存并落盘」） |
   | ② 刻度细密（hair） | ✅ 代码口径：hair = 每相邻真实档位 1 条 → WB 主档（500K）间 = 4 真实 + 4 细分 = **8 条**（参考图 9~10 条同量级） |
   | ③ 切模式 WB 保持 | ✅ 复查日志 ×4 全为「白平衡=手动」；另有「切模式：手动白平衡已按快照重放（5397K）」 |
   | ④ 录制中跨镜头拒绝（补测） | ✅ 3 次拒绝 + toast；录制正常入库 |

   另记：新机制「**0.8s 强制交回硬件真值**」触发 1 次（11:45:10.953）——本例无可见跳变，
   但硬件 clamp 时会表现为**松手 0.8s 后延迟跳变**（观察项，暂不追）。

   **用户 4 问题（2026-09-21，交 WB）**：

   🟠 **1 · 刻度条改行业主流参数 + 复刻飓风相机精度**（用户将另出方案；下列为 Mac 先给的三项分析）：
   - **现状档位**（`ParameterStripCatalog`）：ISO **25 档 = 50…12000**（已是 1/3 档主流序列，
     **只差末档**）；快门 **15 档整档**（1 … 1/8000，另有我们多的 **1/12000**——行业表无此档）；
   - **改动的自检冲突**：`check_presets.js` 第 6 组**逐条比对**原型 `ISO_STRIP` / `SHUTTER_VAL` /
     `SHUTTER_LABEL` ↔ `ParameterStripCatalog` → **Swift 单方面改必红**，需 CB 同步原型
     （或沿用先例：加"Swift 先行 + 标记"机制，同 `FocalPreset.isSwiftExtension` 那套）；
   - ~~细分线量级：… slot 必须压到 ≈18~20pt/档 …~~ ⚠️ **该建议已随拍板 A 撤销**（用户选"复刻飓风"：
     pitch 固定 **40pt/档**、条长就让它长、靠**滚动**解决）→ **以本文下文「A 的完整落地口径」为准**；
   - 快门加 1/3 档后档数 15 → **~40**（条长 ~1600pt，可视 8~9 档 —— 用户已接受该代价）；
     ISO 已是 1/3 档；**三条 slot 统一 40pt**（ISO 46→40 / 快门 56→40 / WB 26→40）；
     ✅ **精度依据已补（2026-09-21 用户给飓风 ISO 条 + 快门条全分辨率截图各一张，1206×2622 = 402pt @3x）**，
     实测见下「飓风刻度条实测规格」；
   - 用户注意点 **已实测修正**：截图里的 **12096 = 设备实读 maxISO**（飓风末档直接显示硬件上限），
     不是 12800 → **修法改为"末档显示设备实读 maxISO"**（比把表扩到 12800 更忠实硬件；12800 大概率在
     设备 maxISO 之外、也会置灰）。我们表里末档 12000 是硬编码，改成实读上限即可对齐。

   **飓风刻度条实测规格（2026-09-21 · 全分辨率截图 1206×2622 = 402pt @3x · 精确到 0.3pt）**：

   | 量 | 飓风实测 | 我们当前 |
   |---|---|---|
   | **tick pitch（每档间距）** | **120px = 40.0pt**（逐根等距，最大偏差 0.3pt）| ISO 46 / 快门 56 / WB 26（`ParameterStripCatalog.slot`）|
   | **主刻度高（每档都有）** | **42px = 14.0pt**（宽 4px = 1.33pt）| 22 / 15 / 10 / 6.5（五档层级）|
   | **选中刻度** | **66px = 22.0pt**（向上长出 8pt）+ 加宽 2pt + 绿 | 指针三角 12×8 + 竖线 1.5 |
   | **细刻度** | **无**（每档一根等高刻度、**每档都有标签**）| 批五新增第 5 档 `hair` 6.5pt |
   | 档位标签字号 | 快门（含斜杠）27~28px ≈ **9.0~9.3pt 高**；ISO 纯数字 23px = **7.7pt cap** → ≈ **11pt 字号** | 10.5pt（接近 ✓）|
   | 选中档标签 | **不放大**（与普通档同大）| — |
   | 标签底 ↔ 刻度顶 | **39px = 13pt** | 数字距底 2pt |
   | 可视档数 | 8~9（402pt / 40pt ≈ 10 slot）| ISO 8.7 档 / 快门 7.2 档 |

   ✅ **两条张力已拍板（2026-09-21 用户）：选 A —— 复刻飓风**。理由（用户原话口径）：
      原始需求就是"复刻飓风相机"；**"细刻度"是基于错判参考图（旧 contact sheet）的产物**；
      **每档带标签才是飓风的做法**；接受 A 的代价 —— **只有 8~9 档可视、滚动式（左右滑）**。
   ① 结论：**"细:主高度比"= 1:1 —— 飓风没有细刻度**（每档一根 **14pt 等高**刻度 + 每档一个标签，
      只有**选中档**长高至 22pt 变绿）→ 批五加的 `hair` 层与全分辨率实测不符，**应删**；
   ② **"slot 压到 18~20pt" 已撤销**（与"每档带标签"不可能同时成立：10.5pt 字 "1/1600" ≈ 30pt 宽）。

   📌 **A 的完整落地口径（WB 实现依据）**：
   - **pitch = 40pt/档**（现 ISO 46 → 40 / 快门 56 → 40 / WB 26 → 40；`ParameterStripCatalog.slot`）；
   - 刻度层级**收敛为两态**：档位刻度 **14pt 高 / 1.33pt 宽**；**选中档 22pt 高（向上 +8pt）/ 2pt 宽 / 绿**；
     **删 `mid(15) / minor(10) / hair(6.5)` 三档细分**（`preset` 橙刻度**保留** —— 那是"预设位"语义、非密度层级）；
   - **每档都带标签**（字号维持 **10.5pt** ≈ 飓风 11pt；飓风快门"1/25"含斜杠 9.0~9.3pt 高 / ISO 数字 7.7pt cap）；
   - 标签底 ↔ 刻度顶 **13pt**（现"数字距底 2pt"口径按此对齐）；
   - **可视 8~9 档 + 滚动**（条变长是接受的代价；我们本就是拖动式，行为不变）；
   - **档位表仍走"行业主流"**（与 A 不冲突，只是条更长）：快门补 **1/3 档**中间值 + 影院值 **1/96 / 1/120**
     （180° 快门角，静照行业表里没有）；ISO 已是 1/3 档，**末档改"设备实读 maxISO"**（本机 12096）。
   ⚠️ 旧 `reference/video-frames-2026-09-16`（contact sheet，588px ≈1.5px/pt）量的
      "WB 主档间 9~10 条细刻度 / 细:主 1:2 / 主档 18pt" **已作废** —— 见上（旧估计误导了批五）。
   📌 另注（观察项，暂不动）：飓风**大字显示硬件真值**（截图 ISO 大字 = `4308`，非档位值）；
      我们（批四拍板）是**吸附到最近档** —— 两套哲学，若要对齐需用户再拍板。

   🔴 **2 · 刚进 App 点对焦键 → 不该有的转场 + 又慢又卡**：= **首次冷探测 4.27s**（静默路径无模糊，
   那 4.27s 画面是**静止帧** → 观感 = 卡住/像转场）。修法只剩**冷启动预热**一条路：
   WB 已评估"私有会话挂物理镜头可能打断虚拟会话（预览黑屏）"→ **这条 Mac 可以在真机验证**
   （需一个带预热探针的构建，或 Mac 加临时 debug 探针跑一次）。**用户口径：该处不该有转场，且不能慢/卡**。

   🔴 **3 · 「不选自动就永远手动」（用户核心口径，非单次 bug）**：**点按对焦顶掉手动曝光**实锤——
   `setFocusAndExposurePoint` 按系统语义设 `exposureMode = .continuousAutoExposure` → 手动 ISO/快门被顶掉。
   日志链：11:45:10.006 手动 799/1/60 → 11:45:13.242 点按对焦（`对焦点 → (0.44,0.22)`）→
   11:45:15.302「全手动档退出 2s → 切回虚拟」→ 11:45:16.329 复查「曝光=自动」。
   修法：点按对焦后**按意图重放手动曝光**（复用换设备/切模式的快照-重放机制）；**口径 = 用户没选自动 → 永远手动**
   （白平衡同理，EV 同族）。

   🟠 **4 · 取景器掉帧（2026-09-21 用户完整场景）**：「**点开所有功能 → 切到手动 → 依次切换
   照片 / 实况 / log / 视频 模式**后开始出现掉帧；**隔一会重启 App 后恢复**。要判定：**App 程序
   问题，还是手机问题（发热/系统）**」。
   - **Mac 初判（待 WB 用日志确认）**：**"重启 App 即恢复"是最强判据** —— 热降频/系统级 throttling
     不会因重启 App 而很快恢复；而 **App 侧资源泄漏**（每次模式切换 / 静默预切换未拆净的
     session / input / output，或各模式各持一份）会随次数累积 → 掉帧，重启即清零 ⇒ **优先按
     "App 侧泄漏"查**；
   - 已有线索：本场 40 秒内静默切换 5 次（每次 = 一次 **input 重挂**）——即"切模式/开条"都伴随重挂；
   - **要 WB 做的**：① 审计"模式切换 / 静默预切换"路径的**资源拆装对称性**（session / input / output /
     预览层是否成对增删、有无各模式常驻副本）；② 加**计数与耗时日志**（活跃 session/input/output 数、
     内存、每次切换耗时）→ 复现时一眼看出是否单调增长；③ 复现脚本：**开所有刻度条 → 切手动 →
     照片/实况/log/视频 依次切 ×N 轮**，观察计数与帧感（Mac 可真机复现 + 日志/截图对账）；
   - 反向排除项：若**只在长时间使用后**出现、且**重启 App 不能立即恢复** → 转"发热 / 系统"线
     （记录复现时刻与热状态）。

**架构级发现 · 正式记录（2026-09-19 · 供 WB/新会话当硬事实引用）**：
iOS 26 SDK `AVCaptureDevice.h:538-541` 明文 —— **`builtInTripleCamera` 等虚拟多摄设备不支持**：
① `AVCaptureExposureModeCustom`（自定义 ISO/快门）；② 锁定对焦到非 Current 的镜头位置；
③ 锁白平衡到非 `AVCaptureWhiteBalanceGainsCurrent` 的增益。传计算值即抛 ObjC 异常
（Swift catch 不住 → SIGABRT；白平衡手动开关 7 连崩即此）。
→ **B2 三条刻度条手动档 + B3 手动对焦，在"虚拟设备会话"架构下永远做不了。**
**用户拍板（2026-09-19 晚）：物理会话按需切换** —— 自动态保留虚拟设备（B1 平滑变焦体验），
进手动档或（物理态下）跨切换点切焦段时换物理设备 + 参数搬运 + 模糊转场（= `docs/18` 第二、三章）。
**Mac 对 `docs/18` 的纸面预检 7 处待补/待改**：① 手动→自动的**回切策略**未写（不切回则一次进过手动
就永久失去平滑变焦）；② 换设备**前置条件**（ramp 中先 cancel / 会话未就绪排队）未写；
③ **参数搬运顺序错**：2.3 第 4 步在 begin~commit 区间内，但 clamp 依赖新设备 activeFormat
（commit 后才写格式，铁律 1）→ 应改为 commit → applyFormat → 再搬运；④ **EV 搬运缺失**
（bias 换设备归零 → 亮度跳 + UI 不一致）；⑤ **对焦档位语义**：默认"落回连续自动"与"用户仍处
手动对焦档"矛盾 → 应显式降级 + toast；⑥ **物理会话档位→zoom 映射表缺失**：现
`nativeZoomLadder` 吃 `virtualDeviceSwitchOverVideoZoomFactors`，物理设备下该数组为空会失效，
物理路径需自己的表（UW→1.0 / Wide→1.0 / 35→1.458 / 48→2.0 / Tele→1.0）；⑦ **转场改顺序触发**
（模糊到位完成回调 → 再换设备 → 淡出，替代"并发 + 同源调参"）；自检缺 3 条（物理映射守卫 /
搬运清单方法体守卫 / 回切策略守卫）。另：13mm 物理会话 + Live Photo 兼容性列为真机待验项。

**A 组收官复验记录（2026-09-18 下午 · iPhone 16 Pro / iOS 26.6 · Debug 真机）**：

| 笔 | 结果 | 关键实测 |
|---|---|---|
| A · EV 回写环（`fe88bd6`） | **5/5 ✅** | 拖动中 thumb/填充/数字单调跟手；快拖到底再拖回无振荡；触觉不重复响；松手无回放跳变（`lastPushedExposureBias` 生效）；**+1.3 EV 三次重新展开不丢不漂**（日志 13:54:10 / 13:54:23 / 13:55:55）+ 归零硬件跟随 |
| B · 二次调参（`e2b81f2`） | **3/3 ✅** | 慢拖每档一响清晰可数；快扫 2~3 声（0.15s 节流生效）；上划明确的一下 ≈6mm（28pt），斜滑/慢滑不受影响 |
| C · #10 功能面板（`ecfbe74`） | **8/8 ✅** | 面板升起 + 7 钮依次浮入；**7 图标无空白**；遮幅/角标/HUD 真生效；设置 → 页开 + 面板收起；冷启动 5 项记得 |
| D · #11 格式芯片（`e3f426e`） | **全过 ✅** | 芯片顶替三图标 + 模式条居中不漂；选择器逐项 toast ×8 留痕；**冷启动持久化实锤**（14:12:58 杀 → 14:13:52 重启 → toast `4K · 30` 一致）；存储胶囊首屏定宽无跳变；互斥时间线不重叠；toast 边界「重设采集格式属 B 组」每次选择都说明 |

另：`156abd9` 修的是 #11 引入的编译错 —— `TopBarView` **重复声明 `storageText`**（39 行原有 + 62 行 #11 新增），删重复份零行为变化。

**已验证 ✅（数据可直接引用）**：
- 2-5a 红方块修复：实测 **26.0×26.0pt，角点 18.38pt** vs 环内沿 28.25 → 余 9.9pt（8 帧一致，中心 −0.2pt）
- 决策 #2 缩略图：`[thumb] 视频首帧缩略图已生成 180x240` ×3 次 + tmp 零残留
- **冷启动死区修复（15a435c）双确认**：视觉 = 冷启动 7s 零交互截图快门环/芯 **255,255,255**（修复前 57 = 禁用态）；行为 = 冷启动后 **2.2s 首条交互即 `[photo] 快门触发`**，无任何前置
- 2-5b 几何：卡上沿 **128.0pt**（= 安全区 62 + 10 + 顶栏 56，与规格一字不差）、圆角 **R=18**（5 采样点曲线全中）、左右不内缩、快门放大态 **82.0pt**（= 60×1.3 + 环加粗抗锯齿 ~4pt）、镜像按钮对齐 **+0.2 / −0.3pt**、徽标红点 y=148.7（胶囊 134~160，= 安全区 62 + offset 72）

> B1 期间另外修掉 2 个真问题：① **"会话就绪 · 焦段已对齐"日志打两遍** ——
> `startInternal()` 冷启动会走两次（`onAppear` 的 `setVisible(true)` + scenePhase 的
> `setAppActive(true)`），而 `.running` 是 `@Published` 的**同值重发**（willSet 语义）；
> 修法两层：会话侧"同值不重发"（根因）+ VM 侧 `removeDuplicates()`（兜底）。
> ② Mac 编译抓到我写的 `let isSwiftExtension: Bool = false` —— **`let` 带默认值不进 memberwise init**，
> `FocalPreset(..., isSwiftExtension: true)` 报 "extra argument"（`[mac-fix] 7bd53b0` 改 `var`，
> 已做成回归守卫：自检 ⑭ 要求 `var`、变异测试 M18 把 var 改回 let 会 FAIL）。

**Backlog（#6 做完后处理）**：① 放大态「影调预览/存储」胶囊行下沿 ~130pt 与卡上沿 128pt 交叠 ~2pt（视觉无碍；要分离需卡上沿下移 4pt，会动已验证的 128）；② **已修 ✅**（2026-09-18 底部三问题之一）：参数排默认收起 + 图标行「曝光补偿」= 展开/收起开关，方案见 `docs/11`；③ 网格线已修 ✅；④ **浮层互斥缺"收起参数刻度条 / EV 圆盘"这一半**（#6 遗留）：参数排那一半已在 `docs/11` 补齐（`collapseOverlays()` 现在收三个），**刻度条（#9）与 EV 圆盘（#8）落地时继续加进同一处**；⑤ **1:1 遮幅画面偏下 14pt（2026-09-19 定案 · ✅ 已修 `8b2dec5`）**：诊断日志（`d5494a1`）实测 ——
**容器 402×778**（安全区）/ 窗高 402 / 容器内上下黑边**各 188** / 窗中心 **451.0pt = 安全区中心**（构造上精确居中，像素扫描上沿 250.0 = 62+188 一字不差）。
但**全屏中心 = 437.0pt** → 窗中心低 **14.0pt**；物理屏上黑边 上 250 / 下 222（上厚 28pt，眼读作"偏下"）。
**拍板：已选 A（全屏居中）并实施** —— 窗中心钉 437 → 上下各 236，**画面上移 14pt**；
偏置 = `(上安全区 − 下安全区) / 2`（安全区上 62 / 下 34），⚠️ **仅常态生效**（⤢ 放大态按取景卡片居中）；
自检第 9 组 ⑥ 已守「算式 + 钳制 + 日志可核对」三件（日志应打 `窗中心（全屏）437 vs 全屏中心 437`）。
**B · 可见区居中**（上移 60pt，以「顶栏下沿 118 ↔ 底栏栈上沿 ≈664」为基准，构图大改）记为**偏好项备选**，本件不做；
⑥ **取景器与成片视场不一致（2026-09-19 B1 复验新发现）**：24mm 拍照实测 ——
**照片内容比取景器多，多出的是左右两侧**。根因假设：预览层是全屏 `aspectFill`
（4:3 源放大到 874 高后裁掉左右各 ~126pt ≈ 全宽 39%），而成片是完整 4032×3024 帧；
遮幅窗（4:3 → 402×536）本应是"所见即所得"窗口，窗内却是裁切画面。
修法方向（待 WB 评估）：预览层按遮幅窗尺寸/比例做 fit（4:3 时窗与源同比例，可完全 WYSIWYG），
与"实拍裁切在 P2 落地"是同一批工作；注意别破坏"取景器铺满全屏"的设计意图；
⑦ **两个既有态在"安全区高"口径下低于 50%（2026-09-19 净可见账表查出）**：**场景·风格展开**
844 上 **49.3%**（底栏块 313pt）/ **滤镜条展开** 844 上 **42.8%**（362pt）—— 自检第 12 组⑩
打印 `注意` 但**不 FAIL**（不在 B2 范围）；要修得动 **147 / 144** 两个既有高度
（还牵着 `147.0 = 147.0` 恒等式与"风格名高钉死 13pt"）→ **需拍板，未动**；
⑧ **架构改造（物理设备 + 参数搬运 + 模糊转场）= `docs/18` 第二、三章**：虚拟多摄不支持全部手动参数
（SDK `AVCaptureDevice.h:538-541` 明文，正式记录见 Mac 快照段）→ B2/B3 手动档在现架构下永远置灰；
**用户已拍板按需切换**（自动态虚拟 / 进手动或跨切换点切物理）。排在 **B3a（EV 圆盘）→ B3b（对焦圆盘 UI）**
之后实施；实施前须先消化 **Mac 纸面预检 7 处**（回切策略 / 搬运顺序放 commit+格式之后 /
物理档位→zoom 映射表（现 `nativeZoomLadder` 吃虚拟切换点数组、物理设备下失效）/ EV 搬运 /
对焦档位语义 / 转场改顺序触发 / 自检 +3 条）—— 完整清单见 Mac 快照段；
⑨ **圆盘样式与交互约定（B3b 对焦盘落地时必须同步）**：两盘目前**不共用视图组件**
（仅 `EvDialGeometry` 预留 mirror 参数），建议 B3b 抽共享盘底/刻度环，别抄第二份。
三条最新口径（2026-09-19 用户拍板 / [mac-fix] 落地）：
① **半透明盘底**：0.78/0.84/0.88 → **0.45/0.50/0.55**（色相不变，刻度/数字/指针保持不透明层）；
② **字号按 400 坐标系缩放**：刻度数字 = `13 × size/400`（≈8pt@246，原型 SVG 文本口径；
EV 盘曾误写死 13pt、偏大 62%，已修）；
③ **拖拽 = 相对位移模型**（顺滑顺走 + 点击不改值）：指尖角位移 1:1 传给盘面（ΔRing = Δcss），
零位移松手不推值；⚠️ 该模型下**顺时针滑 = 值减小**（数字顺时针递增 + 盘面跟手，物理转盘语义）；
⑩ **对焦回读语义 bug（B3b 带出，2026-09-20 复验发现 · Mac 已最小修，等 WB 正源修）**：
`focusMode == .locked` 是"一次性 AF 完成后的**正常状态**"（SDK 明文：`.autoFocus` 对焦一次后
自动转 `.locked`），**不是**"用户锁了手动对焦"。回读直接赋值 → 点按对焦一次后 `isFocusManual`
永久为真 → 后续点按全走"仅测光、不动焦"（该路径不碰 focusMode → **永久死锁**）。
详见 Mac 快照段「B3b 复验记录」；⚠️ **通用教训（已记长期记忆）**：**硬件状态回读不能直接当用户
意图用** —— 先问"这个硬件值会不会在正常操作中自发出现"（**已全闭环 2026-09-20**：
Mac 最小修 `670970b` 复验 ✅ → WB 正源修 `0884879` 复验 ✅ —— `manualFocus` 改用户意图态、
硬件回读删除、自检 ⑱+M5 守卫）；
⑪ **物理架构改造复验带出 5 条（2026-09-20 · 详见 Mac 快照段「物理架构改造复验记录」）**：
🔴 换设备太慢（12~41 次格式探测 → 4.5~11.9s 糊屏；修法=按 (设备,模式) 缓存格式）/
🔴 刻度条初值守卫缺失（换设备后切手动初值取到 ISO 0 → 写进 ISO 54 / 1/71429s 极端曝光）/
🟠 刻度条回弹一档（拖动 0.75 滞后与松手 round 不同源）/
🟠 视频模式下 Live 判据恒 false（照片输出不挂载 → 41 候选跑满 9.1s）/
🟡 会话重启后物理档位对齐静默失败（「没有对应镜头，未推硬件」）；
另：跨镜头拒绝待补测 / 刻度条初值显示侧吸附；
⑫ **物理架构批二复验带出 7 条 + 2 需求（2026-09-20 · 详见 Mac 快照段「第二批复验记录」）**：
🔴 EV 重放杀手动曝光（4 次 WRN，违反 docs/18 §2.4；修法=手动档下不推 EV）/
🟠 刻度条「对应数值」（气泡显示非档位值 → 显示侧吸附）/
🟠 刻度条「灵敏度」（**拍板：跟手倍率 1.5:1，上限 2:1**）/
🟠 刻度条细密度（三条都改：WB 主 500K/细 100K；ISO、快门主档间加细刻度）/
🔴 自动/手动切换要瞬发（**A1 真瞬发 vs A2 极短转场 0.3~0.5s，用户倾向 A2，待 WB 确认**）/
🔴 切模式时参数保持（现会被重置为自动）/
🟡 松手同值重复写入（顺手修）；
⑬ **批三复验 + 用户补充 4 问题（2026-09-20 · 详见 Mac 快照段「批三复验记录 + 用户补充问题」）**：
⚠️ 🔴1 **未修完**（新设备遗留 `.custom` → 2 次 WRN；修法=归一新设备或判新设备实际 mode）/
❌ 🟠3 跟手倍率**方向写反**（`÷1.5` → 应 `×1.5`）/
🟠4 细刻度「外形没变化」（要按参考图重排**层级 + 密度**）/
🔴6 切模式参数保持**未验**（需手动档激活下复测）/
+ 用户 4 问题：刻度条仍不对（合并上述）/ 参数仍回自动 / **转场边界**（只切焦段有转场、其余无感、≤1s；建议预切换）/
**120mm 点按对焦无效**（关「自动对焦」锁定 0.00 + 仅测光无出路 → 需锁定值守卫 + 点按语义重新拍板）；
⑭ **批四复验带出 3 条（2026-09-20 夜 · 详见 Mac 快照段「批四复验记录」）**：
🔴 **静默路径退转场**（每会话首次冷探测 4.2~5.0s > 1.2s 静默超时 → 必然退转场；修法=**冷启动后台预热** +
换设备期间冻结上一帧；目标：只有点焦段有转场，其余人眼无感）/
🟠 刻度条**标点更细更多**（四档层级之上再加密，口径按参考图）/
🔴 手动模式下**切模式仍恢复自动档**（本场日志未覆盖 → 需自测/补测）；
顺带：首次冷探测 4.4~5.0s 建议与静默退转场一起修（冷启动预热）
⑭ **批四（2026-09-20 夜 · 已修完并 push，等 Mac 复验 · 详见 `docs/21`）**：批三 6 条**全部落地** ——
✅ 实锤 A（换设备 `normalizeToAutoLocked` 归一新设备 → 再按快照重放；EV 推前二次判定）/
✅ 实锤 B（跟手倍率改**乘** `translation.width * dragGain`）/ ✅ 问题 1（刻度条**四档层级**
minor10/mid15/major22/preset22 + 快门半档 mid 线；**松手草稿保留到硬件回读同档**治回弹）/
✅ 问题 2（切模式**一律按快照重放** + 「切模式后档位复查」日志）/ ✅ 问题 3（**静默换形态**
`applyFocalTargetSilently`：预切换 + 回切均不显示转场；入口动作改挂 `form` 边沿 + 1.2s 转场兜底）/
✅ 问题 4①（对焦锁定值有效性守卫：0.00 端点不当"当前值"）+ ✅ 问题 4②（**用户拍板方案 A 并已实施**：
点按取景器 = 重新对焦 + 解除手动档；**对焦盘开着时算误触、只测光不解除**）；
自检 ⑲ n 段 15 条 + ⑱ h 改口径（方案 A）+ 变异 **22/22**（对照组绿）
⑮ **backlog（2026-09-20 记，不现在修）**：
① **冷启动后台预热**（用户批四点名，批五**未做**）：唯一不打扰预览的做法是私有 `AVCaptureSession`
   逐颗探物理镜头，但**同一颗物理设备只能属于一个会话** —— 直播会话挂着虚拟多摄（其 constituent 就是
   那三颗镜头），私有会话去挂极可能触发 `.videoDeviceInUseByAnotherClient` → **预览黑屏**。
   本机无法验证，按"不能影响正常使用"**宁可不做**；**等 Mac 确认安全再补**（`docs/22` 一②）；
② 静默换形态**省"模糊"不省"耗时"**：换 input 固有 0.6~0.75s，每设备**首次**冷探测 4~5s
   （批五已让它不再被降级成转场，并打 `静默换形态耗时 X.XXs`）；
③ 预切换期间（刻度条展开中）点**跨镜头焦段**会走换设备+转场（虚拟下本是平滑 ramp），
   关闭参数界面 2s 后回切虚拟即恢复；
④ 刻度**细分线不吸附**（纯视觉细分；吸附永远落在真实档位）；
⑤ `docs/20` 旧名 `applyFocalTarget` 与代码漂移（代码已统一为 `performFocalSwitch` /
   `beginFocalSwitch` / `commitFocalSwitch` / `applyFocalTargetSilently`），下次动那份文档时顺手改；
⑥ **取景器双指缩放**（pinch 连续变焦 —— 当前变焦入口只有焦段档位）；
⑯ **批五复验 + 用户 4 问题（2026-09-21 · 详见 Mac 快照段「批五复验记录 + 用户 4 问题」）**：
🟠 刻度条**改行业主流参数 + 复刻飓风精度**（ISO 只差 12000→12800；快门 15 整档 → 加 1/3 档 ≈40 档、slot 压到 18~20pt；
⚠️ 需 CB 同步原型否则 `check_presets` 第 6 组红；精度依据需清晰参考图）/
🔴 **刚进 App 点对焦 → 首次冷探测 4.27s 静止帧**（用户口径：不该有转场、不能慢/卡；修法=冷启动预热，Mac 可真机验证）/
🔴 **核心口径「不选自动就永远手动」**：点按对焦顶掉手动曝光（`setFocusAndExposurePoint` 设回自动）→ 按意图重放 /
🟠 取景器掉帧（候选=静默切换 input 重挂；待用户补场景）
⑰ **ISO 与快门无法「自由搭配」（2026-09-22 用户新问题 · 定位已完成 · ✅ 拍板 A「不做」· 已结案）**：现象 = 一方手动另一方也手动（自动同）。
**定位结论：绑定在三层，根因在系统 API** —— `AVCaptureDevice.setExposureModeCustom(duration:iso:)` **一次接管两者**，
`exposureMode` 只有 `.custom`（两轴都手动）/ `.continuousAutoExposure`（两轴都自动），**没有「ISO 手动 + 快门自动」这种档位**
（本项目 `CaptureDeviceConfigurator.swift` :332-337 的注释早就写明这是硬件根源，并点名原型 `state.auto.isoShutter` 是**一个共享开关**）。
我们这侧把这条硬约束**结构化了四遍**：① 写入层 `setManualExposure(iso:seconds:)` 成对写 / `setAutoExposure` 成对放；
② 状态层 `session.manualExposure: (iso,seconds)?`（nil = 全自动，非 nil = 两轴手动）+ `manualExposure(of:)` 以 `exposureMode == .custom` 为唯一判据；
③ 模型层 `CapturePreset.isManualExposure` 要求两者都非 nil，`hasPartialManualExposure` 把「只填一个」定义为**非法态**；
④ UI 层 `isISOShutterAuto` **一个布尔**驱动两条刻度条 ⇒ 表现就是「一动全动」。
**要「自由搭配」只能软件模拟**（系统不给档位）：以 `.custom` 为底，**钉住一轴、持续重算另一轴**去追测光（「ISO 手动 + 快门自动」= 钉 ISO、重算 duration；反向同理）。
影响面：`CaptureDeviceConfigurator`（半自动写入 + 反馈回路）· `CaptureSessionController`（意图态从 1 个元组拆成两轴各自意图；
**回读真值不再等价** —— `.custom` 不再等于「两轴都手动」）· `CameraViewModel` + `ParameterStripView`（两条刻度条各自的自动/手动入口、草稿、跟手）· `CapturePreset`（半手动从「非法」改「合法」）·
且要重新对齐三处既有口径：**EV 与手动曝光互斥**（半自动下 EV 是否生效？）· **点按重放 ③**（快照 1 个拆 2 个）· **回读对齐**（`$manualExposure` 订阅）。
风险（诚实）：① 与系统 AE **抢方向盘** → 抖动/闪烁，快速光变场景不如原生；② 每次重算都要 lock 写设备（代价 + 可能影响帧率）；③ 自动那一轴数值会持续跳，UI 显示口径要重新定义；④ 复杂度高（新状态机 + 反馈回路 + 两套 UI 入口）。
**✅ 拍板（2026-09-22 用户）：A 不做 —— 本条结案，零代码改动**（只落文档）。
**依据（Mac SDK 取证已把「系统不支持」钉死 · 2026-09-22）**：
① **单轴 setter 不存在** —— 没有「单独设 `exposureDuration`」或「单独设 `iso`」的 API；
② `exposureMode` **只有 4 档、无半自动档**（`.custom` 一次接管两轴）；
③ 要「自由搭配」**只能软件闭环**（以 `.custom` 为底、钉住一轴 + 持续重算另一轴去追测光）—— 代价大、风险高，四条都不划算：
与系统 AE **抢方向盘**（抖动/闪烁，快速光变不如原生）· 每次重算都要 **lock 写设备**（帧率成本）· 自动那轴数值**持续跳**（UI 显示口径要重定）·
复杂度高（新状态机 + 反馈回路 + 两套 UI 入口）；影响面 **4 层**（configurator / controller / VM+UI / preset）+ 要重新对齐**三处既有口径**
（EV 与手动曝光互斥 / 点按重放 ③ 快照拆两份 / 回读对齐 `$manualExposure`）。
⇒ **现状 = 系统上限**：一方手动另一方必手动、`isISOShutterAuto` 一个布尔驱动两条刻度条，是**正确实现，不是缺陷**。
**📎 SDK 原文留档（本项目既有设计的头文件级依据，别再重复踩）**：
- **虚拟多摄原生不支持 `.custom`**（`AVCaptureDevice.h:538-541`）→ 即「进手动前先切物理单摄」的 SDK 依据（`docs/20` 物理架构）；
- **官方明写「即便已锁定，换镜头时 `duration` / `ISO` 仍可能被系统改掉」** → 这就是「转场切镜头 → 手动档被清」的**头文件级解释**
  ⇒ **App 侧必须自己重放**（批五 `reverifyManualIntent` 四拍 / 批六 ③ 点按意图快照重放，都是为此而设）；
- **EV 是第三轴、只有一条写入口 `setExposureTargetBias`**（且只在自动曝光档生效）—— 与手动曝光互斥的根因。

**Mac 环境备忘（新会话必读）**：
- **真机截图用 `pymobiledevice3 developer dvt screenshot out.png`**（PATH 加 `$HOME/Library/Python/3.9/bin`；已 pip3 install --user 11.15.1，自动走原生隧道无需 sudo）。`idevicescreenshot` 在 iOS 26 确定性损坏（libimobiledevice issue #1465），重启/重插无效，别再试
- 设备偶发 `Device is busy (Waiting to reconnect)`（build 直接 EXIT=70 不编译）与 `Developer Mode is disabled` 假象 —— 都是连接抖动，重试即可；`ddiServicesAvailable: true` 才是真状态
- 装机：`devicectl device install app --device 81FD8131-… /tmp/lumen-dd/Build/Products/Debug-iphoneos/LumenEdit.app`；日志：`devicectl device copy from --domain-type appDataContainer --domain-identifier com.wokenday77.lumenedit --source Documents/lumen-debug.log`
- 签名：Team `H7XJ2PBZJG`（Personal，证书 **7 天过期**，过期重装后在 设置→通用→VPN与设备管理 重新信任）
- 相册测试素材**待清**（唯一不可逆物）：3 段视频（约 202/253/312MB）+ 4 张照片
- zsh 坑：`git show 'abc~1':path` 的 `^`/`~` 要加引号；变量不自分词（curl 参数别走变量拼）

### 关键决策（已拍板，别返工）
1. **选场景不推硬件 EV / 白平衡**：只进提示条，守 A 组边界，B 组统一接硬件（用户 2026-09-17 拍板）
2. **选风格 = 滤镜重置为「无」**：原型注释说两层胶片叠加出脏色（单向；选滤镜不碰风格）
3. **风格卡缩略图与快门排风格方块共用 `StyleThumbnailView`**：取该风格引用滤镜的 `swatches` 三色渐变；纯参数风格中性灰兜底（P4 接渲染只改这一处）
4. **状态 `sceneId / styleId / filterId` 落盘** UserDefaults（键 `lumen.camera.*`）；「保留设置」开关属 #12 未做
5. **展开态内容深度恒等式 147.0 = 147.0 必须保持**：风格名字高钉死 13pt 令牌，不交给字体度量浮动（否则 1pt 账漂）
6. **字号上调理由统一**：模式条/焦段条/场景·风格块标题与徽标都因 HIG 命中下限上调过（11 / 10）
7. **架构方向 = A（物理设备）+ 手动模式下不锁焦段**（2026-09-19 用户拍板）：虚拟多摄不支持全部手动参数
   （SDK `AVCaptureDevice.h:538-541`）→ 按焦段挂物理设备 + 参数搬运 + 「模糊→清晰」转场（方案 B）；
   施工图 = `docs/18` 第二、三章，排在 B3 之后实施（同时重做 B1 变焦、打开 B2 手动档）

### 踩过的坑（Swift 端 + 协作侧）
- **`focal` 被误删导致编译失败（`7d73f70`）**：一次编辑本意新增却替换掉已存在的 `@Published var focal`。→ 新增自检**状态引用完整性**（viewModel.*/env.* 每项必须能在状态类找到，缺失即 FAIL）
- **`storageText` 重复声明导致编译失败（#11 引入，Mac 侧 `156abd9` 修）**：新增字段前没发现 `TopBarView:39` 已有同名 `storageText`，又在 62 行加了一份。**与 `focal` 误删是同一类坑的镜像**（一个删了已有的、一个加了重复的）——新增/改动 struct 成员前先 `grep -n "成员名"` 该文件
- **展开态内容深度溢出**：原型第 5 轮就在这里溢出过 39px；Swift 端用恒等式 + 钉死行高防复发
- **模式条宽度预算**：容器自身 spacing/padding 也在账里（顶栏主行曾因 `HStack(spacing:8)` 把中段从 224 吃到 208，393/390 机型当场溢出）；改几何前提同一批算式要重算
- **中文字宽可按 1.0em 推，拉丁/数字串必须实测**（「120 mm」10.5pt 实测 40.0pt，0.55em 粗估窄 25%）
- **push 通道（2026-09-17 踩两次）**：直连 GitHub 永不通；沙箱 env 代理会坏（报 502）；走用户加速器 `-c http.proxy=http://127.0.0.1:7892`，**不要 --global**（端口动态）；推送前先 `git fetch`
- **提交纪律**：禁 `git add -A`（原型正被 CB 改，会夹带）；按路径明确 add 自己改的文件
- **命名空间坑**：颜色是 `Theme.Palette`（不是 `Theme.Color`），用前先 `grep -n "enum Palette"` 确认外层容器名
- 🔴 **`let` 带默认值的属性不进 memberwise 初始化器**（2026-09-19 Mac 编译实测抓到，`[mac-fix] 7bd53b0`）：
  `let isSwiftExtension: Bool = false` 会让 `FocalPreset(..., isSwiftExtension: true)` 报
  **"extra argument"**（`let` 被当成"已初始化"、从合成初始化器里排除）；改成 `var` 才行。
  → **凡是"给 struct 加一个带默认值的配置字段、又想在某处显式传它"，一律用 `var`。**
  已做成回归守卫（自检 ⑭ 要求 `var`；变异测试 M18 把 var 改回 let 会 FAIL）
- 🔴 **同一个映射来源不要写两处**（2026-09-19 焦段映射那次）：`48mm 档 = 主摄 × 2` 这种"写死的倍数"
  一旦出现第二档（35mm），就必然只改一处。改成"主摄 × 标称比例（`mm / 24`）"**一条式子**，
  两档共用 —— 48 的值一分不变，却再也不会漂。**判据：看到"同类的第 2 个实例"就抽式子。**
- 🔴 **能力探测 API 也要选对 —— "支持模式" ≠ "支持自定义值"**（2026-09-19 白平衡 7 连崩）：
  `isWhiteBalanceModeSupported(.locked)` / `isFocusModeSupported(.locked)` 在虚拟多摄上**误报 true**，
  而SDK 明文（`AVCaptureDevice.h:538-541`）虚拟设备只允许锁 Current —— 写计算值直接抛 ObjC 异常
  （Swift 拦不住）。正确探测：`isLockingWhiteBalanceWithCustomDeviceGainsSupported`（:1601）、
  `isLockingFocusWithCustomLensPositionSupported`（:1110）。
  自检第 12 组⑯守着三处方法体（含负向断言"不得再出现错 API"）。
  → **判据：问能力的 API 要与"将要执行的那次写入"逐字对应，不要用同族的近似 API 替代。**
- 🔴 **检查脚本之间也会打架**：一条守卫的语义若跨两个文件（如"扩展标记该不该在"要同时读 Swift
  与原型），**不能放在只能读一边的脚本里** —— 另一边把数据补齐后，它会把**健康状态**判成 FAIL。
  本轮实例：`check_swift` ⑭ 原本要求"至少一处扩展标记"，CB 补完原型后必 FAIL。
  → **分工**：只读一边的放单侧脚本，需要两侧的放交叉校验脚本（`check_presets` 那种两边都能读的）

### 下一步（2026-09-22 上午 · 最新口径）
1. ✅ **已完成**：A 组收官 / B1 / B2 全件 / B3a / B3b + 正源修 / 物理架构改造 / **批五 3 条**（`docs/22`）/
   **批六 ② 收尾**（候选 2 快路径，`docs/23`）；守卫 **o1/o3/o4/o5/o7/o8** + **变异 17/17**；`check_presets` **全过**
2. ✅ **批六 ①③④ 专项已完成（`ee1953c` 真机 · 2026-09-22 中午）**：① `10000K` 实测**红线未命中**
   （与左邻净间距 4.33pt；真约束 `40−(w左+w右)/2` ⇒ 最坏 4pt）/ ③ 点按重放 4 次全过（曝光保持手动）/
   ④ **306 次连切 0 告警**（input 40/39 · output 307/306 配平 · 0 打断 · 中位 0.30s）；验收设施已删 ✓
3. ✅ **6-①「手动档又回自动」已不再复现**（用户 2026-09-22 中午确认；原触发 = **转场镜头切换**，与判定表
   **首行吻合**：打断清档随删预热一起消失）→ 若再出现，按 `docs/23` 8.5 抓一轮（**先定性别先写码**）
4. **既有项（已定性：旧构建同样有）**：掉帧/内存爬升 = **每次切模式重配 outputs 的抖动** →
   **收尾后单独立项**；raw3 / 卡死黑屏 = ② 引入（已随删预热消失）
5. **观察项（已判"口径差异、不修"）**：打断恰落在最后一个成功候选上时仍写缓存（写的是**验证过 Live** 的候选 +
   Live 自愈兜底）→ 要严格口径得先设计"探测生命周期"语义 → 单独立项（`docs/23` 8.6）
6. **Backlog（未动，等拍板）**：⑥ 取景器与成片视场不一致 / **⑦ 两个既有态 < 50%**（场景·风格 49.3% /
   滤镜条 42.8%，要动 147 / 144）/ 取景器双指缩放 / B4 格式选择器接线 / B5 参数导入（等 P4）
7. 相册测试素材待清；⚠️ **判远端只认 `git ls-remote`**（ref 静默丢失 + `FETCH_HEAD` 过期都遇到过）

---

## 一、产品定位（最新口径）

自用 iOS App，**拍照时就能选好场景和风格，取景框实时看到效果，按快门直接出片。修图是附加功能。**

- 界面严格对标**飓风相机**：全屏取景 + 浮层式布局 + 底部图标行 + 焦段条
- 模式条四个入口：**照片 / 实况 / Log 实况 / 视频**
  - 实况 = 苹果原生 Live Photo（Swift 端 P1b-1 已做）
  - Log 实况 = 拍 Log 视频套 LUT 导出为实况照片（**后续做，先只留界面入口**，置灰）
- 界面文字一律用「**实况**」，与苹果系统叫法对齐；**代码标识符 `livePhoto` / `live` 保留不动**

## 二、用户环境（别再假设错）

- **没有 Mac**，Swift 代码从未编译过。"能编译"是假设，不是事实
- 用户**不懂 Swift 开发**：说清楚"怎么运行、怎么验证"，不要默认用户懂 Xcode
- Windows + 本地 AI 编码；Mac 是租的或借的；App 自用不上架
- 大体积工具链安装用户不接受（Swift for Windows 约 1GB 已被取消；浏览器自动化下载也被转入后台/跳过）

## 三、当前任务与完成状态

**任务：严格对标飓风相机调整网页原型（prototype/index.html）。已完成，自检 7 项全过。**

本次落地的决策：

| # | 决策 | 说明 |
|---|---|---|
| 1 | 模式条改为**四个文字标签** | 照片/实况/Log 实况/视频，替代原"单胶囊循环切换"。置灰入口带纯 CSS 挂锁（`.lk`），点击 toast 说明原因——不做"点了没反应" |
| 2 | 实况角标 | 实况模式下取景器顶部中央显示黄色「实况」小标（`.live-badge`），对标系统相机 LIVE 黄标 |
| 3 | 新增**焦段条** | 13 / 24 / 48 / 120mm（`.row-focal` 44px 常驻，数据 `FOCALS`），默认 24mm 选中；点击切换 + toast「镜头切换与变焦将在 P2 接硬件」 |
| 4 | 焦段条与扩展浮层互斥 | 场景风格条 / 滤镜条任一展开时焦段条自动收起（CSS `.screen.ss-on/.filter-on .row-focal{height:0px}`），守住 50% 底线 |
| 5 | 底部**图标行** | 参数收起态由"仪表条"改为图标行：前置切换 / 对焦 / 白平衡 / ISO / 快门 / EV（图标 + 当前值），对标飓风相机底部图标排；行内按钮 `stopPropagation` 后各自反馈 |
| 6 | 快门排右侧入口图标化 | "场景/风格"文字按钮改为色轮图标 + 小字"风格"（对位飓风相机 LUT 按钮位） |
| 7 | 顶栏 EV 胶囊移除 | 腾出模式条空间；**EV 归零改为点击参数排 EV 数值**（`#valEV` click），功能不丢失 |

布局预算（自检脚本实测）：常态 53.5% / 场景风格展开 53.0%（最差）/ 滤镜展开 57.9% / 参数收起 60.8% / 简易 65.1%，全部 ≥ 50%。

### 第二轮：按用户实拍参考图做「严格对标」（2026-09-15 晚）

用户提供飓风相机运行时的实拍截图，要求界面严格对标。逐项落地：

| # | 参考图元素 | 落地方式 |
|---|---|---|
| 1 | 顶栏是**两行** | `.topbar` 44px → **56px**（主行 30 + 副行 26）。顶栏高度改为从 CSS 读取，自检脚本不再写死 `30+44` |
| 2 | 左上 L / R 音频电平表 | 新增 `.levels`：8 格点阵，`.on` 绿 / `.hot` 琥珀；`buildLevels()` 静态示意（录音时由 AVAudioRecorder 驱动） |
| 3 | 主行右侧三图标 | 闪光灯 / 网格 / 更多（`.tb-icon`）。网格真的可开关（`.screen.grid-off`） |
| 4 | 副行「影调预览」+「31 GB」 | 两个 `.chip`；剩余存储真机读 `DeviceStorage`，原型占位 |
| 5 | 模式条选中态 | **改掉强调色**：选中 = 白色加粗，未选中 = 半透明灰。四入口与顺序不变 |
| 5b | 「实况」这一档改图标 | 按用户要求换成 **Live Photo 同心圆图标**（外圈 12 点阵 + 中环 + 实心内点，`ICON_LIVE`），其余三档仍是文字。图标用 `currentColor`，选中自动转白 |
| 6 | 焦段药丸 | `.focal-pill` 44×30 深色圆角块 = 刻度标记（7 根竖线）+「13 mm」；**选中转白底黑字**（原来只是一个文字胶囊） |
| 7 | 底部图标行 **7 项** | 前置 / 对焦 / 白平衡 / 感光 / 快门速度 / 曝光补偿 / 设置。形态统一为「图标 + 中文小字」，**不再在行内重复显示数值**（值在参数排平铺态与 HUD 里看） |
| 8 | 快门排四件套 | 相册缩略图 · 快门 · **镜头切换小圆键** · **风格预览方块（彩色描边）**；风格方块点击 = 展开场景/风格，缩略图与它共用同一套渲染 |

其他连带改动：

- 原来的「简易 / HUD / 设置」三个顶栏胶囊收进 **「更多」下拉菜单**（`.more-menu`），点别处自动收起。
- 参数排收起态不再"点整行展开"——只有点「曝光补偿」才展开滑块。
- 实况角标与「更多」菜单的 `top` 改为 90px，避开新的两行顶栏。

布局预算（第二轮实测，顶栏变高后）：常态 **52.1%** / 场景风格展开 **51.6%（最差）** / 滤镜展开 56.4% / 参数收起 59.3% / 简易 63.7%，全部 ≥ 50%。

### 第三轮：对焦圆盘（2026-09-16 定稿 · **参数已被第四轮取代，见下**）

点底部图标行「对焦」展开的**模态浮层**。完整规范见 `docs/03` 第 3.6 节。

| 项 | 定稿值 |
|---|---|
| 直径 | **280px** —— `--fd-size`，定义在 `:root`，唯一可调旋钮 |
| 位置 | 水平**居中**；自屏幕底向上：安全区 18 → 开关行 29 → 间距 12 → 圆盘 280 |
| 刻度弧 | **右半圈**，0.0 正下 → 0.5 正右 → 1.0 正上，`focusAngle(v) = 180 − 180v` |
| 刻度 | 51 格三层（主 11 / 中 20 / 副 20），高亮段 `#00E08A` 单独一层 + 外发光 |
| 数字 | 0.0~1.0 共 11 个，径向排布，`FD_NUM_FLIP = 180` —— **顶部数字是倒的，这是径向排布的正常表现** |
| 中心图标 | 「◎ 对焦」在圆盘**几何正中心** |
| 数值框 | 圆盘**内**右侧，偏移 `calc(var(--fd-size) * 0.20)`（按比例，不用死值，否则圆盘改尺寸会压住 0.5 数字） |
| 打开时 | 底部浮层栈**整条收起**，只剩「自动对焦」开关行；出口 = 点屏幕任意处关闭 |
| 自检底线 | 直径 ≤ 屏内宽 372px；圆盘态净可见 ≥ **30%**（模态专用，与常驻浮层的 50% 分开） |

**为什么不是参考图的尺寸**：参考图里**圆心贴在屏幕左边缘、左半圆被裁掉**（此前"占屏高 60%"的口径是错的，已纠正）。「完整露出」下若数值框还要在盘外，上限只有 224px。**280px 居中**曾是折中。

> ⚠️ **本节里的参考图数字（直径约屏宽 58%、占屏高约 23%）于 2026-09-17 被推翻**，见下方「第四轮」。真实值是**直径约屏宽 75%、圆心在屏高 83%**。

**两个必须记住的坑**

1. **半径脱节** —— 曾出现"圆盘 340px、刻度半径却是 224px"，刻度全跑到盘外、还被底部 UI 压住。现在所有半径写在 SVG 的 **400 坐标系**里、随 `viewBox` 等比缩放，改 `--fd-size` 一处即可，结构上不会再脱节。
2. **角度基准混用** —— CSS `rotate` 的 0° 在"上"、`cos/sin` 的 0° 在"右"，差 90°，混用必然错位。现在刻度 / 数字 / 高亮段 / 指针 / 拖动反解**全部只吃 `focusAngle()` 一个函数**。

**验证手段（本轮沉淀，可复用）**：没有 Mac、也不装浏览器自动化，用**本机 Edge 无头模式**截图，零安装：

```bash
msedge --headless=new --disable-gpu --hide-scrollbars --window-size=430,900 \
       --virtual-time-budget=3000 --screenshot=out.png "http://127.0.0.1:8123/index.html"
```

要截"某个状态"（如圆盘打开）时：复制一份 HTML、在 `</body>` 前注入一句自动点击、截完删掉副本 —— **不动正式文件**。

### 第四轮：圆盘圆心钉在「对焦」按钮上（2026-09-17）

用户要求：圆心 **X 和 Y 都**对齐底部「对焦」按钮中心 —— "以按钮为中心展开"，不是"贴屏幕底往上排"。

**实测基准**（浏览器真实渲染，`tools/shot.js`）

| 项 | 值 |
|---|---|
| 「对焦」按钮中心 | **(83.1, 662)** = 22.3%W · **80.1%H** |
| 按钮尺寸 | 51.4 × 42 |
| 按钮 Y 在参数排展开/收起下 | 都是 **662**，不随状态漂移 |

按钮中心 Y 可纯从 CSS 推导：`826 −(安全区 18 + 快门 80 + 焦段 44)− 图标行 44/2 = 662`
（图标行正好填满"参数排收起态"那 44px）。

**定稿值**

| 变量 | 值 | 说明 |
|---|---|---|
| `--fd-size` | **246px**（原 228） | 246 是"圆盘组含开关行仍留在屏内"的上限 |
| `--fd-center-x` | **83.1px**（原 82.5） | 按钮中心 X |
| `--fd-center-y` | **662px**（新增） | 按钮中心 Y |
| `--fd-bottom` | **退役** | 定位从"贴底"改成"钉圆心" |

定位方式：`.fd-wrap` 由 `bottom:var(--fd-bottom)` 改为
`top:calc(var(--fd-center-y) - var(--fd-size)/2)` —— 圆心恒在 (83.1, 662)。
**用 bottom 定位的话，改直径会让圆心上下漂**，钉不住按钮。

**落地结果**（自检 + 浏览器双测一致）

- 圆心 (83.1, 662)，与按钮中心**误差 0px**
- 圆盘 539..785（下沿占屏高 95%）· 左裁 39.9px（占屏宽 10.7%）
- 「自动对焦」开关行 797..826 —— **余量 0px**，贴屏底但未被切（`.sw` 本体就是 29px）
- 圆盘态净可见 54.8%（底线 30%）

**自检规则变动**

| 规则 | 变动 |
|---|---|
| A · 圆心 X ≈ 按钮中心 X（±1px） | 保留 |
| **E · 圆心 Y ≈ 按钮中心 Y（±1px）** | **新增** —— 按钮中心 Y 从 CSS 推导，不硬编码 |
| D · 下沿不得越屏底 | **改口径** → 「圆盘组下沿（圆盘 + 间距 + 开关行）不得越过屏底」。圆心钉住后先被顶出去的是开关行，只量圆盘下沿拦不住 |

**参考帧真实几何（推翻第三轮"58% 屏宽"的口径）**

量的是 `reference/video-frames-2026-09-16/frames/f012_t012.06s.jpg`：

| 项 | 参考图 | 我们 |
|---|---|---|
| 圆心 | 83.3%H | 80.1%H（钉在按钮上，差 26px） |
| 直径 | 75%W | 66%W |
| 左裁 | 16%W | 11%W |

测量可信度：f008–f016 连续 9 帧数值框位置只差 1–2px（确认是稳定态、不是动画中途）；
几何由"轮毂图标 + 沿圆心行/列像素扫描"两条独立路径互验，并把拟合圆叠回帧上肉眼确认落在盘缘。
对比图：`python tools/compare_dial.py` → `shots/compare-dial.png`。

### 第五轮：浮层两处真 bug + 设置页收口（2026-09-17）

#### A. 两处真 bug（截图取证时撞出来的，不是改坏的）

| # | 缺陷 | 根因 | 修法 |
|---|---|---|---|
| 1 | **滤镜条在鼠标下完全打不开** | 拖拽的 `pointerup` 之后浏览器还会补发一个 `click`，被取景器的「点按对焦 + 收起浮层」处理器吃掉 → 刚呼出的浮层立刻被 `collapseAll()` | `bindSwipe` 识别成滑动时记 `lastSwipeAt`；取景器 click 处理器开头 `if (Date.now() - lastSwipeAt < 350) return;` |
| 2 | **场景·风格展开态内容溢出 39px**，压在下面 7 图标行上 | 行高 144 只够 108px 内容，而 `ss-full` 实高 147px。**自检只校验行高，没校验内容深度**，所以一直没报 | 见下 |

Bug 1 现场证据（同一段手势、两种触发方式）：

```
真实鼠标拖拽后     : screen                          ← 修复前：什么都没开
合成事件（无 click）: screen filter-on params-closed  ← 手势逻辑本身是对的
```

- 真机触摸拖动一般不补 click，所以这是**原型专属**缺陷；但原型就是拿鼠标验收的，必须修
- 修完附带好处：拖拽结束时不再误弹对焦黄框与「对焦锁定」小标

Bug 2 的连带决策：**展开时收起折叠胶囊**（`.screen.ss-on .ss-collapsed{display:none}`），
行高 144 → **147**。

| 方案 | 行高 | 场景风格展开态净可见 |
|---|---|---|
| 原来（含胶囊，且溢出） | 144（内容 183） | 溢出 39px ✗ |
| 含胶囊、不溢出 | 183 | 50.2% ⚠️ 余量仅 2px |
| **定稿：收起胶囊** | **147** | **54.6%** ✓ |

胶囊是重复信息（块标题右侧已显示 `#scenePicked` / `#stylePicked`）；
收起后的三条退路：下划 / 点取景器任意处 / 再点底部右侧的「风格」方块。

#### B. 设置页收口

- **B-1 入口不一致（真 bug）**：底部图标行第 7 项「设置」原来只弹「将在 P2 补齐」，
  而顶栏「更多 → 设置」能真打开设置页 —— 同一功能两套行为。现已统一到 `openSettings()`
- **B-2 开关联动**：设置页 10 行原来只有「简易模式」是真联动，其余只改自己的开关状态。现在：

| 设置项 | 处理 |
|---|---|
| 简易模式 | 已联动（原有） |
| **预览网格** | 真接上 —— 与顶栏那个网格图标**共用 `layout.gridOn`**，改一处两边都变 |
| **存储空间提示** | 真接上 —— 控制顶栏副行剩余存储胶囊显隐（`.screen.no-storage .chip-storage`） |
| **水平指示器** | **新增真功能** —— 取景器中央一条水平线（`.level-line`，恒为水平态；真机由 CoreMotion 驱动偏转变色） |
| 曝光参考 / 前置镜像 / 录制保存地理位置 / 二维码扫描 / 稳定器 | **只存状态 + toast 说明** —— 要么是 P2 硬件、要么超出原型范围，原型上做出可见效果反而误导 |

  `openSettings()` 里会把 `stState.simple/grid/storage/level` 从 `layout` 同步一遍，
  否则会出现"设置里显示关、拍摄页其实开着"（预览网格默认就是开的）。

- **B-3「保留设置」二级页**：已建（6 项：简易模式 / 场景与曝光 / 风格 / 滤镜 / 曝光补偿 EV / 预览网格）

#### C. 新增/升级的工具与规则

- `tools/check_prototype.js` 新增规则：**「展开态内容不得溢出其行高」**（纯静态算：
  2×(块边框 .5 + 块上内边距 2 + 块标题 14) + 场景胶囊 28 + 风格卡 84.55 = 145.55 ≤ 147）
  —— 这条规则就是冲着 Bug 2 来的，只校验行高拦不住
- `tools/shot.js` 升级：出 8 个状态截图 + 记录 `screen` 的 class / 底部各行实测高度与透明度
  （用于抓"开关开了但界面没变""某条 CSS 覆盖没生效"这类推算看不见的问题）
- `tools/montage.py` 新增：任意张截图等高等宽并排 + 标题，一键出审阅总览图

**本轮布局预算（顶栏变高、胶囊收起后）**：常态 55.4% / 场景风格展开 **54.6%（最差）** /
滤镜展开 59.8% / 参数收起 62.7% / 简易 67.1% —— 全部 ≥ 50%，且最差状态余量充足。
圆盘态 54.8%（底线 30%）。

### 第六轮：影调预览开关 + 持久化 + 简易模式可配（2026-09-17）

#### 1. 「影调预览」变成真开关（原为只弹提示）
- 顶栏副行的「影调预览」胶囊：**开 = 琥珀高亮**（取景器实时叠上风格与滤镜）、**关 = 灰**（看原片）
- 实现关键：`renderPreview()` 里只摘"影调"两层（`#grade` 滤镜链 + `#tints` 叠加层），
  **`#exposure`（EV）那层不动** —— EV 是拍摄曝光，不是后期调色
- 左下缩略图**不跟着关**：它预览的是"成片"（会被保存成什么），与取景器预览是两回事
- 设置页「拍摄」组同步加了一行「影调预览」，与顶栏胶囊共用 `layout.tonePreview`
- HUD 增加一行「影调预览 开/关」
- ⚠️ 默认风格（柔暖人像）的调子很温和，开/关的视觉差异不明显；
  选一个强滤镜（如中高对比黑白）再开关，差异立刻可见

#### 2. 设置项持久化（localStorage）
三个 key：`lumenedit:settings`（设置开关）/ `lumenedit:keep`（保留设置）/ `lumenedit:shoot`（拍摄现场）。

- **设置页开关一律记住**：简易模式 / 预览网格 / 存储空间提示 / 水平指示器 / 影调预览 / 稳定器档位 / 简易模式保留项(sm-*)
- **「保留设置」只管拍摄现场四项**：场景与曝光(k1) / 风格(k2) / 滤镜(k3) / 曝光补偿EV(k4)。
  原来的 k1简易模式、k6预览网格 两行已并入"设置开关一律记住"，从该页移除（避免出现两个都管的重复开关）
- 落盘走 `scheduleSave()`（500ms 防抖），挂在 `applyLayout()` 与 `renderPreview()` 末尾 ——
  所有变更都会经过这两条路径
- ⚠️ **不挂 `beforeunload`**：挂了它，每次 reload 都会把当前内存状态写回 localStorage，
  会把"想清空重来"的场景抵消掉（截图脚本因此清不干净）。代价是改完 500ms 内直接关页会丢那一下，可接受
- ⚠️ 踩过的坑：`saveState` 把**未定义**的 `sm-*` 写成 `undefined ? 1 : 0 = 0`（不保留），
  而 sm-* 的默认应是"保留(1)"—— 下次加载就误把控件藏掉。修法：loadState 里缺省按 1 处理

#### 3. 简易模式可配
设置页新增「简易模式保留项」二级页，3 个开关：相册缩略图 / 场景条 / EV 横滑条（默认全保留）。
不保留的控件在简易模式下直接隐藏（`.screen.simple.no-sm-*`）。
风格方块未列入（简易模式下它本来就只弹提示、不可用）。

#### 4. 本轮验证（tools/shot.js 实测）
- 影调预览：开 `grade = contrast(0.96) saturate(0.88) sepia(0.12) brightness(1.02)` → 关 `none` ✓
- 简易模式三项全关：class = `simple no-sm-thumb no-sm-ss no-sm-ev`，缩略图不可见、场景条 0px、EV 条 0px ✓
- 持久化：reload 后 class = `simple grid-off no-storage level-on no-sm-*` 全部还原 ✓ 三个 key 均写入 ✓

### 第七轮：功能面板 + 视频格式指示（对标飓风相机，2026-09-17）

#### 1. ⠿ 功能面板（取代原「更多」下拉菜单）
同一个 ⠿ 不能有两个行为 → 原「更多」菜单（简易模式/阶段标记/HUD/设置）**整块移除**，4 项全部并入面板。

两行结构（用户定稿）：

```
第一行  实况 · 画幅比 · 闪光灯 · 倒计时      ← 拍摄高频
第二行  高亮增益(HDR) · 设置 · HUD · 阶段标记 ← 低频 + 调试
底部    简易模式 ›                           ← 简易模式下的唯一退出出口
```

状态规则：开关类开 = 绿高亮（`.fn-btn.on`），入口类（设置）灰 + 箭头，永不 зелёный。

真实度：**画幅比真实遮幅**（4:3/16:9/1:1 循环，取景器上下压黑边，`--rm-h` 由 JS 按比例算、260ms 过渡）、
**实况真实角标**（照片模式下开 → `#liveBadge` 显示）；闪光灯/倒计时/高亮增益只记状态（P2 接硬件）。

- 面板动画：弹出 300ms `cubic-bezier(.22,.9,.3,1)` + 8 按钮依次浮入（每个错 40ms）；收起 220ms
- 互斥：开面板会 `collapseAll()`；与格式选择器互斥；点取景器/别处自动收起
- ⚠️ 简易模式下**必须**能打开本面板 —— 底部链接是退出简易模式的唯一出口
- 状态持久化：`state.fn = {live, ratio, flash, timer, hdr}` 存 `LS_SHOOT`；HUD/阶段标记是调试功能，**不持久化**（会话内有效）

#### 2. 视频格式指示 + 选择器（视频 / Log 实况模式）
- 视频/Log 实况**本就是 enabled:true 可进入**（此前"置灰"的说法有误，无需解锁）
- 右上角三图标在视频/Log 模式下隐藏，原位显示格式芯片：
  视频 = `4K · 60`，Log 实况 = `Log · 4K · 60`（对标参考图）
- 点芯片弹选择器（右上角下拉）：分辨率 720p/1080p/4K + 帧率 24/30/60/120
- 剩余可录时长：`STORAGE_GB(31) × 8 × 1024 ÷ 码率 ÷ 60`；码率占位表 `FMT_BITRATE`（真机读 AVCaptureDevice）
  实测 4K·60 → ≈85 min、1080p·30 → ≈235 min，选完即刷新
- **时长胶囊在视频/Log 模式常显**：`.screen.mode-video .chip-storage{display:inline-flex}` 写在
  `.no-storage` 规则之后靠源码顺序取胜 —— 存储提示关掉也不能藏拍摄关键信息
- `applyLayout` 新增 `mode-video` / `mode-log` / `fn-on` / `fmt-on` 四个 class；`setMode` 现在会调 `applyLayout`

#### 3. 自检新增 2 条（第 8 组）
- 功能面板齐备：`#fnPanel` + 8 个 `.fn-btn`（5 功能 + 设置/HUD/阶段标记）+ `#fnSimple`，且旧 `more-menu` 必须已移除
- 格式芯片齐备：`#fmtChip` + `#fmtMenu`（3+4=7 个 `.fmt-opt`）+ `FMT_BITRATE` 12 值全为正

#### 4. 实测（tools/shot.js，浏览器真值）
- 面板：开 ✓ · HDR 绿 ✓ · 画幅比 16:9 ✓ · 遮幅 82px（=(826−372×16/9)/2，自洽）✓ · 实况角标 ✓
- 1:1 遮幅：黑边 227px（=(826−372)/2）✓ 收面板后一眼可见（`shots/11b-ratio-1-1.png`）
- 视频：芯片 `4K · 60` ✓ 三图标隐藏 ✓ 时长 `≈ 85 min · 31 GB` ✓ 胶囊常显 ✓ 实况角标正确隐藏 ✓
- 选 1080p/30：芯片 `1080p · 30` ✓ 时长 `≈ 235 min` ✓

### 第八轮：圆盘统一 v2 + EV 盘反向镜像（2026-09-17）

> 同一批未提交改动里还有**参数排三条刻度条**与 **⤢ 放大态（3B）**两轮，尚未写进本文件，看 `git diff` 为准。

#### 1. 组件统一：两盘共用一份代码
`DIALS` 表 + 单份 `dialBuild / dialRender / dialFromPoint`。交互口径 **「刻度动、指针不动」**：
指针钉在盘的外侧中腰不动，刻度环（刻度线 + 数字 + 绿色高亮段）整体旋转 `指针角 − fdAngle(cfg,v)`，
于是"当前档位"永远停在指针下。

#### 2. 反向镜像（本期定稿 —— 此前"两盘完全一致"的口径作废）

| 项 | 对焦盘 `mirror:false` | EV 盘 `mirror:true` |
|---|---|---|
| 圆心 X | `--fd-center-x` = 83.1（屏左） | `calc(372px − --fd-center-x)` = 288.9（屏右） |
| 露出 | 左半出屏（露右半） | **右半出屏**（露左半） |
| 数值框 | 盘右 `left:calc(50% + 半径 + 8px)` | **盘左** `right:calc(50% + 半径 + 8px)` |
| 指针 | 3 点 `rotate(90)` | **9 点** `rotate(270)` —— 由 `fdPointerDeg(cfg)` 给出 |
| 刻度弧 | 右半圈 | **左半圈** |
| 角度映射 | `fdAngle = 180 − 180v`（0 正下 → 中值正右 → 满值正上） | `fdAngle = 180 + 180v`（0 正下 → **中值正左** → 满值正上） |
| 拖拽有效弧 | `[0,180]` → `v=(180−css)/180`，其余就近吸两端 | `[180,360]` → `v=(css−180)/180`，其余就近吸两端 |

- 372 = 屏内宽（外壳 390 − 左右各 9）。EV 圆心写成 `calc` 而不是硬编码 —— 对焦盘中线以后要挪，EV 跟着镜像
- 行为不变：进盘收起底部浮层栈（模态）、两盘互斥、都在同一处 `--fd-center-y`

#### 3. 实测（tools/shot.js，浏览器真值）
- **圆心守恒 83.1 + 288.9 = 372** ✓（对焦盘 [−40,539,246] / EV 盘 [166,539,246]）
- **数值框守恒 214 + 158 = 372** ✓（对焦框左缘 / EV 框右缘）
- 指针 `rotate(90)` / `rotate(270)` ✓；同一 v 下环旋转反号（+10.80 / −9.90 = 镜像的直接表现）
- **拖拽手感**（版式镜像截图能看，手感必须真拖）：正下 `−3.0 EV` / 正左 `+0.0 EV` / 正上 `+3.0 EV` ✓；点数值框归零 ✓
- 出图：`shots/28-focus-new.png` · `shots/29-ev-mirrored.png` · `shots/30-pair.png`（并排核镜像）

#### 4. 自检改写（第 8 组参数排那条）
- `evMirror`：`.ev-wrap` 必须 `left:calc(372px − var(--fd-center-x))`、`.ev-val` 必须 `right:calc(50% + 半径 + 8px)`，且全文件不许出现镜像坐标硬编码
- `dialMirror`：`mirror:false` / `mirror:true` 两参数 + `cfg.mirror ? 180 + v * 180` + `cfg.mirror ? 270 : 90` + 环旋转式
- ⚠️ 硬编码守卫是**纯文本匹配**：注释里写那个禁用字面量也会被拦下（本轮被自己绊过一次，注释措辞已改）

### 第九轮：顶栏模式条居中（2026-09-17）

**问题**（用户报告）：模式条被左（L/R 电平表）右（三图标）挤在中间偏左；选中项「照片」太靠左。

**量测（浏览器真值，清 localStorage 的出厂常态，三态都量）**

| 元素 | 改前 | 改后 |
|---|---|---|
| 电平表块 | 14..64（宽 50） | 14..87（宽 73；点仍贴左，多出的是不可见空白） |
| 模式条盒 | 80..269（189，中心 174.5） | 103..269（166，中心 **186** ✓） |
| 模式条内容 | 80..219.6（中心 **149.8**，偏 −36.2） | 116.2..255.8（中心 **186**，偏 **0**） |
| 右上三图标 | 285..358（73） | 不变 |
| 四项盒间距 | 10 / 10 / 10 | 不变 |
| 四项中心间距 | 30.5 / 41.8 / 44.3 | 不变（图标项只有 18px 宽，属正常节奏） |

偏左 36.2 的账：**盒偏 11.5**（左块 50 vs 右块 73）**+ 盒内左对齐 24.7**（盒有 49.4 富余全堆在右边）。

**改法（A 方案 · 四处）**
1. `:root` 加 `--tb-side-w:73px`（= 右上三图标自然宽 3×17 + 2×11）
2. `.levels{min-width:var(--tb-side-w)}` —— 点左右对齐，多出的是空白，视觉零变化
3. `.fmt-chip{min-width:var(--tb-side-w)}` —— 视频模式芯片 60.4 → 73（胶囊略宽 12.6px）；Log 模式本就 91 不受影响
4. `.mode-tabs{justify-content:center}`

**口径与取舍**
- 照片 / 视频：**偏 0**（精确居中，两侧留白各 29.2 ✓）
- **Log 实况：偏 −9.1** —— 芯片「Log · 4K · 60」自然宽 91 > 73，把盒推左；**用户已确认接受**（不上"脱离文档流"的 C 方案）
- **图标间距刻意不动**：实测名义等距（盒间距 10/10/10、墨迹 10.3/10.4）；中心间距不齐来自图标项窄，改 CSS 不会变"更等距"——居中后再看观感
- `.mode-tabs` 居中的已知边界：内容溢出时居中两端不可滚达（现在内容 139.6 vs 行长 344，余量足）；真要溢出改用「内层包裹层 + margin:auto」

**自检 / 实测**
- 静态 +1 条：「模式条居中三件套」= `--tb-side-w` 存在 + `.levels`/`.fmt-chip` 都吃它 + `.mode-tabs` 有 `justify-content:center`
- `tools/shot.js`「模式条居中」：三态各量一次（照片/视频 ≤1px、Log ≤10px 才判过）
- 出图：`shots/32-tb-before.png` · `33-tb-after.png` · `34-tb-compare.png`（4x 放大叠对比）

### 第十轮：焦段条加 35mm 档（2026-09-19 · 原型补齐 Swift 先行扩展）

**背景**：Swift 侧先加了 35mm 档（`aac31d4`，`FocalCatalog.all` 5 档 + 该档标 `isSwiftExtension: true`）。
原型是档位数据的**真源** → 本轮把原型补齐，让比对自动收紧。方案落盘在 WB 的 `docs/17`（第十一节）。

**改了什么（5 处 + 2 条固化）**

| # | 位置 | 改动 |
|---|---|---|
| 1 | `prototype/index.html` `FOCALS` | 24 与 48 之间插 `{ id:'35', name:'35' }` —— **`on` 省略**（与 13/48 邻项同款；语义等价于 `on:null`，比对式是 `jf.on === true`） |
| 2 | 同文件 头注释 | 5 档语义：超广角(13) / 主摄(24) / **主摄数码裁切(35、48)** / 长焦(120)；注明与 Swift 对齐 |
| 3 | 同文件 3 处文案 | HTML 注释 / 帮助浮层 / 构建注释的 `13 / 24 / 48 / 120` → 加 35（顺带修正 HTML 注释里过时的"右对齐"→"居中对齐"） |
| 4 | `tools/check_prototype.js` | 档数断言 4 → 5 |
| 5 | 同文件 **新增宽度预算** | 药丸宽/间距/条内边距 **从 CSS 真读**：`档数×44 + (档数−1)×9 ≤ 372 − 2×14`。这是 `docs/17` 指出的原型侧盲区（Swift 侧同款是第 11 组 ⑫） |
| 6 | `tools/shot.js` | 「焦段条」运行时实测：档数 · 条内总宽 ≤ 可用宽 · 点 35 选中；出图 |

**两边比对口径（`check_presets.js` 第 4 组 · 选 B「声明式扩展」）—— 已实测三步时序**
1. 补原型前：`WARN 原型尚未同步以下档位：35`（Swift 已声明为扩展）
2. **补完原型后**（本轮的实测结果）：WARN 消失，改为 `FAIL 这些档位原型里已经有了，却还标着 isSwiftExtension: true：35 —— 请去掉标记` = **递给 WB 的信号** ✓
3. WB 去掉标记后：本组自动收紧为**逐档全等**（id / 显示名 / 默认标记 / 顺序），两边已一致 → 直接过

**转给 WB 的连带提醒**：`tools/check_swift.js` 第 11 组 ⑭ 硬要求"**至少一处**显式 `isSwiftExtension: true`"
（`extDeclared < 1` 直接 bad）。35 的标记一去掉这条会 FAIL —— 需同步改成"若存在先行扩展则必须显式声明"。

**实测（浏览器真值）**
- 出厂：档数 **5**（13/24/35/48/120 mm）· 条内总宽 **256** ≤ 可用 **344**（余 88）· 选中 `24 mm` · 内容中心 **186 = 屏中心** ✓
- 点第 3 颗：选中 `35 mm` ✓（toast「焦段 35mm · 镜头切换与变焦将在 P2 接硬件」）
- 自检：`焦段档位 = 5` ✓ · `焦段条宽度预算：5 档 = 256px ≤ 可用 344px（余 88px）` ✓ · **全部通过**
- 出图：`shots/35-focal-5.png`（整机）· `36-focal-zoom.png`（4x 放大）
- 顺带：参数导入 `焦段 35mm` 现在会吸附到 35（`presetSnap` 走 `FOCALS`，自动生效）

## 四、待验证（用户亲自做）

1. 打开 `prototype/index.html` 看整体观感 —— 本轮重点看**圆盘是否真的"长在"对焦按钮上**
2. **对焦圆盘**：点底部「对焦」图标展开 —— 圆心应**正好落在**那颗按钮的位置上（上下去比：
   展开前记住按钮在哪，展开后圆心就在那一点）；盘底离屏底只剩 **0px 余量**，看是否显得太挤
3. **横滑条在鼠标下能否拖动**：参数排 EV 竖滑块、简易模式 EV 横条、焦段药丸点击
   - 机制说明：与旧版已验证滑块同构（pointerdown/move/up + setPointerCapture），理论可用，但需人眼+手确认
3. 模式条点「Log 实况」「视频」→ 应出现置灰 + 原因 toast，不可切换
4. **更多菜单 → HUD** 打开，看各状态净可见取景高度是否 ≥50%
5. 新增件逐个点：闪光灯 / 网格（真开关）/ 影调预览 / 31 GB / 镜头切换 / 风格方块 —— 都要有 toast 或可见变化，**不允许"点了没反应"**
6. 焦段药丸的选中态，是否跟参考图一样"转白底黑字"

## 五、下一步（不要一次吞下整个项目）

1. 用户确认原型观感后 → 把模式条四入口 / 焦段条 / 图标行同步到 **Swift 端 UI**（`CameraView.swift` / `ModeSelector.swift`），并改 `CaptureSessionMode` 增加 `logLive` case（置灰）
2. 然后才是 Swift 端 **P1b-2 视频录制**（`MovieCaptureService` + 录制指示，交接单第七节第 3 步）
3. 产品主线顺序：场景/风格拍摄体验（P1 完整闭环）→ P4 修图引擎；Log 实况排最后

## 六、工程纪律（沿用，未变）

- 会话配置顺序 / `CaptureDeviceConfigurator` 唯一 lock / Live Photo 成对提交 / 原始字节直存 —— 见 `docs/04-交接单.md` 第六节 8 条
- UI 不做"点了没反应"；浮层只遮挡不挤压；同一时刻只允许一个扩展浮层展开
- 原型改完必跑 `node tools/check_prototype.js prototype/index.html`（当前全过）
- Swift 改完必跑 `node tools/check_swift.js LumenEdit`

## 七、环境备忘

- 本机：Node 24 / Python 3.10 / Git 2.54；项目目录已 `git init`（改动暂存未提交，是否提交由用户决定）
- `tools/*.txt` 报告被 .gitignore 忽略；`.workbuddy/` 建议后续也加入 .gitignore
- 自检报告最新一份：`tools/check-report-prototype.txt`（2026-09-15 第二轮对标改造后，8 组全过）
- 自检脚本已能自动读顶栏高度（不再写死 `30+44`），并新增「对标参考图元素齐备性」检查（底部 7 图标 / 焦段 4 档 / 电平表 / 三图标 / 影调预览 / 存储 / 镜头切换 / 风格方块）
- 圆盘几何全部从 `:root` 的 `--fd-*` 真读；新增规则 E（圆心 Y 对齐按钮中心 ±1px），规则 D 改为查「圆盘组下沿」
- 截图与浏览器实测：`playwright-cli run-code --filename=tools/shot.js` → `shots/01-normal.png`、`shots/02-dial.png`
- 参考帧并排对比：`python tools/compare_dial.py` → `shots/compare-dial.png`
- 审阅总览图：`python tools/montage.py shots/出图.png "标题A=shots/a.png" "标题B=shots/b.png"`（等高等宽并排 + 标题）
- **一键起/停预览服务**：项目根目录 `start-preview.cmd`
  - 双击即可：起 `prototype`→8123、`shots`→8124，端口已在监听就**复用**，然后打开默认浏览器
  - `start-preview.cmd nobrowser` 只起服务不开浏览器；`start-preview.cmd stop` 停掉两个服务
  - ⚠️ 该文件**必须以 GBK(936) 保存**，且首行 `chcp 936`。中文 Windows 的 cmd 原生码页就是 936；
    若按 UTF-8 保存，cmd 会按 936 解析字节，脚本第一行就报「系统找不到指定的路径」。
    用 `[System.IO.File]::WriteAllText($p, $s, [System.Text.Encoding]::GetEncoding(936))` 重新写回。

> **【任务二（旧构建回跑）结论 · 2026-09-21 深夜】🟢 内存爬升 = 既有 / 🔴 卡死黑屏 = ② 引入**：
> 旧构建 `f5cfda7`（**无预热、无批六任何改动**）+ Mac 补的**同一套 5s 资源账**（独立文件，只读探针，不进主线），
> 用户做同款连切 **155 次**（峰值 6 次/秒）：
> - **内存**：25.5 → **32.4 峰 → 31.1 稳、不回落** —— 与新构建会话 5（26.2→37.6→31.5）**同款** ⇒ **既有**；
>   （新构建会话 3 的 42.6MB 是因那场多了**一次视频录制** + 6 分钟时长）
> - **raw 3 / 打断**：旧构建 **0 次** ⇒ **② 引入**；
> - **掉帧观感（用户补答）**：旧构建那轮「**有些许掉帧卡顿，但恢复比较快**」 ⇒ **轻微掉帧 = 既有**；
>   **卡死黑屏（必须清后台）= ② 引入**（旧构建同节奏 155 次从未卡死）；
> - 计数配平（in 活1 / out 活1）、无配额告警（旧构建无该判据）。
> 原始日志：`reference/batch6-runB-2026-09-21/旧构建-f5cfda7-连切155次-原始.log`（1525 行）+ 摘要「追加⑦」。
